# 史都華平台設計聖經（Stewart Platform Design Bible）

**版本：v1.1a（對齊 v4.3 拓樸 + 致動器實體結構補充）**
**整理者：ChatGPT 工程機械助手**
**最後更新：2025-11-09**

> 檔名建議：`Stewart_Platform_Design_Bible_v1.1a.md`（Markdown + LaTeX，VS Code 可直接預覽）

---

## 版本與變更記錄（簡要）

* **v1.0**：原始整理版（含幾何、運動學、Jacobian 等推導）。
* **v1.1**：對齊用戶提供之 `v4.3 拓樸修正版`（變數命名、節點順序、相位角 θ、半徑命名 R_A/R_B 等）。
* **v1.1a（本檔）**：在 v1.1 基礎上補充並明確說明致動器緩衝在實體硬體中的分配方式，並附上自然高度 $H$ 的具體數值求解演算法（含牛頓法實作與收斂建議）。所有補充段落以 **[v1.1a 補充]** 標記。

---

# 目錄

1. 總覽與符號定義
2. 節點幾何與圓心半徑求解（含數值法）
3. 節點座標與對齊規則（含 v4.3 拓樸）
4. 致動器與平台高度（$h$ 與 $H$）【含 v1.1a 補充】
5. 正向運動學（位置層級）
6. 微分運動學（Jacobian 與速度）
7. 小角線性化與敏感度分析
8. 工作空間、行程檢查與奇異性檢查
9. 數值求解策略與實作建議（含演算法偽碼）
10. 範例（數值驗算示範）
11. 後續延伸項目與檔案／模組建議
12. 參考來源

---

# 1. 總覽與符號定義

史都華平台（6-6 Stewart Platform）是一種 **6 自由度平行機構**，由固定平台（Base）、活動平台（Moving）與六支致動器（actuators）組成。致動器通常是電動缸或伺服螺桿缸，兩端以球鉸或球頭連接上下平台節點。

**主要符號（v1.1a 對齊）**

* 固定平台節點（逆時針編號）：$A_1,;A_2,;\dots,;A_6$。
* 活動平台節點（逆時針編號）：$B_1,;B_2,;\dots,;B_6$。
* 連接拓樸：第 $i$ 支致動器連接 $A_i \leftrightarrow B_i$。
* 固定平台（Base）弦長：短弦 $d_f$、長弦 $D_f$。
* 活動平台（Moving）弦長：短弦 $d_m$、長弦 $D_m$。
* 固定平台半徑：$R_A$（對應 v4.3 的 $R_A$）。
* 活動平台半徑：$R_B$（對應 v4.3 的 $R_B$）。
* 相位角（Phase Angle）：$\theta$（活動平台相對固定平台繞 $Z$ 軸的 yaw 偏移）。
* 致動器：最小長度 $L$（$=L_{\min}$）、行程 $s$、緩衝總量 $s_{\text{buffer}}$（或細分為 $s_{\text{buf,front}}$、$s_{\text{buf,back}}$）。
* 中間長度（用於自然高度計算）：$L_{\text{mid}} = L + s_{\text{buf,back}} + \dfrac{s}{2}$.（對稱時 $L_{\text{mid}} = L + \dfrac{s_{\text{buffer}}}{2} + \dfrac{s}{2}$）
* 初始高度：$h$（當致動器長為 $L$ 時之平台 $Z$）。
* 自然高度：$H$（當致動器長為 $L_{\text{mid}}$ 時之平台 $Z$）。
* 上平台本體座標系下第 $i$ 點：$\mathbf{b}_i$。
* 下平台全域座標系下第 $i$ 點：$\mathbf{A}_i$。
* 上平台於全域座標下第 $i$ 點：$\mathbf{B}_i'$。
* 旋轉矩陣：$R(\alpha,\beta,\gamma) = R_z(\gamma) R_y(\beta) R_x(\alpha)$（Z–Y–X）。

---

# 2. 節點幾何與圓心半徑求解（含數值法）

## 2.1 弦長與圓心角基本關係

任意弦長 $c$ 與圓半徑 $R$ 的圓心角 $\phi$（弧度）關係：

$$
c = 2R \sin\left(\frac{\phi}{2}\right)
\quad\Longrightarrow\quad
\phi = 2\arcsin\left(\frac{c}{2R}\right).
$$

## 2.2 固定平台（A）與活動平台（B）的基礎角

固定平台（A）：
$$
\alpha_A = 2\arcsin\left(\frac{D_f}{2R_A}\right),\qquad
\beta_A  = 2\arcsin\left(\frac{d_f}{2R_A}\right).
$$

活動平台（B）：
$$
\alpha_B = 2\arcsin\left(\frac{D_m}{2R_B}\right),\qquad
\beta_B  = 2\arcsin\left(\frac{d_m}{2R_B}\right).
$$

## 2.3 半徑求解方程（v4.3 規則）

由六個弧角和為 $2\pi$：

固定平台：
$$
3\alpha_A + 3\beta_A = 2\pi
\quad\Longrightarrow\quad
2\arcsin\left(\frac{D_f}{2R_A}\right) + 2\arcsin\left(\frac{d_f}{2R_A}\right) = \frac{\pi}{3}.
\tag{E-RA}
$$

活動平台：
$$
2\arcsin\left(\frac{D_m}{2R_B}\right) + 2\arcsin\left(\frac{d_m}{2R_B}\right) = \frac{\pi}{3}.
\tag{E-RB}
$$

這兩個方程通常透過數值方法（如二分法或牛頓法）求 $R_A$ 與 $R_B$。

---

## 2.4 數值求解（建議：二分法）

二分法實作建議（數值穩定）：

```python
def solve_radius(d, D, tol=1e-9, max_iter=1000):
    import math
    R_low = max(d, D) / 2 * 1.000001
    R_high = R_low * 10.0
    for _ in range(max_iter):
        R_mid = 0.5*(R_low + R_high)
        f_mid = 2*math.asin(d/(2*R_mid)) + 2*math.asin(D/(2*R_mid)) - math.pi/3
        if abs(f_mid) < tol:
            return R_mid
        f_low = 2*math.asin(d/(2*R_low)) + 2*math.asin(D/(2*R_low)) - math.pi/3
        if f_low * f_mid <= 0:
            R_high = R_mid
        else:
            R_low = R_mid
    return R_mid
```

---

# 3. 節點座標與對齊規則（含 v4.3 拓樸）

## 3.1 角度序列（按 v4.3）

* 固定平台（A）節點角序列（逆時針）為短–長–短–長–短–長：

$$
(\beta_A,;\alpha_A,;\beta_A,;\alpha_A,;\beta_A,;\alpha_A)
$$

* 活動平台（B）節點角序列（逆時針）為長–短–長–短–長–短：

$$
(\alpha_B,;\beta_B,;\alpha_B,;\beta_B,;\alpha_B,;\beta_B)
$$

角度累加（索引從 1 到 6）：

$$
\phi_{A,1} = \phi_{A0} \quad\text{(可選初始角)},\quad
\phi_{A,i+1} = \phi_{A,i} + \begin{cases}\beta_A & \text{若為短弦段}[4pt]\alpha_A & \text{若為長弦段}\end{cases}
$$

$$
\phi_{B,1} = \phi_{B0} \quad\text{(可選初始角)},\quad
\phi_{B,i+1} = \phi_{B,i} + \begin{cases}\alpha_B & \text{若為長弦段}[4pt]\beta_B & \text{若為短弦段}\end{cases}
$$

> 設計者常選擇初始角使節點對齊特定方位（例如 $\phi_{A0}$ 使得 $A_1$ 與 $A_6$ 的中點朝向 6 點鐘/南方），v4.3 的檔案中有指定標準方位，請依你的機構需求設定 $\phi_{A0},\phi_{B0}$。

## 3.2 節點座標（XY 平面）

下平台（全域坐標）：

$$
A_i = \begin{bmatrix} R_A \cos\phi_{A,i}[4pt] R_A \sin\phi_{A,i}[4pt] 0 \end{bmatrix}
$$

上平台（本體座標系）：

$$
\mathbf{b}*i = \begin{bmatrix} R_B \cos\phi*{B,i}[4pt] R_B \sin\phi_{B,i}[4pt] 0 \end{bmatrix}
$$

**相位角 ($\theta$)**
 - **固定平台 $\{A\}$ 參考軸：** 節點對 $(A_3, A_4)$ 是完美對稱地分佈在 **Y 軸**兩側的。
 - **活動平台 $\{B\}$ 參考軸：** 節點對 $(B_1, B_6)$ 也是完美對稱地分佈在 **Y 軸**兩側的。
 - **解讀：** 由於 $\{A\}$ 的參考軸 (A3-A4 中心線) 與 $\{B\}$ 的參考軸 (B1-B6 中心線) **完全重疊**（都在 Y 軸上），它們之間沒有 $Yaw$ 扭轉。因此，這張圖展示的是**相位角 $\theta = 0^\circ$** 的情況。

上平台應用相位角 $\theta$（相對 $A$ 的 yaw）與位移 $\mathbf{p}=[x,y,z]^\mathsf{T}$ 後在全域上的座標：

$$
\mathbf{B}_i' = R_z(\theta) \mathbf{b}_i + \mathbf{p}
\quad\text{(若僅考慮零位 yaw 為 $\theta$ 時)}
$$

或一般情況（包含任意三軸旋轉）：

$$
\mathbf{B}_i' = R(\alpha,\beta,\gamma),\mathbf{b}_i + \mathbf{p}
$$

---

# 4. 致動器與平台高度（$h$ 與 $H$）【含 v1.1a 補充】

> 本章整合 v1.1 的定義與 v1.1a 的實體緩衝與求解補充。

## 4.1 致動器長度範圍與實體緩衝（**[v1.1a 補充]**）

### (A) 致動器結構與緩衝定義

實際致動器（電動缸 / 螺桿缸）通常在兩端設有 **緩衝（cushion / buffer）** 或是有機械止擋。為避免設計誤差，需明確在模型中標示緩衝如何分配。

定義：

* 前端緩衝距離：$s_{\text{buf,front}}$（推桿完全伸出端的安全緩衝）
* 後端緩衝距離：$s_{\text{buf,back}}$（推桿完全縮回端的安全緩衝）
* 若將緩衝合稱為 $s_{\text{buffer}}$，可有 $s_{\text{buffer}} = s_{\text{buf,front}} + s_{\text{buf,back}}$。

### (B) 致動器長度上下界（具體表達）

* 最小全長（包含後端緩衝）：
  $$
  L_{\min} = L
  $$
  （此處 $L$ 為設備手冊規定或安裝名義之最短長度，包含後端緩衝）

* 中間長度（自然中點）：
  $$
  L_{\text{mid}} = L + s_{\text{buf,back}} + \frac{s}{2}
  $$
  （若緩衝對稱，$L_{\text{mid}} = L + \dfrac{s_{\text{buffer}}}{2} + \dfrac{s}{2}$）

* 最大全長（包含前端與後端緩衝）：
  $$
  L_{\max} = L + s + s_{\text{buf,front}} + s_{\text{buf,back}}
  $$

**注意**：前一個版本（v1.0 / v1.1）曾以 $L_{\max} = L + 2 s_{\text{buffer}} + s$ 的形式出現，該式等價於假設 $s_{\text{buf,front}} = s_{\text{buf,back}} = s_{\text{buffer}}$（即把每端緩衝都命名為 $s_{\text{buffer}}$）。為避免歧義，建議在設計文件/採購規格中明確指定 $s_{\text{buf,front}}$ 與 $s_{\text{buf,back}}$。

### (C) 緩衝分配建議（工程經驗值）

|      應用場景 | 建議分配                                                                            |
| --------: | :------------------------------------------------------------------------------ |
|     高精度定位 | 對稱分配：$s_{\text{buf,front}} \approx s_{\text{buf,back}} \approx 0.05s$ ～ $0.10s$ |
|   受力碰撞風險高 | 前端緩衝較大：$s_{\text{buf,front}} \approx 0.10s$ ～ $0.15s$                           |
| 空間受限（短行程） | 後端緩衝較大以保護內件：$s_{\text{buf,back}} \approx 0.10s$ ～ $0.20s$                       |

在實際結構與布線（感測器、行程開關、機構干涉）設計前，應與元件供應商確認緩衝機制與實際測試數據。

---

## 4.2 水平投影距離 $D_{xy,i}$（重申）

當活動平台以零位姿（$Roll=0,;Pitch=0,;Yaw=\theta$）時，第 $i$ 根致動器在 XY 投影上的距離為：

$$
D_{xy,i} = \left| (A_i)*{xy} - (B_i)*{xy} \right|
$$

其中 $A_i$、$B_i$ 如第 3 章所定義。

---

## 4.3 初始高度 $h$（定義重申）

當所有致動器長等於 $L_{\min}=L$ 時（平台零位），**平台的最低 $Z$ 高度** 定義為 $h$。若以等長假設（或欲找能使至少最短致動器剛滿足 $L$ 的高度），可根據最小水平投影距離求得：

$$
h = \sqrt{L^2 - \min_i \left( D_{xy,i}^2 \right) }.
\tag{DEF-h}
$$

> *物理解說*：若某一支致動器在 XY 平面的距離為最小值，該致動器在最低高度時最先達到最短長度（$L$），因此由該 $D_{xy,\min}$ 推導得到 $h$。

---

## 4.4 自然高度 $H$ 的數值求解（**[v1.1a 補充]**）

### (A) 自然高度定義（重申）

自然高度 $H$ 定義為在零位姿（$Roll=0,;Pitch=0,;Yaw=\theta$）時，使 6 根致動器的平均長度等於中間長度 $L_{\text{mid}}$ 的 $Z$ 值。形式上：

$$
\frac{1}{6} \sum_{i=1}^6 \sqrt{H^2 + D_{xy,i}^2} = L_{\text{mid}}.
\tag{Eq-H}
$$

此為單一變數非線性方程，需數值解法。

### (B) 轉為求根函數

定義：

$$
f(H) = \frac{1}{6} \sum_{i=1}^6 \sqrt{H^2 + D_{xy,i}^2} - L_{\text{mid}}
$$

求 $H$ 使 $f(H) = 0$。

### (C) 牛頓法（Newton-Raphson）推導與實作

導數為：

$$
f'(H) = \frac{1}{6} \sum_{i=1}^6 \frac{H}{\sqrt{H^2 + D_{xy,i}^2}}.
$$

牛頓疊代：

$$
H_{k+1} = H_k - \frac{f(H_k)}{f'(H_k)}.
$$

#### Python 範例（穩定實作）：

```python
def solve_H(Dxy, Lmid, H0=None, tol=1e-9, max_iter=100):
    """
    求解自然高度 H，使平均 sqrt(H^2 + Dxy_i^2) = Lmid。
    Dxy: iterable of 6 D_xy values
    Lmid: desired mean length
    返回 H (float)
    """
    import math
    Dxy = list(Dxy)
    assert len(Dxy) == 6
    # 初始猜測：若無特殊情況，用 Lmid 作為上界的合理初值
    if H0 is None:
        # 以 Lmid 為初值，或更保守地使用 sqrt(max(0, Lmid^2 - mean(Dxy^2)))
        avgD2 = sum(d*d for d in Dxy) / 6.0
        guess = max(1e-6, math.sqrt(max(0.0, Lmid*Lmid - avgD2)))
        H = guess
    else:
        H = H0
    for _ in range(max_iter):
        terms = [math.sqrt(H*H + d*d) for d in Dxy]
        f = sum(terms)/6.0 - Lmid
        if abs(f) < tol:
            return H
        df = sum(H / t for t in terms) / 6.0
        # 若 df 非常小，改用保守的阻尼步長防止跳躍
        if abs(df) < 1e-12:
            H_new = H - f * 0.5
        else:
            H_new = H - f/df
        # 保持 H 不為負
        if H_new < 0:
            H_new = H * 0.5
        if abs(H_new - H) < tol:
            return H_new
        H = H_new
    # 若未收斂，回傳最後值並可用外層檢查
    return H
```

### (D) 二分法作為替代（保守、必收斂）

若牛頓法在某些病態情況收斂性差，可採二分法（需選定上下界）：

* 下界：$H_{\text{low}} = 0$（或 $h$）
* 上界：$H_{\text{high}} = L_{\text{mid}}$（因 $\sqrt{H^2 + D^2} \ge H$）

對 $f(H)$ 在 $[H_{\text{low}},H_{\text{high}}]$ 進行二分查找直到 $|f(H)|<$ tol。

### (E) 收斂性與檢查

* 解 $H$ 必滿足 $H \ge 0$。
* 收斂後檢查誤差：
  $$
  \max_i \left| \sqrt{H^2 + D_{xy,i}^2} - L_{\text{mid}} \right| < \varepsilon_H
  $$
  建議 $\varepsilon_H = 10^{-6},\text{至},10^{-3}$ mm（依需求）。

---

## 4.5 小結（本章重點整理）

* 致動器緩衝應在設計規格中明確分配為前端與後端（$s_{\text{buf,front}}$、$s_{\text{buf,back}}$），以免混淆。
* $L_{\max} = L + s + s_{\text{buf,front}} + s_{\text{buf,back}}$ 為最一般且準確的表述；對稱情況則可簡化。
* 自然高度 $H$ 由非線性方程（Eq-H）決定，牛頓法是推薦解法（快速且通常穩定），二分法可作保守備用。
* 實務上請在採購與整合文件中同時列出 $s_{\text{buf,front}}$ 與 $s_{\text{buf,back}}$ 數值。

---

# 5. 正向運動學（Forward Kinematics）

## 5.1 平台位姿表示

平台位姿由平移向量與三個歐拉角：

$$
\mathbf{p} = \begin{bmatrix} x \ y \ z \end{bmatrix},\qquad
(\alpha,\beta,\gamma) = (\text{roll},;\text{pitch},;\text{yaw})
$$

旋轉矩陣採 Z–Y–X（yaw–pitch–roll）順序：

$$
R(\alpha,\beta,\gamma) = R_z(\gamma) , R_y(\beta) , R_x(\alpha)
$$

各分矩陣如常見定義。

## 5.2 上平台節點（全域）與致動器長

上平台本體座標下節點：

$$
\mathbf{b}*i = \begin{bmatrix} R_B\cos\phi*{B,i} [4pt] R_B\sin\phi_{B,i} [4pt] 0 \end{bmatrix}
$$

轉換到全域並平移：

$$
\mathbf{B}_i' = R(\alpha,\beta,\gamma),\mathbf{b}_i + \mathbf{p}
$$

致動器長：

$$
\boxed{l_i = | \mathbf{B}_i' - \mathbf{A}_i | }
\tag{FK}
$$

---

# 6. 微分運動學（Jacobian 與速度）

## 6.1 伸長方向單位向量

$$
\mathbf{n}_i = \frac{\mathbf{B}_i' - \mathbf{A}_i}{l_i}
$$

## 6.2 速度關係

若平台速度為線速度 $\dot{\mathbf{p}}$ 與角速度 $\boldsymbol{\omega}$（平台參考系或全域需一致），則：

$$
\dot{l}_i = \mathbf{n}_i^\mathsf{T}\dot{\mathbf{p}} + \mathbf{n}_i^\mathsf{T}\big(\boldsymbol{\omega}\times (R\mathbf{b}_i)\big)
$$

等價寫法：

$$
\dot{l}_i = \mathbf{n}_i^\mathsf{T}\dot{\mathbf{p}} + (\mathbf{n}_i \times R\mathbf{b}_i)^\mathsf{T}\boldsymbol{\omega}
$$

## 6.3 Jacobian（第 i 行）

$$
J_i = \begin{bmatrix} \mathbf{n}_i^\mathsf{T} & (\mathbf{n}_i \times R\mathbf{b}_i)^\mathsf{T} \end{bmatrix}
$$

整體 $6\times6$ 矩陣：

$$
\dot{\mathbf{l}} = J,\boldsymbol{\nu},\qquad \boldsymbol{\nu} = \begin{bmatrix}\dot{\mathbf{p}} \ \boldsymbol{\omega}\end{bmatrix}
$$

---

# 7. 小角線性化與敏感度分析

當 $\alpha,\beta,\gamma \approx 0$ 時：

$$
R \approx I + [\boldsymbol{\theta}]*\times,\qquad
[\boldsymbol{\theta}]*\times =
\begin{bmatrix}
0 & -\gamma & \beta\
\gamma & 0 & -\alpha\
-\beta & \alpha & 0
\end{bmatrix}
$$

一階近似下：

$$
\Delta l_i \approx \mathbf{n}_i^\mathsf{T}\Delta\mathbf{p} + (\mathbf{n}_i \times \mathbf{b}_i)^\mathsf{T}\Delta\boldsymbol{\theta}
$$

此式常用於剛性分析、控制器線性設計與靈敏度估算。

---

# 8. 工作空間、行程檢查與奇異性檢查

## 8.1 行程檢查

對任意位姿計算 $l_i$，必須滿足：

$$
L_{\min} \le l_i \le L_{\max},\quad \forall i
$$

並在必要時考慮安全 margin：

$$
L_{\min}+s_{\text{margin}} \le l_i \le L_{\max}-s_{\text{margin}}
$$

## 8.2 奇異性檢查

計算 Jacobian 的奇異值：

* 若 $\sigma_{\min}(J) \le \varepsilon_\sigma$ 或條件數 $\kappa$ 很大，則位姿接近奇異。
* 建議監控 $\sigma_{\min}$ 與 cond(J) 於工作空間掃描中。

---

# 9. 數值求解策略與實作建議（含演算法偽碼）

總流程（簡要）：

1. 輸入：$d_f,D_f,d_m,D_m,\theta,L,s,s_{\text{buf,front}},s_{\text{buf,back}}$。
2. 求 $R_A$、$R_B$（式 E-RA、E-RB）。
3. 累加角度：產生 $\phi_{A,i}$ 與 $\phi_{B,i}$。
4. 計算 $A_i$、$\mathbf{b}*i$，及 $D*{xy,i}$。
5. 計算 $h$（式 DEF-h）與用牛頓/二分法求 $H$（式 Eq-H）。
6. 建立 FK 與 Jacobian，進行行程與奇異性檢查。

範例偽碼片段已在前面章節提供（求解半徑、求解 $H$）。

---

# 10. 範例（數值驗算示範）

**示例數值（僅供檢驗）**

* $d_f = 120$ mm, $D_f = 200$ mm
* $d_m = 110$ mm, $D_m = 180$ mm
* $\theta = 20^\circ$
* $L = 300$ mm, $s = 100$ mm, $s_{\text{buf,front}} = s_{\text{buf,back}} = 10$ mm

**步驟（簡述）**

1. 求 $R_A,R_B$（使用 `solve_radius`）。
2. 產生 $\phi_{A,i}$、$\phi_{B,i}$，計算 $A_i$、$B_i$（XY）。
3. 計算 $D_{xy,i}$，以此求 $h$ 與 $H$（用 `solve_H`）。
4. 驗證 $l_i(H)$ 與 $L_{\text{mid}}$ 差異小於容差。

（若你要我直接用上述假設數值跑一次並回報每支致動器的數值結果、Jacobian 與最小奇異值，我可以直接產出數表。）

---

# 11. 後續延伸項目與檔案／模組建議

建議開發模組（含檔名與版本）：

* `geometry_v1.1a.py`：計算 $R_A,R_B$、產生角度序列、節點座標與 $D_{xy}$。
* `kinematics_v1.1a.py`：`forward_kinematics()`、`compute_jacobian()`、`solve_H()`。
* `workspace_v1.1a.py`：掃描工作空間、檢查奇異、行程合規性。
* `docs/Stewart_Platform_Design_Bible_v1.1a.md`（本檔）與 `Stewart_Platform_Design_Bible_v1.1a.pdf`（可按需生成）。

版本命名規則遵循你之前指定：**大改版進位（1→2）；小幅進展用小數點（1.1→1.2…）**。本次為小幅補充，版號為 **v1.1a**（等同於 v1.1 的 micro-update）。

---

# 12. 參考文獻

1. Stewart, D. (1965). *A Platform with Six Degrees of Freedom.* Proc. Inst. Mech. Eng.
2. Merlet, J.-P. (2006). *Parallel Robots*. Springer.
3. Tsai, L.-W. (1999). *Robot Analysis: The Mechanics of Serial and Parallel Manipulators*.
4. 使用者提供文件：**6-DOF 史都華平台完整幾何參數定義 v4.3 拓樸修正版**（作為本版命名/拓樸規範依據）。

---
