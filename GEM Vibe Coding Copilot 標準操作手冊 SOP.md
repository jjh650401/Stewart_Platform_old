# GEM Vibe Coding Copilot 標準操作手冊 (SOP)

**版本**：v1.0
**適用對象**：Vibe Coding 開發者 (使用 Google Gemini "Gems")
**核心目的**：確保每次開發時，AI 都在「邏輯最清晰」且「規則最明確」的狀態下工作，避免長對話導致的記憶混淆。

---

## 核心概念

1. **Gem (寶石)**：您的專屬 AI 助手（已內建 v8.0 規則）。它負責「記住規則」。
2. **New Chat (新對話)**：每次開發的工作區。它負責「保持邏輯純淨」。
3. **黃金法則**：**「一個功能，一個對話」**。當一個功能開發完成並測試通過後，請果斷開啟新對話進行下一個任務。

---

## 操作流程圖 (Workflow)

### 步驟 1：啟動專屬環境

* **動作**：進入 Google Gemini (gemini.google.com)。
* **操作**：在左側選單點擊您建立的 Gem **「20251228 Vibe Coding Copilot」**（或您命名的名稱）。
* **注意**：**不要**點擊普通的 "New Chat"，否則 AI 會忘記您的 v8.0 規則。

### 步驟 2：上下文注入 (Context Loading)

* *這是最關鍵的一步。因為新對話是「空白」的，您必須把目前的進度「餵」給 AI。*
* **動作**：找出您電腦中目前**最新、可運行**的程式碼檔案。
* **提示詞 (Prompt) 範本**：

> 這是目前 `[專案名稱]` 的最新進度。
> 檔案名稱：`[例如：main.py]`
> 內容如下：
>
> ```python
> (在此貼上完整程式碼)
> 
> ```
>
>
> 請確認你已讀取並理解目前的程式碼邏輯。確認後請回答「收到」，我們先不要進行修改。

### 步驟 3：指派任務 (Task Assignment)

* **動作**：當 AI 回覆「收到」後，發布本次的新任務。
* **提示詞範本**：

> 我現在要進行下一個任務。
> **任務說明**：`[例如：幫 Video Downloader 增加一個進度條顯示的功能]`
> 請依照 v8.0 規則，先進行 Deep Think 並列出修改計畫。

### 步驟 4：執行與迭代

* **AI 行為**：AI 會依照 `<workflow_engine>` 輸出計畫 -> 等您同意 -> 輸出代碼。
* **您的動作**：將代碼複製回 VS Code -> 執行測試。
* **若成功**：任務結束，準備進入步驟 5。
* **若報錯**：將錯誤訊息貼回給 AI，執行 **[Search First]** 策略修復。

### 步驟 5：歸檔與重啟 (Reset)

* **判斷點**：
* ✅ 當前功能已完美運行。
* ⚠️ 對話過長，AI 開始鬼打牆或遺忘前面的設定。
* 🔄 要切換到另一個完全不同的專案 (例如從 Video Downloader 切換到 Stewart Platform)。

* **動作**：

1. 確認 VS Code 中的檔案已儲存且是最新版本。
2. **關閉當前對話**。
3. **回到步驟 1**，重新點擊 Gem 開啟新對話，並貼上最新的檔案。

---

## 常見情境 Q&A

**Q1: 如果我的專案有多個檔案 (例如 `main.py` 和 `utils.py`) 怎麼辦？**

* **A**: 在 **步驟 2** 時，分次貼上，或者在一個 Prompt 中貼上但標示清楚。
* *話術*：「這是 `main.py` (貼上)... 接著這是 `utils.py` (貼上)... 請整合這兩個檔案的上下文。」

**Q2: 為什麼不一直在同一個對話裡聊下去？**

* **A**: 對話越長，AI 的「注意力」越分散，且舊的錯誤代碼會干擾新的邏輯。重開對話能確保 AI 永遠只看到「最新、最正確」的版本，效率最高。

**Q3: Gem 的內容需要更新嗎？**

* **A**: 只有當您想修改「根本規則」（例如換了電腦、改變了資安紅線）時，才需要去編輯 Gem 的 Instructions。日常開發不需要動它。

---

## 您的 Vibe Coding 檢查清單

* [ ] 是否使用了專屬 Gem 開啟對話？
* [ ] 是否已貼上最新的完整程式碼？
* [ ] 任務描述是否清晰？
* [ ] AI 是否有先給出「修改計畫」才寫 Code？(若無，請提醒它遵守 v8.0)
* [ ] 功能完成後，是否已開啟新對話？
