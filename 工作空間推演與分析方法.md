# 📗 史都華平台工作空間與極限機械空間分析說明（基於 v4.3 拓樸修正版）

**文件版本：v1.0**  
**幾何基準：依《6-DOF 史都華平台完整幾何參數定義 v4.3 拓樸修正版》一致**  
**適用範圍：對稱式 6-6 史都華平台（Short–Long / Long–Short 交錯拓樸）**

---

## 一、分析目的與定義

### 1.1 目的
本文件用於說明史都華平台在給定幾何參數、行程限制與力學條件下的 **工作空間（Workspace）** 與 **極限機械空間（Mechanical Envelope）** 推導與分析方法。

---

### 1.2 名詞定義

| 名稱 | 符號 | 說明 |
|:--|:--|:--|
| 下平台節點 | $A_1 \sim A_6$ | 位於半徑 $R_A$ 的圓上，編號採 **逆時針** 方向 |
| 上平台節點 | $B_1 \sim B_6$ | 位於半徑 $R_B$ 的圓上，編號採 **逆時針** 方向 |
| 相位角 | $\theta$ | 上、下平台節點的偏移角（Yaw 預置角） |
| 平台中心距 | $h$ | 上、下平台圓心距離（沿 Z 軸） |
| 平台最低高度 | $h_{\min}$ | 缸長 = $L_{\min}$ 時的高度 |
| 平台自然高度 | $H$ | 缸長 = $L_{\text{mid}} = L + s_\text{buf} + s/2$ 時的高度 |
| 行程 | $s$ | 可動行程長度 |
| 緩衝距離 | $s_\text{buf}$ | 上下限安全緩衝距離 |
| 缸最小長度 | $L_{\min} = L$ | 名義長度（初始） |
| 缸最大長度 | $L_{\max} = L + 2s_\text{buf} + s$ | 全伸長位置 |

---

## 二、平台幾何基礎方程

### 2.1 上、下平台節點座標

下平台節點（固定）：
$$
A_i = 
\begin{bmatrix}
R_A \cos\phi_i\\[4pt]
R_A \sin\phi_i\\[4pt]
0
\end{bmatrix}
,\quad i = 1,\dots,6.
$$

上平台節點（在自身平台座標系）：
$$
b_i =
\begin{bmatrix}
R_B \cos\psi_i\\[4pt]
R_B \sin\psi_i\\[4pt]
0
\end{bmatrix}
,\quad i = 1,\dots,6.
$$

---

### 2.2 上平台全域座標
若上平台的位姿為：
$$
\mathbf{p} = 
\begin{bmatrix}
x\\y\\z
\end{bmatrix},\quad
R = R_z(\gamma) R_y(\beta) R_x(\alpha),
$$
則上平台節點在全域座標為：
$$
B_i' = R\,b_i + \mathbf{p}.
$$

---

### 2.3 缸長定義
第 $i$ 支缸的實際長度為：
$$
l_i(\mathbf{p},\alpha,\beta,\gamma) = 
\big\|\,R(\alpha,\beta,\gamma)\,b_i + \mathbf{p} - A_i\,\big\|.
$$

---

## 三、幾何條件下的工作空間限制

### 3.1 行程約束式
$$
L_{\min} \le l_i(\mathbf{p},\alpha,\beta,\gamma) \le L_{\max}, \quad i=1,\dots,6.
$$

---

### 3.2 平行狀態下的解析式（僅沿 Z 軸變化）
當 $x=y=\alpha=\beta=\gamma=0$ 時，平台上下圓心共軸，則：
$$
l_i^2 = D_i^2 + h^2,
$$
其中  
$$
D_i^2 = R_A^2 + R_B^2 - 2R_A R_B \cos(\psi_i - \phi_i).
$$

對第 $i$ 支缸可求得 $h$ 的可行範圍：
$$
h \in 
\Big[
\sqrt{\max(0,L_{\min}^2 - D_i^2)},\;
\sqrt{\max(0,L_{\max}^2 - D_i^2)}
\Big].
$$

整體可行高度為：
$$
h_{\min} = \max_i \sqrt{\max(0,L_{\min}^2 - D_i^2)},\quad
h_{\max} = \min_i \sqrt{\max(0,L_{\max}^2 - D_i^2)}.
$$

若 $h_{\min} > h_{\max}$，代表機構無可行的平行同心狀態。

---

## 四、一般位姿下的工作空間條件

### 4.1 可行性判定
對任意位姿 $(x,y,z,\alpha,\beta,\gamma)$，  
若全部缸長滿足：
$$
L_{\min} + s_{\text{margin}} \le l_i \le L_{\max} - s_{\text{margin}},
$$
則該位姿為 **幾何可行位姿**。

---

### 4.2 數值求解與分析方式
常用方法：

1. **格點掃描法（Grid Scan）**  
   - 對 $x,y,z$（及姿態角）建立離散網格。  
   - 每點計算 $l_i$，檢查是否在允許行程內。  
   - 可得可行點雲、切面圖或投影圖。

2. **邊界交集法（快速解析法）**  
   - 對每個 $x,y,R$，計算  
     $$
     D_i^2(x,y,R) = (R_B\cos\psi_i' - R_A\cos\phi_i')^2 + (R_B\sin\psi_i' - R_A\sin\phi_i')^2,
     $$
     然後求：
     $$
     h_{\min}(x,y) = \max_i\sqrt{\max(0,L_{\min}^2 - D_i^2)},\quad
     h_{\max}(x,y) = \min_i\sqrt{\max(0,L_{\max}^2 - D_i^2)}.
     $$

3. **優化法（Optimization Method）**  
   - 若要求最大/最小 z（或 α,β,γ），則設為非線性最佳化問題：  
     $$\max h$$ subject to 缸長約束。  
   - 可使用數值求解器（如 IPOPT、trust-region、Levenberg–Marquardt）。

---

## 五、Jacobian 奇異性分析

### 5.1 單支缸方向向量
$$
\mathbf{n}_i = \frac{R\,b_i + \mathbf{p} - A_i}{l_i}.
$$

---

### 5.2 Jacobian 定義
$$
J_i = [\mathbf{n}_i^\top \quad (\mathbf{n}_i \times Rb_i)^\top],
\quad
J =
\begin{bmatrix}
J_1\\J_2\\ \vdots\\ J_6
\end{bmatrix}.
$$

---

### 5.3 奇異性檢查
對每個可行位姿計算 Jacobian 的奇異值分解：
$$
J = U\Sigma V^\top, \quad \Sigma = \text{diag}(\sigma_1, \dots, \sigma_6).
$$
若 $\sigma_{\min}(J) < \varepsilon$，則該位姿接近奇異。  
可利用條件數 $\kappa = \sigma_{\max}/\sigma_{\min}$ 反映平台剛性。

---

## 六、力學可行性（受力限制）

### 6.1 平台受力平衡
若上平台受外力／力矩六維向量：
$$
\mathbf{w} = [F_x, F_y, F_z, M_x, M_y, M_z]^\top,
$$
則需存在缸力向量 $\mathbf{f} = [f_1,\dots,f_6]^\top$ 滿足：
$$
\mathbf{w} = J^\top \mathbf{f}.
$$

---

### 6.2 可行力條件
若每支缸力限制為 $|f_i| \le f_{i,\max}$，  
則該位姿在力學上可行當且僅當：
$$
\exists \mathbf{f}:\; \mathbf{w} = J^\top \mathbf{f},\quad |f_i| \le f_{i,\max}.
$$

可用線性或二次規劃（QP）檢查是否存在滿足條件的解。

---

### 6.3 最大可承受外力
對給定位姿，可解以下最優化問題：
$$
\max_{\mathbf{w}} \|\mathbf{w}\|_2
\quad \text{s.t.}\quad |(J^\top)^{-1}\mathbf{w}| \le f_{i,\max}.
$$

此結果可繪製成 **受力能力包絡圖（Force Capability Map）**。

---

## 七、分析流程建議

1. **建立幾何模型**  
   - 定義 $R_A, R_B, \theta, d_A, D_A, d_B, D_B$。  
   - 建立節點角度與座標。  

2. **行程分析（純 Z 向）**  
   - 以第 3 章公式解析求得 $h_{\min}$ 與 $h_{\max}$。  

3. **全域空間掃描**  
   - 以離散網格方式檢查可行位姿，繪製工作空間點雲。  

4. **Jacobian 奇異性檢查**  
   - 對每個可行點計算 $\sigma_{\min}(J)$、條件數 $\kappa$。  

5. **力學可行性**  
   - 對可行區域中的代表點計算可承受外力區域。  

6. **整合輸出**  
   - 繪製 x–y 投影圖、z–α 切片圖、條件數熱圖、力域可行圖。

---

## 八、圖形化輸出建議

| 類型 | 說明 |
|:--|:--|
| **工作空間點雲** | 顯示所有可行位姿（以顏色表示 z 或姿態角） |
| **高度切片圖** | 固定 α,β,γ，顯示 x–y 投影可行範圍 |
| **奇異性熱圖** | 顯示 $\sigma_{\min}$ 或 cond(J) 分佈 |
| **力學可行域圖** | 顯示在不同位姿下可承受最大外力或力矩的範圍 |

---

## 九、實務檢查項目（建議流程）

| 檢查項目 | 檢查目標 |
|:--|:--|
| 初始高度 $h_{\min}$ 是否在可行域內 | 確認機構裝配無干涉 |
| 各缸行程利用率 | 評估是否接近端點 |
| 奇異點分佈 | 確保操作區域避開奇異區 |
| 最大外力檢查 | 評估缸力與結構限制 |
| 剛性分析 | 評估工作空間中條件數最差位置 |

---

## 十、結論

- 採用與《v4.3 拓樸修正版》一致的幾何與變數定義，可統一平台設計與分析資料。  
- 平行同心解析式可快速檢查初步幾何合理性。  
- 經 Jacobian 奇異性與力域檢查後，可劃定安全工作空間。  
- 本文件可直接作為控制器可行域參考與平台設計驗證依據。

---

**文件版本：v1.0**  
**版權與用途**：僅供工程設計與分析用，可作為《史都華平台設計聖經 v1.1》附錄文件。
