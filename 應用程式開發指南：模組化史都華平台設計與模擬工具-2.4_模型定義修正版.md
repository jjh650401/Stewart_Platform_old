### **第一部分：版本變更與開發歷程 (Preamble)**

這部分是文件的核心靈魂，記錄了專案如何從 `v2.2` 的失敗中重生，直到 `v2.4` 的功能完備。我已詳細撰寫了 `v2.3` (核心修復) 與 `v2.4` (自動展演) 的完整日誌。

---
**(檔案第一部分開始)**

# **應用程式開發指南：模組化史都華平台設計與模擬工具**

**版本：2.4 (自動展演與穩定性修正版)**
**日期：2025年11月17日**

**目標：** 為遊樂設備行業的技術人員，打造一款基於 Python 的視窗版桌面應用程式，用於史都華平台 (Stewart Platform) 的參數化設計、性能分析、動態模擬與驗證，並整合獨立的電動缸驅動系統設計功能。

**[開發者註記：版本變更日誌 (累積所有版本)]**

  - **[新增] [從 v2.3 模型定義修正版 -\> v2.4 自動展演與穩定性修正版]**

      - **[核心修復] `kinematics.py` 求解器與邊界強化**
          - **問題診斷**：在 `v2.3` 修正模型定義後，發現：
          - 1. 啟用關節限制時，「計算零位」因初始點違反物理約束而失敗；
          - 2. 工作空間分析的角度結果偶爾出現非物理的巨大數值 (±1000°)。
          - **解決方案**：
            1.  **移除 H 計算約束**：在 `_calculate_geometry_6dof` 中移除了物理約束檢查。確立了「零位高度 H 是一個幾何定義 (`avg(L)=L_mid`)，不應受行程或角度限制阻擋」的原則。
            2.  **收緊求解器邊界**：在 `_get_workspace_variable_bounds` 中，將角度搜尋範圍從 `±π` 強制收緊為 `±85°` (約 1.48 rad)，防止 `SLSQP` 求解器發散至非物理空間。
            3.  **修正 3-DOF 雙重扣減**：修改 `_analyze_workspace_3dof`，使其直接回傳絕對座標，交由 UI 層統一處理相對座標轉換，解決了 Heave 數值顯示錯誤與 3D 預覽塌陷問題。

      - **[UI修復] 解決雙重單位轉換與邏輯錯誤**
          - **單位修正**：在 `main_window.py` 中移除了多餘的 `rad2deg` 轉換，解決了 `analysis_widget.py` 收到度數後再次轉換導致 `±1000°` 顯示錯誤的問題。
          - **按鈕解鎖**：修正了 `main_window.py` 的按鈕啟用邏輯，使其在 3-DOF 模式下不再檢查 `Ra/Rb`，解決了「計算零位」按鈕無法使用的問題。
          - **滑塊修正**：修正了 `analysis_widget.py` 中 `_apply_slider_value` 的計算錯誤，移除了負值時多餘的 `* -1`，解決了自動展演時負半週期方向錯誤的問題。

      - **[功能新增] 自動姿態展演 (Auto-Demo)**
          - **新增模式**：在 `analysis_widget.py` 中實作了三種展演模式：
            1.  **自訂勾選 (手動選擇)**：維持原有功能，僅驅動勾選的軸。
            2.  **單軸極限順序 (自動)**：依序驅動每一個自由度 (0 $\to$ Max $\to$ Min $\to$ 0)。
            3.  **畫圓/畫球繞行 (自動)**：控制 Pitch (Sin) 與 Roll (Cos) 進行相位差運動，產生平滑的繞圓效果 (Gyroscopic Motion)。
          - **UX 優化**：將「展演時間」重新定義為「週期時間 (Cycle Time)」，確保不同模式下的運動節奏一致。

  - **[從 v2.2 診斷失敗版 -\> v2.3 模型定義修正版]**

      - **[核心幾何模型重構]** 根據 `v4.3` 和 `v4.6` 幾何定義文件，對 `kinematics.py` 進行了根本性的重構。

          - **關鍵挑戰**：`v2.1` 版本的 `kinematics.py` 幾何模型與 `v4.3` 拓樸文件不符，導致「啟用關節角度限制」時求解器必定失敗。
          - **解決方案**：
            1.  **`_calculate_radius_from_chords` (6-DOF)**：從舊的 `alpha + beta = 120 度` 邏輯，重寫為使用 `scipy.optimize.minimize` 求解 `arcsin(D/(2R)) + arcsin(d/(2R)) = pi/3`，以匹配 `v4.3` 的 3 長 3 短交錯拓樸。
            2.  **`_get_6dof_nodes`**：重寫為使用「角度累加法」 (`[0, beta, beta+alpha, ...]`)，並**移除**了錯誤的 `stagger` 計算。
            3.  **`_get_3dof_nodes`**：根據 `v4.6` 文件，將節點座標系原點從「外接圓圓心」修正為「**質心**」（例如 Y 座標為 `-D2/3` 和 `2*D2/3`）。
          - **[註記：已解決]** 此項重構成功解決了 `Ra`, `Rb` 和 `H`（零位高度）的計算問題。

      - **[核心分析功能除錯]** 解決工作空間分析的錯誤數值問題。

          - **根本原因診斷**：失敗的**真正根源**在於 `_get_6dof_nodes` 函式**錯誤地**計算並應用了一個「內建交錯角 `stagger`」，這與 `v4.3` 規格書中「相位角 $\theta$ 是輸入參數」的定義**完全矛盾**。這種模型與分析之間的定義衝突，導致求解器在一個數學上不一致的模型中運算，最終崩潰。

  - **[從 v2.0 最終統一版 -\> v2.1 單位系統審計修正版]**

      - **[全域單位系統審計與穩定性修正]** 完成了對整個專案的 `mm` 單位系統進行的全面審計、除錯與穩定性強化。
          - **關鍵挑戰**：在 `v2.0` 完成單位正常化重構後，專案出現了一系列頑固且相互關聯的錯誤，包括 UI 介面單位顯示混亂、3D 預覽模型變形、以及求解器計算發散。
          - **問題診斷**：日誌數據證明，工作空間分析失敗的**根本原因**在於 `_find_limit` 方法中，對求解器的「目標函式」應用了錯誤的尺度還原。
          - **最終解決方案**：
            1.  **`kinematics.py`**：重構 `load_parameters` 支援舊檔自動轉換；重寫 `_find_limit` 實作正確的變數正規化。
            2.  **`pyvista_widget.py`**：加入 `MM_TO_M` 尺度轉換。
            3.  **`main_window.py`**：加入舊檔案轉換提示。
      - **[註記：已解決]** 所有與 `mm` 單位系統轉換相關的已知問題均已解決。

  - **[從 v1.23 重整版 -\> v2.0 最終統一版]**
      - **[文件最終校正與註記]** 完成了《開發指南》與《計算指南 2.0》的最終內容對齊。將舊變數（如 `moving_part_mass`）更新為統一符號（`m`），並替換了核心動力學模型。

  - **[從 v1.22 穩定版 -\> v1.23 彙整版]**
      - **[資料整合]** 解決了先前版本內容省略與重複的問題，確保 v1.19 至 v1.22 所有內容無縫整合。

  - **[從 v1.21 穩定版 -\> v1.22 穩定版]**
      - **[資料庫優化]** 支援外部 Excel 匯入，並實作了依據計算結果篩選馬達、螺桿、皮帶輪的功能。
      - **[GUI 優化]** 在「零件篩選結果」區塊新增多重排序按鈕。

  - **[從 v1.21 穩定版 -\> v1.20 穩定版]**
      - **[核心功能擴充]** 導入通用角度計算模型，修正了 $F_s$ 與 $F_{eq}$ 的計算錯誤。
      - **[核心功能擴充]** 新增需求動態負荷 ($C_{ac}$) 計算與速度校核功能。

  - **[從 v1.19 穩定版 -\> v1.20 穩定版]**
      - **[驅動系統重構]** 導入「自動模式」與「手動模式」雙重計算邏輯，並支援獨立 `.dsd` 存檔。
      - **[儀表板介面]** 導入 `QStackedWidget` 主佈局，建立專用的驅動系統設計儀表板。

  - **[從 v1.18 開發分支 -\> v1.19 穩定版]**
      - **[UI 導航重構]** 廢除頁籤，改用 `QMenuBar` 導航，並實作獨立滾動區 `QScrollArea`，解決佈局問題。

  - **[從 v1.12 -\> v1.18 開發分支]**
      - **[驅動系統模組]** 新增完整的驅動系統計算模組，包含資料庫 (`database_manager.py`)、計算引擎 (`drive_system_engine.py`) 與 UI (`drive_system_widget.py`)。

  - **[從 v1.11 -\> v1.12 穩定版]**
      - **[新增分析]** 新增「平台節點擺角範圍分析」功能。
      - **[報告生成]** 新增 `report_generator.py`，支援生成 PDF 設計報告。

  - **[從 v1.10 -\> v1.11 穩定版]**
      - **[功能新增]** 新增可選式「顯示節點座標」功能。
      - **[架構重構]** 將核心參數全面遷移至 `config.py` 集中管理；對 UI 進行依賴注入重構。

  - **[從 v1.9 -\> v1.10 穩定版]**
      - **[模型修正]** 引入「上置式/下置式」關節安裝選項，修正物理模型誤差。
      - **[術語統一]** 全局統一使用「固定平台/活動平台」。

  - **[從 v1.8 -\> v1.9 穩定版]**
      - **[功能新增]** 新增「可選式關節角度限制分析」。
      - **[核心修正]** 引入 `_find_feasible_initial_pose` 預處理，提升工作空間分析穩定性。

  - **[從 v1.7 -\> v1.8 穩定版]**
      - **[功能升級]** 全域分析升級為可選模式（靜態/動態）。
      - **[修正]** 修正 `QThread` 生命週期問題。

  - **[從 v1.6 -\> v1.7 穩定版]**
      - **[核心重構]** 導入四層架構 (`StateManager`, `CoreEngine`, `AnalysisEngine`, `MainWindow`)。
      - **[功能新增]** 實作全域最大靜載分析 (LHS+SQP)。

  - **[從 v1.5 -\> v1.6 正式版]**
      - **[核心修正]** 修正狀態管理與初始化流程；標準化平台方位定義。

---

### **第二章：專案目錄架構**

**[v2.4 更新]** 此架構反映了目前的 29 個檔案結構，確保所有功能模組（包含資料庫管理與自訂元件）皆已就位。

```
D:\PYTHON_PROGRAMS\STEWART_PLATFORM\
│
├── .venv/                  # Python 虛擬環境
├── data/                   # 數據存儲
│   └── projects/           # 預設專案存檔路徑
├── src/
│   ├── core/               # [核心層] 負責運算、邏輯與數據管理
│   │   ├── __init__.py
│   │   ├── config.py             # 中央參數設定檔 (v1.11+)
│   │   ├── analysis_engine.py    # 分析引擎 (工作空間/全域出力/擺角)
│   │   ├── database_manager.py   # 資料庫管理器 (SQLite)
│   │   ├── dynamics_engine.py    # 動力學計算引擎
│   │   ├── drive_system_engine.py # 驅動系統計算引擎
│   │   ├── kinematics.py         # 運動學核心 (幾何/逆向/正向/求解器)
│   │   ├── report_generator.py   # PDF 報告生成器
│   │   └── state_manager.py      # 狀態管理器 (Single Source of Truth)
│   └── gui/                # [介面層] 負責顯示與使用者互動
│       ├── __init__.py
│       ├── controls/       # [控制元件] 各功能面板的實作
│       │   ├── __init__.py
│       │   ├── analysis_widget.py          # 姿態控制、展演與結果顯示
│       │   ├── database_management_widget.py # 資料庫維護視窗
│       │   ├── dynamics_widget.py          # 動力學參數與結果
│       │   ├── drive_system_widget.py      # 驅動系統設計儀表板
│       │   ├── master_geometry_widget.py   # 幾何設計主容器
│       │   ├── six_dof_widget.py           # 6-DOF 參數輸入
│       │   └── three_dof_widget.py         # 3-DOF 參數輸入
│       ├── geometry_widget.py    # UI 工廠 (產生通用輸入欄位)
│       ├── main_window.py        # 主視窗、選單與導航邏輯
│       └── pyvista_widget.py     # 3D 視覺化視窗 (PyVista)
│   └── utils/              # [工具層] 通用輔助工具
│       ├── __init__.py
│       └── custom_widgets.py     # 自訂 UI 元件 (如支援雙擊的 SpinBox)
├── tests/                  # 單元測試
├── .gitignore              # Git 忽略清單
├── main.py                 # 程式進入點
└── requirements.txt        # 專案依賴套件清單
```

-----

### **第三章：專案初始化與環境設定**

  - **環境需求**：

      - **Python 版本**：建議使用 Python 3.9 或更高版本。
      - **依賴安裝**：使用 `requirements.txt` 安裝以下函式庫：
          - `PyQt6` (GUI 框架)
          - `PyVista` 與 `PyVistaQT` (3D 顯示)
          - `NumPy` 與 `SciPy` (科學計算與優化求解)
          - `Pandas` 與 `openpyxl` (Excel 資料處理)
          - `reportlab` (PDF 報告生成)
      - **安裝指令**：`pip install -r requirements.txt`。

  - **初始化步驟**：

      - **專案啟動**：執行 `main.py` 啟動應用程式。
      - **配置文件**：`config.py` 集中管理所有預設參數（如求解器公差、介面尺寸）。
      - **資料庫**：首次運行時，`database_manager.py` 會自動建立 `stewart_platform.db`。

-----

### **第四章：核心理念與技術棧**

  - **核心理念**：

      - **模組化架構**：嚴格區分核心計算 (`src/core`) 與介面顯示 (`src/gui`)，透過 `StateManager` 進行數據同步。
      - **單位統一 (mm)**：整個專案（除 3D 渲染內部轉換外）統一使用**毫米 (mm)** 作為長度單位，杜絕單位混淆。
      - **配置集中化**：所有靜態參數與演算法設定皆由 `config.py` 管理，不寫死在邏輯代碼中。
      - **物理準確性**：採用基於 `Scipy` 的數值求解器 (`minimize`, `least_squares`) 處理非線性幾何問題，並實作真實的物理約束（行程、角度）。

  - **技術棧**：

      - **計算核心**：NumPy (矩陣運算), SciPy (非線性優化 SLSQP/LM)。
      - **GUI 框架**：PyQt6。
      - **3D 引擎**：PyVista (VTK wrapper)。

-----

### **第五章：應用程式架構與模組設計**

  - **整體架構**：

      - **主視窗 (`MainWindow`)**：作為應用程式的骨幹，管理選單、工具列以及不同功能模式（幾何/動力/驅動）的切換。
      - **狀態管理 (`StateManager`)**：作為「唯一數據中心 (Single Source of Truth)」，負責儲存跨模組共享的數據（如分析結果、檔案路徑）。
      - **執行緒模型**：耗時的計算（如工作空間分析）由 `AnalysisEngine` 在獨立的 `QThread` 中執行，避免凍結 UI。

  - **關鍵模組職責**：

      - **`kinematics.py`**：封裝所有幾何與運動學演算法。負責計算 $H$、節點座標、逆向/正向運動學，以及工作空間邊界求解 (`_find_limit`)。**[v2.3]** 已修正幾何模型與求解器穩定性。
      - **`analysis_widget.py`**：**[v2.4 增強]** 負責姿態控制滑塊與自動展演邏輯。包含單軸順序、畫圓繞行等自動化模式的實作。
      - **`drive_system_engine.py`**：獨立的電動缸選型計算引擎，實作七段式運動負載模型與壽命估算。
      - **`analysis_engine.py`**：整合後的分析工作者，負責執行 LHS+SQP 搜尋與擺角分析。

---

### **第六章：變數與參數定義**

**[v2.3/v2.4 標準]** 本章節定義全專案通用的變數標準。根據 `config.py` 與核心架構，所有長度單位均為 **毫米 (mm)**，角度為 **度 (deg)** (UI層) 或 **弧度 (rad)** (計算層)。

#### **6.1 平台幾何參數 (Geometry)**

**A. 6-DOF 平台專用**
* `Df`：固定平台相鄰節點間的較大弦長 (毫米)。
* `df`：固定平台相鄰節點間的較小弦長 (毫米)。
* `Dm`：活動平台相鄰節點間的較大弦長 (毫米)。
* `dm`：活動平台相鄰節點間的較小弦長 (毫米)。
* `θ` (`phase_angle_deg`)：**[v2.3 新增]** 相位角 (Phase Angle)。由使用者**輸入**的設計參數 (單位: deg)。它定義了活動平台相對於固定平台**預先設定的 Yaw 旋轉偏移量**，並作為「零位姿態」的基準。(註：`v2.2` 中錯誤的「計算型交錯角 `Δθ`」已廢棄)。
* `Ra`：固定平台半徑 (毫米)，由 `Df/df` 自動計算。
* `Rb`：活動平台半徑 (毫米)，由 `Dm/dm` 自動計算。

**B. 3-DOF 平台專用**
* `D1`：固定平台等腰三角形的底邊長度 (A1 到 A5) (毫米)。
* `D2`：固定平台等腰三角形的高度 (從 A1-A5 中點到 A3) (毫米)。
* `d1`：活動平台等腰三角形的底邊長度 (B1 到 B5) (毫米)。
* `d2`：活動平台等腰三角形的高度 (B3 到 B1-B5 中點) (毫米)。
* **[v2.3 修正]**：3-DOF 平台採用「質心模型」，**不使用**也不計算 `Ra` 與 `Rb`。

#### **6.2 通用與電動缸參數**

* `L`：電動缸最小機械長度 (毫米)。
* `s`：電動缸可用行程 (毫米)。
* `s_buffer`：安全裕量 (毫米)，保留在兩端的緩衝距離。
* `s_mech`：機械行程 (毫米)，計算為 $s + s_{buffer}$。
* `h`：**平台初始高度 (Initial Height)** (毫米)。定義為當平台處於零位姿態 $(Roll=0, Pitch=0, Yaw=\theta)$ 時，可達到的**最低** $Z$ 軸高度。此高度由最短的致動器長度 $L$ 決定。
* `H`：**自然平衡零位高度 (Natural Balance Height)** (毫米)。定義為當平台處於零位姿態時，能使所有致動器長度平均值等於 $L_{mid}$ ($L + s_{buffer}/2 + s/2$) 時的 $Z$ 軸高度。**[v2.3 修正]** 此計算不再受物理行程或角度限制，確保幾何定義的純粹性。

#### **6.3 關節與角度參數**

* `base_joint_limit`：基座關節限制角度 (rad)，[UI顯示為 deg]。
* `platform_joint_limit`：活動平台關節限制角度 (rad)，[UI顯示為 deg]。
* `install_angle`：驅動系統安裝角度 (rad)，[UI顯示為 deg]。注意：此為驅動系統計算專用參數，勿與 6-DOF 相位角 `θ` 混淆。
* `zero_pose_offset`：零位偏移向量 (x, y)，自動計算。

#### **6.4 動力學參數**

* `m_p`：平台質量 (kg)。
* `m_l`：負載質量 (kg)。
* `com_l_x/y/z`：負載質心偏移 (mm)。
* `a_lin`：線性加速度 (mm/s²)，預設包含重力補償。
* `a_ang`：角加速度 (rad/s²)。

#### **6.5 驅動系統參數 (v2.0 標準)**

* `m`：移動件重量 (kg)。
* `a_max`：最高加速度 (m/s²)。
* `v_max`：最高速度 (mm/s)。
* `μ`：摩擦係數。
* `f_w`：負荷係數。
* `η_screw` / `η_pulley`：螺桿/傳動效率 (%)。
* `R_h`, `R_d`, `R_y`：每日/每年/總年數運轉時間。
* `Aval`：稼動率 (%)。
* `i`：減速比。

#### **6.6 姿態與座標系統**

* **平移 (Translation)**：
    * `Surge (X)` (mm)：前後移動。
    * `Sway (Y)` (mm)：左右移動。
    * `Heave (Z)` (mm)：垂直移動。**[v2.3 修正]** 3-DOF 的 Z 軸在核心層計算為絕對座標，由 UI 層負責減去 `H` 轉換為相對座標，解決雙重扣減問題。
* **旋轉 (Rotation)**：
    * `Pitch (θx)` (deg)：俯仰。
    * `Roll (θy)` (deg)：滾轉。
    * `Yaw (θz)` (deg)：偏航。

---

### **第七章：數學模型與演算法**

#### **7.1 平台標準方位與自由度定義**

[開發者註記：v2.3] 此標準方位定義是所有運動學計算的基礎。

  * **基準座標系**: 原點 `(0, 0, 0)` 固定於**下平台**的幾何中心。採用右手座標系 (`+Z` 向上)。
  * **自由度定義**:
      * **平移**: Surge (X), Sway (Y), Heave (Z)。
      * **旋轉**: Pitch (X軸), Roll (Y軸), Yaw (Z軸)。**操作定義**符合直覺（如 +Pitch 抬頭）。

#### **7.2 運動學模型**

  - **逆向運動學 (Inverse Kinematics)**: $L_i = || p_i - q_i ||$。
  - **正向運動學 (Forward Kinematics)**: 使用 `least_squares` 或 `minimize` 求解平台姿態。

  [變更註記 - v2.3 (幾何模型修正)]
  - **6-DOF 半徑計算**：使用 `scipy.optimize.minimize` 求解非線性方程 $\sum(\alpha + \beta) = 2\pi$。
  - **3-DOF 節點計算**：採用 **質心模型**，直接由 `D1, D2` (固定平台) 和 `d1, d2` (活動平台) 計算節點座標，**不使用**外接圓半徑 `Ra/Rb`。

#### **7.3 優化演算法 (v2.4 強化版)**

  1.  **幾何零位 (H) 求解 (`_calculate_geometry_...`)**
      * **目標**：尋找 $Z$ 使得 $\text{avg}(L_i) = L_{mid}$。
      * **[v2.4 修正]**：此計算過程**不再應用**物理約束（行程/角度）。即使零位高度導致腿長超出物理極限，程式仍會算出數學上的 $H$，並在後續分析階段才報告超出範圍，以避免求解器過早失敗。

  2.  **工作空間分析 (`_find_limit`)**
      * **演算法**：`SLSQP`。
      * **[v2.4 修正]**：為了防止求解器在極端幾何下發散，角度搜尋邊界被強制收緊為 **$\pm 85^\circ$** (約 1.48 rad)。這解決了先前版本中出現 `±1000°` 異常數值的問題。
      * **[v2.3 修正]**：位置變數使用 `Ra` 進行正規化縮放，角度變數使用 `1.0` 縮放，確保梯度穩定。

  3.  **自動展演軌跡生成 (`analysis_widget.py`)**
      * **[v2.4 新增]** 實作了基於時間 $t$ 的參數化軌跡：
          * **單軸順序 (Sequential)**：$P_i(t) = A \cdot \sin(2\pi \cdot t_{local})$。利用時間切片依序驅動各軸。
          * **畫圓/畫球 (Circular)**：$Pitch(t) = A \cdot \sin(\omega t)$, $Roll(t) = A \cdot \cos(\omega t)$。利用 $90^\circ$ 相位差產生繞圓運動 (Gyroscopic Motion)。

#### **7.4 動力學與驅動系統模型**
  * **運動負載**：採用七段式運動負載模型 ($F_{ua} \dots F_{dd}$)。
  * **壽命估算**：基於平均負載 $F_{eq}$ 與平均轉速 $n_{eq}$ 計算 $L_{rev}$ 與 $L_t$。

---

### **第八章：功能說明**

[開發者註記：v2.4 彙整] 本章節整合了 v1.22 的資料庫功能與 v2.4 的 UI 邏輯修正。

#### **8.1 主視窗 (`main_window.py`)**
- **導航**：使用功能表列切換「幾何設計」、「動力學分析」、「驅動系統計算」。
- **檔案 I/O**：支援專案 (`.json`) 與驅動設計 (`.dsd`) 存取。**[v2.4 修正]** 讀取舊版檔案時會自動轉換單位。
- **按鈕邏輯**：**[v2.4 修正]** 「計算零位」按鈕的啟用條件已根據平台類型分流：6-DOF 檢查 `Ra/Rb`，3-DOF 檢查 `D1/D2`，解決了 3-DOF 無法計算的問題。

#### **8.2 幾何設計 (`master_geometry_widget.py`)**
- **參數輸入**：
  - **6-DOF**：輸入 `Df, df, Dm, dm` 及 **[v2.3] 相位角 $\theta$**。
  - **3-DOF**：輸入 `D1, D2, d1, d2`。
- **計算零位**：求解 $H$ 與 $h$。此步驟不再受物理限制阻擋。
- **分析功能**：提供「可用工作空間」、「機械極限空間」及「節點擺角範圍」分析。

#### **8.3 姿態控制與分析 (`analysis_widget.py`)**
- **即時控制**：提供 6 個 (或 3 個) 自由度的滑塊控制。
- **結果顯示**：**[v2.4 修正]** 顯示正確的相對座標範圍。移除了 `main_window` 與 `analysis_widget` 間的重複單位轉換。
- **自動展演 (Auto-Demo)**：**[v2.4 新增]**
  - **自訂勾選**：僅驅動勾選的軸。
  - **單軸極限順序**：自動依序驅動所有軸。
  - **畫圓/畫球繞行**：驅動 Pitch/Roll 進行繞圓。
  - **週期時間 (Cycle Time)**：統一控制動作快慢。

#### **8.4 動力學與驅動系統 (`dynamics_widget.py` / `drive_system_widget.py`)**
- **動力學**：計算當前姿態出力或全域最大出力 ($F_{max}$)。
- **驅動系統**：支援自動模式 (讀取 $F_{max}$) 或手動模式 (輸入參數) 進行馬達/螺桿選型與壽命校核。

#### **8.5 資料庫管理 (`database_management_widget.py`)**
- 提供馬達、螺桿、皮帶輪的 CRUD 操作及 Excel 匯入功能。

#### **8.6 報告生成 (`report_generator.py`)**
- 生成包含所有參數與分析結果的 PDF 設計報告。

---

### **第九章：技術規格**

[開發者註記：v2.4 彙整]

- **核心引擎 (`kinematics.py`)**
  - **演算法**：SciPy `minimize` (SLSQP), `least_squares` (LM)。
  - **穩定性 (v2.4)**：
    - `_find_limit`：角度邊界收緊至 $\pm 85^\circ$。
    - `_calculate_geometry_6dof`：移除物理約束，保證幾何解的存在性。
    - `scale_vector`：位置變數以 `Ra` 縮放，角度變數以 `1.0` 縮放。
  - **模型定義 (v2.3)**：
    - 6-DOF：相位角由使用者輸入，移除 `stagger` 計算。
    - 3-DOF：質心模型，移除 `Ra/Rb` 依賴，核心回傳絕對座標。

- **分析引擎 (`analysis_engine.py`)**
  - **策略**：LHS (2000 樣本) + SQP (10 候選點)。
  - **架構**：獨立 `QThread`，透過 Signal/Slot 通訊。

- **GUI 框架**
  - **PyQt6**：主介面。
  - **PyVistaQT**：3D 渲染，內部單位統一轉換為公尺 (m)。

---

### **第十章：使用指南**

[開發者註記：v2.4 彙整] 本章節內容整合了 v1.12 的基礎操作、v1.22 的資料庫功能，以及 v2.4 的自動展演與 3-DOF 修正流程。

1.  **啟動程式**：執行 `main.py`。
2.  **檔案操作**：
    * **新建專案**：點擊「檔案」->「新建專案」。
    * **讀取專案**：點擊「檔案」->「讀取專案...」，選擇 `.json` 檔案。程式會自動識別並轉換舊版 (`m` 單位) 檔案。
    * **儲存專案**：點擊「檔案」->「儲存專案」。

3.  **幾何參數設計 (Geometry Design)**：
    * **平台選擇**：在面板頂部選擇「六自由度 (6-DOF)」或「三自由度 (3-DOF)」。
    * **參數輸入**：
        * **6-DOF**：輸入 `Df, df, Dm, dm` 以及 **[v2.3] 相位角 $\theta$**。
        * **3-DOF**：輸入 `D1, D2` (固定平台) 與 `d1, d2` (活動平台)。
    * **計算零位**：
        * 點擊「確認所有幾何參數」。(注意：3-DOF 模式下 `Ra/Rb` 顯示 N/A 為正常現象)。
        * 點擊「計算零位」。程式將求解 $H$ (自然平衡高度) 與 $h$ (初始高度)。**[v2.4]** 即使參數導致腿長超出行程，此步驟現在也能成功算出幾何上的 $H$，不會報錯崩潰。
    * **[選用] 關節限制**：勾選「啟用關節角度限制」並設定角度，再重新點擊「計算零位」。

4.  **工作空間分析 (Workspace Analysis)**：
    * 點擊「分析可用工作空間」或「分析機械極限空間」。
    * 等待進度條完成。
    * **結果檢視**：右側「平台空間範圍」將顯示各自由度的最大/最小值。**[v2.4]** 3-DOF 的 Heave (Z) 現在會正確顯示相對於 $H$ 的位移 (如 `±175`)，而不再是絕對座標。

5.  **姿態控制與自動展演 (Auto-Demo) [v2.4 新增]**：
    * **手動控制**：拖動滑塊即時改變平台姿態。
    * **自動展演模式** (位於「姿態展演與視覺化」區塊)：
        1.  **自訂勾選**：勾選滑塊旁的核取方塊，點擊「開始展演」，選定的軸將同步進行正弦波運動。
        2.  **單軸極限順序**：選擇此模式後，系統將**自動依序**驅動每一個自由度 (0 $\to$ Max $\to$ Min $\to$ 0)，無需手動勾選。
        3.  **畫圓/畫球繞行**：選擇此模式後，系統將控制 Pitch (Sin) 與 Roll (Cos) 進行相位差運動，使平台呈現繞圓傾斜的效果。
    * **速度控制**：調整 **「週期時間 (Cycle Time)」**。例如設定 `5.0s`，代表完成一個單軸來回或畫一圈的時間為 5 秒。

6.  **動力學分析**：
    * 輸入質量與加速度參數，點擊「計算當前姿態出力」。
    * 點擊「分析全域最大出力」以執行 LHS+SQP 搜尋。

7.  **驅動系統計算**：
    * 切換至「驅動系統計算」頁籤。
    * 使用「資料庫維護」匯入零件資料。
    * 選擇「自動模式」或「手動模式」進行馬達/螺桿選型與壽命校核。

8.  **生成報告**：
    * 點擊「檔案」->「生成設計報告...」以匯出 PDF。

---

### **第十一章：開發記錄與迭代修正**

[開發者註記：v2.4 彙整] 本章節保留了 v1.6 至 v1.12 的關鍵歷史修正記錄，v2.0 之後的變更請參閱文件開頭的「版本變更日誌」。

* **6.1 迭代修正一：物理模型與方位定義的標準化 (v1.6)**
  - **主題**：確保軟體的計算、顯示與物理現實完全一致。
  - **解決方案**：修正了 3-DOF 旋轉中心 (改為幾何中心)；強制 6-DOF 標準方位 (A1-A6 平行 X 軸)；修正前端 Roll 顯示方向以符合直覺。

* **6.2 迭代修正二：狀態管理與初始化流程的根本性修復 (v1.6)**
  - **主題**：解決「新建專案」後狀態殘留導致的計算錯誤。
  - **解決方案**：確立 `on_new_project` 為唯一重置入口；簡化子元件職責，由 `MainWindow` 統一管理狀態。

* **6.3 迭代修正三：v1.7 多執行緒穩定性重構**
  - **主題**：解決分析時介面卡死與崩潰。
  - **解決方案**：導入 `AnalysisEngine` 與 `QThread`；建立 `StateManager` 隔離數據；確保執行緒生命週期正確 (使用 `finished` 信號清理)。

* **6.4 迭代修正四：v1.8 全域分析功能升級**
  - **主題**：擴充全域分析能力。
  - **解決方案**：新增「靜態/動態」模式切換；完善執行緒 `quit()` 呼叫時機。

* **6.5 迭代修正五：v1.9 工作空間分析穩定性**
  - **主題**：解決啟用角度限制後求解器失敗的問題。
  - **解決方案**：引入 `_find_feasible_initial_pose` 預處理步驟，確保求解器從合法點開始搜尋。

* **6.6 迭代修正六：v1.10 物理模型修正**
  - **主題**：修正活動平台關節安裝方式導致的角度誤差。
  - **解決方案**：新增「上置式/下置式」選項，正確計算關節法線向量。

* **6.7 迭代修正七：v1.11 UI 佈局與依賴注入**
  - **主題**：解決 UI 寬度異常與狀態污染。
  - **解決方案**：將 `GeometryWidget` 重構為無狀態工廠模式；使用依賴注入傳遞 UI 元件。

* **6.8 迭代修正八：參數中央化 (v1.11)**
  - **主題**：提升可維護性。
  - **解決方案**：建立 `config.py`，集中管理所有參數與設定。

* **6.9 迭代修正九：v1.12 新增分析維度**
  - **主題**：支援硬體選型與報告。
  - **解決方案**：實作擺角範圍分析；整合 `reportlab` 生成 PDF 報告。

---

### **第十二章：推薦的第三方函式庫與資源**

[開發者註記：v2.4 彙整]

1.  **3D 視覺化: `PyVista` 與 `PyVistaQT`**
      * **用途**: 3D 渲染核心。提供 `QtInteractor` 嵌入 PyQt。
      * **資源**: PyVista 官方文件。

2.  **科學計算: `NumPy` 與 `SciPy`**
      * **用途**: `NumPy` 處理矩陣運算；`SciPy` (`optimize.minimize`) 處理非線性幾何求解與最佳化。

3.  **資料處理: `Pandas`**
      * **用途**: 處理 Excel 匯入與資料庫表格操作。

4.  **GUI 框架: `PyQt6`**
      * **用途**: 應用程式介面。

5.  **資料庫: `SQLite`**
      * **用途**: 內建零件資料庫。

6.  **文件生成: `ReportLab`**
      * **用途**: 生成 PDF 設計報告。

---

