# **電動缸設計輔助程式文檔 2.0 (最終統一版)**

## 目錄

1.  [專案概述與基礎需求](https://www.google.com/search?q=%2310-%E5%B0%88%E6%A1%88%E6%A6%82%E8%BF%B0%E8%88%87%E5%9F%BA%E7%A4%8E%E9%9C%80%E6%B1%82)
2.  [符號與變數定義](https://www.google.com/search?q=%2320-%E7%AC%A6%E8%99%9F%E8%88%87%E8%AE%8A%E6%95%B8%E5%AE%9A%E7%BE%A9)
3.  [設計資料輸入與校驗](https://www.google.com/search?q=%2330-%E8%A8%AD%E8%A8%88%E8%B3%87%E6%96%99%E8%BC%B8%E5%85%A5%E8%88%87%E6%A0%A1%E9%A9%97)
4.  [運作條件資訊](https://www.google.com/search?q=%2340-%E9%81%8B%E4%BD%9C%E6%A2%9D%E4%BB%B6%E8%B3%87%E8%A8%8A)
5.  [螺桿計算公式](https://www.google.com/search?q=%2350-%E8%9E%BA%E6%A1%BF%E8%A8%88%E7%AE%97%E5%85%AC%E5%BC%8F)

-----

## 1.0 專案概述與基礎需求

### **專案描述**

本專案旨在開發一個程式，用於輔助設計電動缸，並驗證選用的主要零件（如螺桿、伺服馬達、皮帶輪）規格是否符合設計要求。每種電動缸的設計需要將個別電動缸的設計相關資訊儲存在工作的電腦裡，以利後續取出來運用或修改。

### **程式基礎要求**

1.  **視窗環境：** 本程式需要在全視窗環境運行，所有資料的顯示和輸入都應在視窗環境中進行，並以常見的 Windows 程式形式規劃視窗介面。
2.  **模組化程式設計：** 將程式拆分為多個模組或函數，每個模組只負責一個特定的功能，使程式碼簡潔易維護。
3.  **類別組織：** 使用類別進行組織，將相關的功能放在類別中，使程式更具結構性和可擴展性。
4.  **文件和目錄結構管理：** 將程式碼分成不同的文件和目錄，有助於更好地管理和尋找程式碼。
5.  **模組設計與實作說明：** 為各模組提供設計與實作說明，包含通用工具函數，例如時間格式化、字符數檢查等。這些函數會在其他模組中被重複使用。
6.  **測試與驗證：** 撰寫單元測試以驗證各模組的功能。
7.  **配置檔案：** 引入配置檔案（例如 `.env` 或 `config.json`），避免將如 API 金鑰等敏感資訊硬編碼在程式中。這樣可以讓程式更靈活，且不必每次修改程式碼來更改配置。
8.  **日誌記錄：** 使用日誌記錄程式的運行狀態、錯誤訊息等，方便除錯和監控。
9.  **註解與文件：** 保持程式碼的簡潔和清晰，但對於複雜的邏輯，應當加入詳細的註解。同時，為每個模組撰寫簡單的文件說明（docstring），這樣可以幫助後續的開發者理解程式碼。

-----

## 2.0 符號與變數定義

#### 說明

**變數定義：**

  - ${m}$：移動件重量 (質量)，單位為 **${kg}$**。

  - $F_{i}$：需求電動缸軸向推力，單位為 **${N}$**。

  - $\theta$：為安裝角度(與水平面夾角)，通常以 **$^\circ$** 表示。

  - ${g}$：重力加速度(1 ${g}$ = 9.81 $\text{m/s}^2$)。

  - $\mu$：摩擦係數。

  - ${stroke}$：電動缸活塞或推桿可移動的最大距離，單位為 **${mm}$**。

  - $v_{max}$：電動缸(螺桿)在運行過程中能達到的最大線速度，單位為 **${mm/s}$**。

  - $a_{max} $：電動缸(螺桿)能達到的最大加速度，單位為 **${m/s}^2$** 。

  - $t_{acc}$：電動缸(螺桿)從靜止加速到最大速度 $v_{max}$ 所需的時間，單位為 **${s}$** 。

  - $L_{T}$：**需求壽命** (設計目標)，單位為 **${hr}$**。

  - $L_{t}$：**預估疲勞壽命** (計算結果)，單位為 **${hr}$**。

  - $R_{h}$：電動缸每日計劃運行的總時間，單位為 **${hr}$**，由程式提示使用者輸入。

  - $R_{d}$：電動缸每年運行的總天數，單位為 **${day}$**，由程式提示使用者輸入。

  - $R_{y}$：電動缸的預計使用總年限，單位為 **${year}$**，由程式提示使用者輸入。

  - ${Aval}$：運作稼動率，電動缸實際運作時間占可用時間的比例（範圍： **$0 < {Aval} < 1$**），由程式提示使用者輸入，無單位。
    $$
    Aval = \frac{\text{實際運作時間}}{\text{計劃運作時間}}
    $$

  - $TN_{screw}$：螺桿型號，無單位。

  - $D_{N}$：螺桿軸外徑，單位為 **${mm}$**。

  - $P_h$：螺桿導程規格，單位為 **${mm/rev}$**。

  - $D_{ball}$：螺桿珠徑規格(只有滾珠螺桿有此規格)，單位為 **${mm}$**。

  - $C_{a}$：螺桿動額定負荷規格 **${N}$ 或 ${kgf}$**。

  - $C_{oa}$：螺桿靜額定負荷規格，單位為 **${N}$ 或 ${kgf}$**。

  - $Bd_{screw}$：螺桿廠牌資訊，無單位。

  - $TN_{motor}$：馬達型號，無單位。

  - $TN_{controller}$：馬達控制器型號，無單位。

  - $P_{motor,rated}$：馬達額定功率規格，單位為 **${kW}$**。

  - $n_{motor,rated}$：馬達額定轉速規格，單位為 **${rpm}$**。

  - $n_{motor,max}$：馬達最大轉速規格，單位為 **${rpm}$**。

  - $T_{motor,rated}$：馬達額定扭力規格，單位為 **${Nm}$ 或 ${kgf-m}$**。

  - $T_{motor,peak}$：馬達峰值扭力規格，單位為 **${Nm}$ 或 ${kgf-m}$**。

  - $A_{motor,rated}$：馬達額定電流規格，單位為 **${A}$**。

  - $Bd_{motor}$：馬達廠牌資料，無單位。

  - $Bd_{controller}$：馬達控制器廠牌資訊，無單位。

  - $TN_{pully}$：皮帶輪型號，無單位。

  - $N_{pully, teeth}$：皮帶輪齒數規格，是整數值，無單位。

  - $D_{pully}$：皮帶輪直徑，單位為 **${mm}$**。

  - $W_{belt}$：皮帶輪適用皮帶寬度，單位為 **${mm}$**。

  - $Bd_{pully}$：皮帶輪廠牌資訊，無單位。

  - $N_{m}$：馬達端皮帶輪齒數 (主動輪)，為正的整數值。

  - $N_{s}$：螺桿端皮帶輪齒數 (從動輪)，為正的整數值。

  - $i$：減速比，無單位，可根據以下條件計算 (若 i \> 1 則為減速增扭)：
    $$
    i = \frac{\text{螺桿端齒輪齒數}}{\text{馬達端齒輪齒數}} = \frac{N_s}{N_m}
    $$

  - $F_{i}$：在某負載條件下的實際軸向負載，可為 $F_{ua}, F_{uc}, \dots, F_{s}$(見運行條件)。(i = ua, uc, ud, da, dc, dd, s)，預設單位為 **${N}$**。

  - $F_{ua}、F_{uc}、F_{ud}$：分別為加速上升、等速上升、減速上升段負載，預設單位為 **${N}$**。

  - $F_{da}、F_{dc}、F_{dd}$：分別為加速下降、等速下降、減速下降段負載，預設單位為 **${N}$**。

  - $F_{s}$：為靜止段負載，預設單位為 **${N}$**。

  - $F_{eq}$：平均(等效)軸向負載，預設單位為 **${N}$**。

  - $F_{comp\_max}$：最大壓縮負載 (用於挫曲計算)，預設單位為 **${N}$**。
    $$
    F\_{comp\_max} = \max(F\_{ua}, F\_{uc}, F\_{ud}, F\_s)
    $$

  - $F_{max}$：最大靜態負載 (用於靜態安全係數)，預設單位為 **${N}$**。
    $$
    F\_{max} = \max(|F\_{i}|) \quad (i = ua, uc, ud, da, dc, dd, s)
    $$

  - $t_{i}$：在某負載條件下的實際運行時間，可為 $t_{ua}, t_{uc}, \dots, t_{s}$(見運行條件)。(i = ua, uc, ud, da, dc, dd, s)，預設單位為 **${s}$**。

  - $t_{ua}、t_{uc}、t_{ud}$：分別為上升加速、上升等速、上升減速段的運行時間，預設單位為 **${s}$**。

  - $t_{da}、t_{dc}、t_{dd}$：分別為下降加速、下降等速、下降減速段的運行時間，預設單位為 **${s}$**。

  - $t_s$：為靜止段的運行時間，預設單位為 **${s}$**。

  - $T_{total}$：總運行時間，預設單位為 **${s}$**。
    $$
    T_{total} = t_{ua} + t_{uc} + t_{ud} + t_{da} + t_{dc} + t_{dd} + t_s
    $$

  - $t_{i,\%}$：各階段對應總運行時間的百分比。(i = ua, uc, ud, da, dc, dd, s)
    $$
    t_{i,\%} = \frac{t_i}{T_{total}} \times 100 \quad (i = ua, uc, ud, da, dc, dd, s)
    $$

  - $n_{i}$：在某負載條件下的實際螺桿轉速，可為 $n_{ua}, n_{uc}, \dots, n_{s}$(見運行條件)。(i = ua, uc, ud, da, dc, dd, s)，預設單位為 **${rpm}$**。

  - $n_{ua}、n_{uc}、n_{ud}$：分別為加速上升、等速上升、減速上升段螺桿轉速，預設單位為 **${rpm}$**。

  - $n_{da}、n_{dc}、n_{dd}$：分別為加速下降、等速下降、減速下降段螺桿轉速，預設單位為 **${rpm}$**。

  - $n_{s}$：為靜止段螺桿轉速，預設單位為 **${rpm}$**。

  - $n_{eq}$：平均(等效)螺桿轉速，預設單位為 **${rpm}$**。

  - $L_{rev}$：螺桿疲勞壽命，單位為 **${rev}$**。

  - $L_{s}$：螺桿總行程距離，單位為 **${km}$**。

  - $C_{ac}$：需求螺桿動額定負荷， 預設單位為 **${N}$**。

  - $C_{m}$：剛性修正係數 (用於挫曲計算)，無單位。

  - $C_{N}$：臨界載荷修正係數 (用於臨界轉速計算)。

  - $Sup_{s}$：螺桿 Rod 端的固定方式，為邏輯值。

  - $f_{w}$：負荷係數(無單位，詳見負荷係數 **$f_{w}$** 表)，負荷係數與運動條件(震動/衝擊)及速度 **$v_{max}$** 有關。

  - $T_{screw}$：螺桿側扭矩，預設單位為 **${Nm}$**。

  - $T_{motor\_req}$：需求馬達扭矩，預設單位為 **${Nm}$**。

  - $P_{req}$：所需馬達功率，預設單位為 **${kW}$**。

  - $\eta_{screw}$：螺桿傳動效率。

  - $\eta_{pulley}$：皮帶傳動效率。

  - $S_{0}$：靜態安全係數

  - $v$：螺桿運動的直線速度，表示螺桿沿其運行方向的運動速率，通常由設計目標或運動條件給定，與最大速度 $v_{max}$ 比較，校驗設計是否合理，單位為 **${mm/s}$ 或 ${m/s}$**。

  - $v_{ref}$：參考線速度，通常是設計中設定的目標速度，與最大速度 $v_{max}$ 比較，校驗設計是否合理，單位為 **${mm/s}$** 或 **${m/s}$**。

  - $n_{screw}$：螺桿的實際轉速，用於運動負載和等效負載的計算，單位為 **${rpm}$** 或 **${rev/s}$**。
    $$
    n_{screw} = \frac{v}{P_h}
    $$

  - $n_{screw,rpm}$：螺桿的轉速，與馬達轉速或等效轉速相關的計算中常用單位，單位為 **${rpm}$**。
    $$
    n_{screw,rpm} = n_{screw,rev/s} \times 60
    $$

  - $n_{screw,rev/s}$：螺桿的轉速，單位為 **${rev/s}$**。
    $$
    n_{screw,rev/s} = \frac{n_{screw,rpm}}{60}
    $$

-----

## 3.0 設計資料輸入與校驗

### **由資料庫取得的零件規格參數**

#### **螺桿資料庫**

**由資料管理工具，讀取螺桿資料庫，可得以下資料：**

  - **$TN_{screw}$**：螺桿型號，無單位。
  - **$D_{N}$**：螺桿外徑規格，預設單位為 ${mm}$。
  - **$P_h$**:：螺桿導程規格，預設單位為 **${mm/rev}$**。
  - **$D_{ball}$**：螺桿珠徑螺桿規格，預設單位為 **${mm}$**。(只有滾珠螺桿有此規格)
  - **$C_{a}$**：螺桿動額定負荷螺桿規格，預設單位為 **${N}$**。
  - **$C_{oa}$**：螺桿靜額定負荷螺桿規格，預設單位為 **${N}$**。
  - **$Bd_{screw}$**：螺桿廠牌資訊，無單位。

#### **馬達資料庫**

  - **$TN_{motor}$：馬達型號**
  - **$TN_{control}$：馬達控制器型號**
  - **$P_{motor,rated}$**：馬達額定功率規格，單位為 **${kW}$**。
  - **$n_{motor,rated}$**：馬達額定轉速規格，預設單位為 **${rpm}$**。
  - **$T_{motor,rated}$**：馬達額定扭力規格，預設單位為 **${Nm}$**。
  - **$T_{motor,peak}$**：馬達峰值扭力規格，預設單位為 **${Nm}$**。
  - **$A_{motor,rated}$**：馬達額定電流規格，預設單位為 **${A}$**。
  - **$Bd_{motor}$**：螺桿廠牌資料，無單位。
  - **$Bd_{controller}$**：馬達控制器廠牌資訊，無單位。

#### **皮帶輪資料庫**

  - **$TN_{pully}$**：皮帶輪型號，無單位。

  - **$N_{pully, teeth}$**：皮帶輪齒數規格，是整數值，無單位。

  - **$D_{pully}$**：皮帶輪直徑，預設單位為 **${mm}$**。

  - **$W_{belt}$**：皮帶輪適用皮帶寬度，預設單位為 **${mm}$**。

  - **$Bd_{pully}$**：皮帶輪廠牌資訊，無單位。

  - **${i}$**：減速比，使用者可以直接提供此值，也可根據以下條件計算：

    1.  **$N_{m}$**：馬達端皮帶輪齒數，由使用者從皮帶輪資料庫中選取的皮帶輪規格取得齒數。
    2.  **$N_{s}$**：螺桿端皮帶輪齒數，由使用者從皮帶輪資料庫中選取的皮帶輪規格取得齒數。
        $$
        i = \frac{\text{螺桿端皮帶輪齒數}}{\text{馬達端皮帶輪齒數}} = \frac{N_{s}}{N_{m}}
        $$

### **需要使用者輸入的參數**

#### 以下資訊需由使用者輸入，以做為設計電動缸的設計依據：

  - **$m$**：移動件重量 (質量)

  - **$\theta$**：電動缸安裝角度 (預設$\theta$=90$^\circ$)

  - **$stroke$**：電動缸活塞桿可移動的最大距離

  - **$v_{max}$**：電動缸的直線運動最高速度

  - **$a_{max}$**：電動缸的直線運動最大加速度

  - **$t_{acc}$**：電動缸的加速時間

  - **$t_{ua}, t_{uc}, t_{ud}, t_{da}, t_{dc}, t_{dd}, t_s$**：各運動階段的運行時間，預設單位為 **${s}$**。

  - **$L_{T}$**：**需求壽命**
    **使用者可以直接提供此值，也可根據以下條件計算：**

    1.  **$R_{h}$**：電動缸每日計劃運行的總時間。
    2.  **$R_{d}$**：電動缸每年運行的總天數。
    3.  **$R_{y}$**：電動缸的預計使用總年限。
    4.  **$Aval$**：電動缸運作稼動率，實際運作時間占可用時間的比例。

  - **$Sup_{s}$**：螺桿Rod端的固定方式。

      - **$Sup_{s}$**：為邏輯值：

        1.  **$Sup_{s}$ = false**：表示固定方式為 **固定-自由**。
        2.  **$Sup_{s}$ = true**：表示固定方式為 **固定-支撐**。
            剛性修正係數 $C_{m}$ 與臨界載荷修正係數 $C_{N}$ 的取值如下 $Sup_{s}$ 表，無單位：

        | 支撐條件 | $C_{m}$ | $C_{N}$ |
        |-----------|------|------|
        | 固定-支撐  | 10.20 | 2.00 |
        | 固定-自由  | 1.30  | 0.25 |

      - $f_{w}$：負荷係數與運動條件(震動/衝擊)及速度 $v_{max}$ 有關，使用者需輸入 $f_{w}$ 的值，並滿足以下限制：

        | **震動與衝擊** | **速度範圍 $v_{max}$ (m/min)** | **$f_{w}$ 範圍** |
        |----------------|---------------------------------|------------------|
        | 輕             | $v_{max}$ \< 15      | ( 1.0 \\sim 1.2 ) |
        | 中             | 15 $\leq v_{max}$ \< 60 | ( 1.2 \\sim 1.5 ) |
        | 重             | $v_{max} \geq$ 60   | ( 1.5 \\sim 3.0 ) |

        #### **條件定義**

        1.  可由 **$v_{max}$** 確定震動與衝擊類型(輕/中/重)。
        2.  使用者輸入的 $f_{w}$ 值必須符合對應類型的範圍：
              - 若 $v_{max}$ \< 15，則 1.0 $\leq f_{w} \leq$ 1.2。
              - 若 15 $\leq v_{max}$ \< 60，則 1.2 $\leq f_{w} \leq$ 1.5。
              - 若 $v_{max} \geq$ 60，則 1.5 $\leq f_{w} \leq$ 3.0。

#### **使用者輸入單位檢查**

為了避免計算過程中的單位不一致，程式應包含以下檢查機制：

#### **通用性輸入要求**

  - **提示詞與單位選擇：**
      - 在每次要求輸入時，程式會顯示清晰的提示詞，幫助使用者理解輸入需求。
      - 提供多種常用單位供使用者選擇。例如：
          - **力的單位：${kgf}, {N}$**
          - **長度單位：${mm}, {cm}, {m}$**
          - **時間單位：${s}, {min}, {hr}, {day}, {year}$**
          - **速度單位：${m/s}, {mm/s}, {m/min}, {mm/min}$**
          - **加速度單位：${g}, {m/s}^2, {mm/s}^2$**
          - **扭力單位：${Nm}$, ${kgf} \cdot {m}$**

#### **數據核對與轉換邏輯**

1.  **長度單位檢查：**

      - 確保輸入的 **${stroke}、{P_h}、{L}$**，默認單位為 **${mm}$**。
      - 在程式中，由清單方式提供單位選擇 **${mm} 或 {m}$**。
      - 若選擇為 **${m}$**，程式將自動轉換為 **${mm}$** 來進行計算。
        $
        Len(mm) = Len(m) \\times 10^{3}
        $

2.  **推力單位檢查：**

      - 確保輸入的 **$F_{i}$** 默認單位為 **${N}$**。
      - 在程式中，由清單方式提供單位選擇 **${kgf}$** 或 **${N}$**。
      - 若選擇為 **${kgf}$**，程式將自動轉換為 **${N}$**。
        $
        F(N) = F(kgf) \\times 9.81
        $

3.  **扭力單位檢查：**

      - 確保輸入的 **$T$** 默認單位為 **${Nm}$**。
      - 在程式中，由清單方式提供單位選擇 **${kgf} \cdot {m}$** 或 **${Nm}$**。
      - **扭力**的單位轉換提示
      - 若選擇為 **${kgf} \cdot {m}$**，程式將自動轉換為 **${Nm}$**。

#### **單位轉換說明**

**程式需自動檢查輸入的單位，若用戶選擇的單位不一致(非默認單位)，程式會自動進行單位轉換。**
例如：

  - **力： $1 \, kgf = 9.81 \, N$**
  - **長度： $1 \, m = 100 \, cm$**
  - **長度： $1 \, cm = 10 \, mm$**
  - **時間： $1 \, year = 365 \, day$**
  - **時間： $1 \, day = 24 \, hr$**
  - **時間： $1 \, hr = 60 \, min$**
  - **時間： $1 \, min = 60 \, s$**
  - **扭力：$1 \, {kgf} \cdot {m} = 9.81 \, Nm$**

### **錯誤處理**

  - **程式會自動檢查數據格式的正確，程式會顯示具體提示，確保值在合理範圍內。**
  - 如果輸入數據格式錯誤，程式會顯示具體提示，要求重新輸入：
    ```
    輸入錯誤：每日運作時數必須為正數，請重新輸入。
    ```

-----

## 4.0 運作條件資訊

#### **(1) 運動負載公式**

$F_{i} \quad (i = ua, uc, ud, da, dc, dd, s)$

1.  **上升加速段($F_{ua}$)**
    $F_{ua} = m \cdot g \cdot (\sin\theta + \mu \cdot \cos\theta) + m \cdot a_{max}$

2.  **上升等速段($F_{uc}$)**
    $F_{uc} = m \cdot g \cdot (\sin\theta + \mu \cdot \cos\theta)$

3.  **上升減速段($F_{ud}$)**
    $F_{ud} = m \cdot g \cdot (\sin\theta + \mu \cdot \cos\theta) - m \cdot a_{max}$

4.  **下降加速段($F_{da}$)**
    $F_{da} = \mu \cdot m \cdot g \cdot \cos\theta - m \cdot g \cdot \sin\theta - m \cdot a_{max}$

5.  **下降等速段($F_{dc}$)**
    $F_{dc} = \mu \cdot m \cdot g \cdot \cos\theta - m \cdot g \cdot \sin\theta$

6.  **下降減速段($F_{dd}$)**
    $F_{dd} = m \cdot a_{max} + \mu \cdot m \cdot g \cdot \cos\theta - m \cdot g \cdot \sin\theta$

7.  **靜止段($F_{s}$)**
    $F_s = \max(0, m \cdot g \cdot \sin\theta - \mu \cdot m \cdot g \cdot \cos\theta)$

#### **(2) 平均(等效)軸向負載公式**

$$
F_{eq} = \sqrt[3]{|F_{ua}|^3 \cdot \frac{t_{ua}}{T_{total}} + |F_{uc}|^3 \cdot \frac{t_{uc}}{T_{total}} + |F_{ud}|^3 \cdot \frac{t_{ud}}{T_{total}} + |F_{da}|^3 \cdot \frac{t_{da}}{T_{total}} + |F_{dc}|^3 \cdot \frac{t_{dc}}{T_{total}} + |F_{dd}|^3 \cdot \frac{t_{dd}}{T_{total}} + |F_s|^3 \cdot \frac{t_{s}}{T_{total}}}
$$

#### **螺桿轉速代號與計算公式**

螺桿的轉速分以下情況進行計算：

1.  **等速上升或下降條件(適用於 $t_{uc}, t_{dc}$)**

      - 螺桿轉速 $n_{screw,rpm}$ 按最大速度 $v_{max}$ 計算。
      - 馬達轉速 $n_{motor,rpm} = n_{screw,rpm} \times i$。
      - 若 $n_{motor,rpm} > n_{motor,rated}$，需警示或重新調整設計。

2.  **加速或減速條件(適用於 $t_{ua}, t_{ud}, t_{da}, t_{dd}$)**

      - 螺桿平均轉速可近似為等速段轉速的一半。

3.  **靜止條件(適用於 $t_{s}$)**

      - 螺桿轉速為 $0$。

#### **(3) 平均(等效)轉速公式**

$$
n_{eq} = \frac{n_{ua} \cdot t_{ua} + n_{uc} \cdot t_{uc} + n_{ud} \cdot t_{ud} + n_{da} \cdot t_{da} + n_{dc} \cdot t_{dc} + n_{dd} \cdot t_{dd} + n_{s} \cdot t_{s}}{T_{total}}
$$

-----

## 5.0 螺桿計算公式

#### **(4) 壽命公式**

1.  **螺桿壽命公式（總旋轉圈數）**

  - 螺桿壽命的總迴轉次數($L_{rev}$)計算公式為：
    $$
    L_{rev} = \left( \frac{C_{a}}{F_{eq} \cdot f_{w}} \right)^3 \times 10^6
    $$

2.  **預估疲勞壽命($L_t$)公式（總運行時間）**

  - 將總迴轉次數($L_{rev}$)轉換為總運轉時間($L_{t}$)：
    $$
    L_{t} = \frac{L_{rev}}{60 \cdot n_{eq}}
    $$

3.  **螺桿壽命公式（總行程）**

  - 將總迴轉次數($L_{rev}$)轉換為總行程距離($L_{s}$)：
    $$
    L_{s} = \frac{L_{rev} \cdot P_h}{10^6}
    $$

4.  **需求動額定負荷($C_{ac}$)定義**
    $$
    C_{ac} = F_{eq} \cdot f_w \cdot \left( \frac{60 \cdot n_{eq} \cdot L_T}{10^6} \right)^{\frac{1}{3}}
    $$

#### **(5) 靜態安全係數公式**

靜態安全檢查：
$$
S_{0} = \frac{C_{oa}}{|F_{max}|}
$$

  - $S_{0}$：靜態安全係數，通常要求 $S_{0} \geq 1.5$。

#### **(6) 臨界轉速公式**

$$
n_{c} = C_{N} \cdot \frac{d_r \cdot 10^7}{L_k^2}
$$

  - $n_c$：臨界轉速(rpm)。
  - $C_N$：支撐條件係數 (例: 固定-支撐為2.00)。
  - $d_r$：螺桿牙底直徑(mm)。
  - $L_k$：螺桿有效支撐長度(mm)。

#### **(7) 臨界載荷 (挫曲) 公式**

$$
d_{r, req} > \sqrt[4]{\frac{F_{comp\_max} \cdot L_k^2}{C_m \cdot 10^3}}
$$

  - $d_{r, req}$：螺桿所需最小牙底直徑 (mm)。
  - $F_{comp\_max}$：最大壓縮軸向負荷 (N)。
  - $L_{k}$：安裝間距(螺桿兩端支撐之相對距離，單位：mm)。
  - $C_{m}$：剛性修正係數 (例: 固定-支撐為10.20)。

#### **(8) 馬達功率公式**

$$
P_{req} = \frac{T_{motor\_rated} \cdot n_{motor\_rated}}{9550}
$$

  - $P_{req}$：所需馬達功率，預設單位為 **${kW}$**。

### **(9) 推力、扭力與效率公式**

*註：以下公式中導程 $P_h$ 單位為 mm，扭矩單位為 Nm，為保持單位一致，已加入 `1000` 的轉換係數。*

1.  **軸向力轉換為螺桿扭矩**
    $$
    T_{screw} = \frac{F_{i} \cdot P_h}{2 \pi \cdot \eta_{screw} \cdot 1000}
    $$

2.  **由螺桿扭矩反推需求馬達扭矩**
    $$
    T_{motor\_req} = \frac{T_{screw}}{i \cdot \eta_{pulley}}
    $$

3.  **由軸向力反推總需求馬達扭矩 (組合公式)**
    $$
    T_{motor\_req} = \frac{F_{i} \cdot P_h}{2 \pi \cdot \eta_{screw} \cdot i \cdot \eta_{pulley} \cdot 1000}
    $$

4.  **由馬達額定扭力推算最大輸出軸向力**
    $$
    F_{max\_output} = \frac{T_{motor\_rated} \cdot 2 \pi \cdot \eta_{screw} \cdot i \cdot \eta_{pulley} \cdot 1000}{P_h}
    $$