# 史都華平台全域最大靜載推力計算演算法

**文件版本：** 1.0  
**日期：** 2025年8月24日  
**作者：** Grok (xAI)  
**目的：** 本文件提供一個穩定、確定性的演算法，用於計算史都華平台（Stewart Platform，6自由度與3自由度）在最大靜載下的全域最大推力。該演算法設計為嵌入模組化史都華平台設計與模擬工具的v1.7開發分支，增強現有的「邊界掃描」方法，結合SQP優化以實現全面覆蓋和高效計算，確保一致性、可重複性，並解決浮點誤差等問題。

此文件可導入你的開發環境（例如作為 `docs/` 或 `src/core/` 中的Markdown文件），供AI協助整合。請在 `analysis_engine.py` 或回歸測試中引用。

---

## 1. 介紹與理由

### 概述
本演算法計算在最大靜載下，於整個工作空間內，致動器所需的最大推力（力）。它聚焦於靜態條件（無運動，加速度=0），將扳手簡化為重力和負載偏移扭矩。

- **關鍵輸入：** 幾何參數（Ra、Rb等）、質量（platform_mass、load_mass）、負載質心偏移、空間範圍。
- **輸出：** 全域最大推力（牛頓，N）、發生姿態（字典），UI顯示N與kgf單位。
- **相容性：** 符合v1.6/v1.7指南（單位m，g=9.81，可用/機械極限模式）。使用`CoreEngine`單一計算路徑（`is_pose_valid`、`calculate_force_for_ui_pose`）。
- **對v1.7的改進：**
  - 將純邊界掃描替換為混合方法：確定性採樣（拉丁超立方，均勻覆蓋）+ SQP精煉（捕捉內部極值）。
  - 確保確定性（固定種子，無隨機）。
  - 處理一致性問題（例如浮點容差、單一計算路徑）。
  - 高效處理高維自由度；可調參數（如N）具擴展性。

### 假設
- 靜載：W_ext = [F; M]，F = [0, 0, -(總質量)*g]，M = cross(load_com, F)。
- 工作空間：合法姿態的腿長範圍（可用：[L + s_buffer/2, L + s_buffer/2 + s]；機械：[L, L + s_mech]）。
- 避免奇異點：cond(J) < 1e6。
- 自由度適配：3-DOF僅採樣相關維度（升降、俯仰、橫滾）；其他維度補0。

### 潛在問題與緩解措施
- **不一致性：** 強制單一計算路徑；使用np.isclose比較（rtol=1e-5）。
- **計算時間：** 限制樣本數（例如10k）；在執行緒中運行，發送進度信號。
- **極值位置：** 文獻（例如IEEE史都華優化論文）顯示最大力常在邊界，但奇異點附近可能有內部極值，混合方法覆蓋兩者。

---

## 2. 演算法步驟

1. **初始化**
   - 從`StateManager`載入：範圍（workspace_limits，Nx2陣列，N=自由度）、模式（'usable'或'mechanical'）、質量、load_com、g=9.81。
   - 設定參數：N=15（每自由度分辨率）、total_samples=10000、K=20（SQP初始猜測數）、seed=42（確定性）。

2. **生成測試點（確定性採樣）**
   - 使用拉丁超立方採樣（LHS，scipy.stats.qmc.LatinHypercube）實現均勻、確定性覆蓋。
   - 縮放樣本至範圍：x = min + samples * (max - min)。
   - 3-DOF：僅採樣活躍自由度；固定其他（例如tx=ty=gamma=0）。
   - 過濾與計算：對每個x，若`is_pose_valid(x)`成立，計算thrust = max(abs(calculate_force_for_ui_pose(x)))。
   - 追蹤最高推力與姿態。

3. **SQP優化精煉**
   - 從採樣中取top K高推力點作為初始猜測。
   - 目標：最小化-thrust(x)（負值轉最大化）。
   - 約束：is_pose_valid(x) >= 0（不等式）；範圍框。
   - 使用scipy.optimize.minimize('SLSQP')多跑初始猜測，選全球最大。
   - 處理無效：若無效返回inf。

4. **後處理與輸出**
   - 將max_thrust轉為kgf（thrust / g）供UI顯示。
   - 格式化worst_pose為字典（例如{'tx': val, ...}）用於多行列表。
   - 發送信號：progress_updated（每100點）、analysis_completed(max_thrust, worst_pose)。

5. **錯誤處理**
   - 無合法姿態：發送0和空字典。
   - 浮點處理：驗證中使用容差。
   - 日誌：可選列印/除錯worst_pose計算。

---

## 3. Python程式碼框架

嵌入`src/core/analysis_engine.py`。假設已有`CoreEngine`方法。使用PyQt6發送信號，NumPy/SciPy（requirements.txt已有）。

```python
import numpy as np
from scipy.optimize import minimize
from scipy.stats.qmc import LatinHypercube
from PyQt6.QtCore import QObject, pyqtSignal

class AnalysisEngine(QObject):
    progress_updated = pyqtSignal(int)  # 進度百分比
    analysis_completed = pyqtSignal(float, dict)  # 最大推力 (N), 最差姿態字典

    def __init__(self, core_engine, state_manager):
        super().__init__()
        self.core = core_engine  # CoreEngine實例
        self.state = state_manager  # StateManager實例

    def run_global_analysis(self, mode='usable'):
        """
        在執行緒中運行全域最大靜載推力計算。
        :param mode: 'usable' 或 'mechanical'
        """
        # 步驟1：初始化
        bounds = np.array(self.state.get_workspace_limits(mode))  # [[min, max]] 每自由度
        dof = self.core.dof
        total_samples = 10000
        K = 20
        seed = 42

        # 步驟2：確定性採樣（LHS）
        sampler = LatinHypercube(d=dof, seed=seed)
        samples = sampler.random(n=total_samples)
        scaled_samples = bounds[:, 0] + samples * (bounds[:, 1] - bounds[:, 0])

        if self.core.dof == 3:  # 適配3-DOF（例如[tz, alpha, beta]）
            full_samples = np.zeros((total_samples, 6))
            full_samples[:, [2, 3, 4]] = scaled_samples  # tx=ty=gamma=0
            scaled_samples = full_samples

        thrusts = []
        for i, x in enumerate(scaled_samples):
            if self.core.is_pose_valid(x):
                force = self.core.calculate_force_for_ui_pose(x)
                thrust = np.max(np.abs(force))
                thrusts.append((x, thrust))
            if i % 100 == 0:
                self.progress_updated.emit(int(100 * i / total_samples))

        if not thrusts:
            self.analysis_completed.emit(0.0, {})
            return

        # 排序取top K初始猜測
        thrusts.sort(key=lambda t: t[1], reverse=True)
        init_guesses = [t[0] for t in thrusts[:K]]

        # 步驟3：SQP精煉
        def objective(x):
            if not self.core.is_pose_valid(x):
                return np.inf
            force = self.core.calculate_force_for_ui_pose(x)
            return -np.max(np.abs(force))

        # 約束（直接在minimize中使用範圍）
        cons = [{'type': 'ineq', 'fun': lambda x: 1 if self.core.is_pose_valid(x) else -1}]  # 簡化

        global_max = 0.0
        worst_pose = None
        for x0 in init_guesses:
            res = minimize(objective, x0, bounds=bounds, method='SLSQP', constraints=cons, tol=1e-6)
            if res.success and -res.fun > global_max:
                global_max = -res.fun
                worst_pose = {
                    'tx': res.x[0], 'ty': res.x[1], 'tz': res.x[2],
                    'alpha': res.x[3], 'beta': res.x[4], 'gamma': res.x[5]
                }

        self.analysis_completed.emit(global_max, worst_pose)
```

### 在MainWindow中的使用
- 在`on_global_force_analysis_requested`中：創建QThread，將AnalysisEngine移至執行緒，連接信號到UI更新（例如更新「全域最大靜載分析」群組）。

---

## 4. 導入v1.7分支的指南

### AI協助整合步驟
1. **複製程式碼：** 將框架貼入`analysis_engine.py`。AI可自動補全導入或適配現有方法。
2. **StateManager連結：** 確保`get_workspace_limits(mode)`返回範圍陣列。若缺失，需添加。
3. **CoreEngine相容性：** 驗證`is_pose_valid(x)`和`calculate_force_for_ui_pose(x)`接受陣列輸入（x為np.array）。
4. **UI更新：** 在`dynamics_widget.py`中，連接`analysis_completed`顯示max_thrust（N與kgf=thrust/9.81），worst_pose用多行列表。
5. **測試：**
   - **單元測試：** 在`tests/`中，測試零姿態：推力 ≈ (總質量 * g) / 6（對稱）。
   - **回歸測試：** 在v1.7運行，交叉驗證即時與全域（例如在worst_pose）。
   - **效能：** 計時10k樣本；若慢，調整total_samples。
6. **合併至基線：** 驗證後，按你的流程：回歸測試v1.6所有功能，然後合併至main。

### 潛在增強
- **動態擴展：** 未來加入加速度項到W_ext（例如M \ddot{q}，指南）。
- **視覺化：** 發送信號更新PyVista顯示worst_pose。
- **參數調優：** 在config.py公開N/K供用戶調整。
