# **應用程式開發指南：模組化史都華平台設計與模擬工具**

**版本：2.2 (診斷失敗版)**
**日期：2025年11月15日**

**目標：** 為遊樂設備行業的技術人員，打造一款基於 Python 的視窗版桌面應用程式，用於史都華平台 (Stewart Platform) 的參數化設計、性能分析、動態模擬與驗證，並整合獨立的電動缸驅動系統設計功能。

**[開發者註記：版本變更日誌 (累積所有版本)]**

  - **[新增] [從 v2.1 單位系統審計修正版 -\> v2.2 診斷失敗版]**

      - **[核心幾何模型重構]** 根據 `v4.3` 和 `v4.6` 幾何定義文件，對 `kinematics.py` 進行了根本性的重構。

          - **關鍵挑戰**：`v2.1` 版本的 `kinematics.py` 幾何模型與 `v4.3` 拓樸文件不符，導致「啟用關節角度限制」時求解器必定失敗。
          - **解決方案**：
            1.  **`_calculate_radius_from_chords` (6-DOF)**：從舊的 `alpha + beta = 120 度` 邏輯，重寫為使用 `scipy.optimize.minimize` 求解 `arcsin(D/(2R)) + arcsin(d/(2R)) = pi/3`，以匹配 `v4.3` 的 3 長 3 短交錯拓樸。
            2.  **`_get_6dof_nodes`**：重寫為使用「角度累加法」 (`[0, beta, beta+alpha, ...]`)，並正確實作 `v4.3` 的 (短-長) / (長-短) 交錯拓樸與相位角 `stagger`。
            3.  **`_get_3dof_nodes`**：根據 `v4.6` 文件，將節點座標系原點從「外接圓圓心」修正為「**質心**」（例如 Y 座標為 `-D2/3` 和 `2*D2/3`）。
          - **[註記：部分解決]** 此項重構成功解決了 `Ra`, `Rb` 和 `H`（零位高度）的計算問題。

      - **[核心分析功能除錯 (失敗)]** 嘗試解決工作空間分析的錯誤數值問題。

          - **關鍵挑戰**：在 `v2.2` 開發週期中，工作空間分析暴露出兩個頑固錯誤：1) 啟用關節限制時求解器失敗； 2) 未啟用限制時，`Surge` 範圍單邊，且 `Pitch/Roll/Yaw` 範圍顯示 `±1000` 度以上的不合理數值。
          - **問題診斷與嘗試 (歷程)**：
            1.  **`AttributeError` 修正**：修正了 `analysis_engine.py` 中呼叫 `analyze_workspace` 的 `AttributeError` 錯誤，使其能正確呼叫 `analyze_operational_workspace`。
            2.  **雙重單位轉換修正**：修正了 `kinematics.py` 中多餘的 `np.rad2deg` 轉換，解決了 `v2.1` 遺留的「雙重轉換」問題。
            3.  **Heave (Z) 顯示修正**：修正了 `analysis_widget.py`，使其能正確顯示 Heave 相對於 `H` 的 `+/-` 相對座標。
            4.  **雙重相位錯誤修正**：一度懷疑「相位角」被套用兩次。嘗試將 `_analyze_workspace_6dof` 的 `neutral_pose` Yaw 值改回 `0`。此修改正確，但未能解決根本問題。
            5.  **最終診斷 (變數正規化失敗)**：經 `image_f7251a.png` 和 `image_f72571.png` 測試確認，問題的最終根源在於 `kinematics.py` 的 `_find_limit` 方法中，其「變數正規化」(Scaling) 邏輯存在缺陷（未正確區分 `mm` 和 `rad` 的尺度），導致求解器在數值上極度不穩定。
          - **[註記：未解決]** `_find_limit` 方法的數值穩定性問題**未被解決**。`kinematics.py` 目前停留在此錯誤狀態，導致工作空間分析和關節限制功能**完全不可用**。

  - **[從 v2.0 最終統一版 -\> v2.1 單位系統審計修正版]**

      - **[全域單位系統審計與穩定性修正]** 完成了對整個專案的 `mm` 單位系統進行的全面審計、除錯與穩定性強化。
          - **關鍵挑戰**：在 `v2.0` 完成單位正常化重構後，專案出現了一系列頑固且相互關聯的錯誤，包括：(1) UI 介面單位顯示混亂；(2) 3D 預覽模型消失或嚴重變形；(3) 工作空間分析的求解器在計算平移自由度時，結果發散成物理上不可能的巨大數值。
          - **問題診斷**：
            1.  初期的修正嘗試集中在 `kinematics.py` 的求解器穩定性上，但因未能準確診斷根本原因而多次失敗。
            2.  最終透過「功能隔離法」與「診斷輸出法」，收集了程式執行時的詳細日誌。
            3.  日誌數據確鑿地證明，工作空間分析失敗的**根本原因**在於 `_find_limit` 方法中，對求解器的「目標函式」應用了錯誤的尺度還原，導致求解器在一個尺度不匹配的空間中運算而崩潰。
            4.  同時，也發現了 `main_window.py` 中殘留的錯誤單位轉換 (`* 1000`)，這是導致 3D 模型一度變形的獨立原因。
          - **最終解決方案**：
            1.  **`kinematics.py`**：
                  - **智慧讀取**：`load_parameters` 方法被重構，增加了 `unit_system` 標記檢測，可自動轉換舊版 `m` 單位 `.json` 檔案。
                  - **求解器修正**：`_find_limit` 方法被徹底重寫，正確實作了「變數正規化」邏輯，確保目標函式在縮放後的空間運作，而物理约束在真實的 `mm` 空間運作，從根本上解決了工作空間計算發散的問題。
            2.  **`pyvista_widget.py`**：在所有繪圖方法的入口處加入了 `MM_TO_M` 尺度轉換，確保 `mm` 單位座標能被正確渲染。
            3.  **`main_window.py`**：在 `on_load_project` 方法中加入了接收 `status` 的邏輯，用以在舊檔案轉換後彈出提示視窗。
            4.  **周邊修正**：修正了 `drive_system_widget.py` 中因 `database_manager` 回傳舊鍵名而導致的 `KeyError`；修正了 `config.py` 與 `drive_system_engine.py` 之間的變數依賴性問題。
      - **[註記：已解決]** 所有與 `mm` 單位系統轉換相關的已知問題均已解決，專案達到一個新的穩定基線 `v2.1-alpha.1`。

  - **[從 v1.23 重整版 -\> v2.0 最終統一版]**

      - **[文件最終校正與註記]** 完成了《開發指南》與《計算指南 2.0》的最終內容對齊與註記工作。
          - **目標**：確保本開發指南中的所有變數命名、數學模型、零件篩選條件均與最新的《電動缸設計輔助程式文檔 (最終版)》 保持絕對一致。
          - **執行方案**：
            1.  **全局變數統一**：在第六章「變數與參數定義」中，將所有舊變數（如 `moving_part_mass`, `ratio`, `lead_mm`）系統性地更新為最終版統一符號（`m`, `i`, `P_h`），並為每一處變更添加了詳細的 `[變更註記]`。
            2.  **核心模型替換**：在第七章「數學模型與演算法」中，將舊的、簡化的動力學與扭矩公式，完整替換為經過驗證的、包含單位轉換與多重效率考量的精確模型，並附上變更註記。
            3.  **歷史日誌校對**：在 v1.22 的開發日誌中，針對零件篩選條件的描述，補充了變更註記，指明其舊有變數應對應至第六章的哪個新變數，以確保歷史記錄的可讀性與向前兼容性。
      - **[註記：已完成]** 本文件 v2.0 版本的所有電動缸相關內容，現已與設計標準完全同步。

  - **[從 v1.22 穩定版 -\> v1.23 彙整版]**

      - **[資料整合與文件彙整] 完成 v1.23 版本的內容重新彙整**
          - **關鍵挑戰**：先前 v1.22 彙整過程中發現內容省略與重複問題，需確保 v1.19 至 v1.22 所有版本內容無縫整合，並保留歷史記錄。初期分析顯示 v1.22 日誌未完整反映 v1.21 細節，部分章節（如變數定義、數學模型）被簡化。
          - **問題診斷**：檢查 v1.19、v1.20、v1.21、v1.22 的指南與交接清單，確認差異包括資料庫優化、UI 優化及開發記錄不一致。發現 v1.22 因篇幅考量省略了部分 v1.21 內容，並在日誌中出現重複描述。
          - **最終解決方案**：手動合併 v1.19 至 v1.22 所有內容，保留舊版完整記錄，新增 v1.23 彙整調整，標示 `[開發者註記：...]` 說明變更理由，確保無刪減並消除重複。補充專案初始化與環境設定、核心理念與技術棧的詳細內容。
      - **[註記：已解決]** 所有內容省略與重複問題已解決，指南內容與歷史記錄一致。

  - **[從 v1.21 穩定版 -\> v1.22 穩定版]**

      - **[資料庫優化] 支援外部 Excel 匯入與設計條件零件篩選**
          - **匯入功能**：支援三個 Excel 檔案（螺桿資料.xlsx、伺服馬達資料.xlsx、皮帶輪資料.xlsx）的匯入，新增額外欄位（pearl\_dia\_mm, rigidity\_kg\_um, servo\_driver\_model 等），處理重複型號（顯示差異，提供「跳過」、「覆蓋」、「新增」選項），缺少欄位預設值為 0 或空字串，max\_length\_mm 預設 2000。
          - **零件篩選**：根據計算結果篩選馬達、螺桿、皮帶輪。
              - 馬達篩選條件：`required_motor_torque <= rated_torque_nm * (1 + (max_torque_nm - rated_torque_nm) / (2 * rated_torque_nm)), rated_torque_nm >= required_motor_torque, rated_rpm >= required_rpm`。
                [變更註記]

  - **原因**: 為了與第六章的統一變數命名對齊，並提高歷史日誌的可讀性。

  - **舊版內容**: `required_motor_torque`, `rated_torque_nm`, `max_torque_nm`, `rated_rpm`, `required_rpm`

  - **新版內容**: (對應統一變數) `$T_{motor\_req}$`, `$T_{motor\_rated}$`, `$T_{motor\_peak}$`, `$n_{motor,rated}$`, `(需求轉速)`
    [註記結束]
    \- 螺桿篩選條件：`ca_n >= C_ac, C_ac = F_eq * f_w * ((60 * n_eq * L_T) / 10^6)^(1/3), diameter_mm >= 10 mm, 5 <= lead_mm <= 50 mm, 優先 required_lead_mm < lead_mm <= required_lead_mm * 1.1, 無則最接近 required_lead_mm, 驗證 L_t >= L_T, L_t = (ca_n / (F_eq * f_w))^3 * (10^6 / (60 * n_eq))`。
    [變更註記]

  - **原因**: 為了與第六章的統一變數命名對齊，並提高歷史日誌的可讀性。

  - **舊版內容**: `ca_n`, `lead_mm`, `required_lead_mm`

  - **新版內容**: (對應統一變數) `$C_a$`, `$P_h$`, `(需求導程)`
    [註記結束]
    \- 皮帶輪篩選條件：100% 精確匹配 `rated_torque_nm` 和 `rated_rpm`，無匹配則顯示「無可用零件」。

      - **關鍵挑戰**：初始匯入邏輯未處理重複型號，導致數據覆蓋錯誤；篩選條件計算中 L\_T 和 L\_t 的驗證邏輯未完善。
      - **問題診斷**：發現 `database_manager.py` 中的 `_import_excel_data` 函數缺少重複檢查，篩選邏輯在 `analysis_engine.py` 中未包含 L\_t 驗證。
      - **最終解決方案**：新增重複型號處理選項，更新篩選邏輯以包含 L\_t 驗證，並在 `database_management_widget.py` 中加入用戶提示介面。
      - **[GUI 優化] 儀表板與主視窗佈局調整**
          - **新增功能**：在「零件篩選結果」區塊新增多重排序按鈕，允許按 torque、rpm、price 排序。
          - **關鍵挑戰**：排序邏輯初始未穩定，多次點擊導致結果無序。
          - **問題診斷**：`analysis_widget.py` 中的排序函數未處理狀態重置。
          - **最終解決方案**：新增狀態管理，確保排序穩定，更新 `StateManager` 同步數據。
      - **[異常處理] 增強資料處理穩健性**
          - **新增功能**：為 Excel 匯入新增異常處理，處理缺失數據和格式錯誤。
          - **關鍵挑戰**：初始未檢查缺失欄位，導致程式崩潰。
          - **問題診斷**：`pandas.read_excel` 未設定 error\_bad\_lines 參數。
          - **最終解決方案**：新增 try-except 塊，設定預設值並記錄錯誤日誌。
      - **[註記：已解決]** 所有 v1.22 新增功能與整合問題已解決，指南內容與程式碼一致。

  - **[從 v1.21 穩定版 -\> v1.20 穩定版]**

      - **[核心功能擴充] 導入通用角度計算模型**
          - **修正物理模型**：修正了先前版本中，因簡化假設而導致的「靜止負載 ($F_s$)」與「平均軸向負載 ($F_{eq}$)」計算錯誤。
          - **提升通用性**：將「安裝角度 ($\theta$)」從手動模式的固定值，提升為一個在所有模式下均可由使用者設定的通用參數，確保了計算引擎在各種工況下的物理準確性。
          - **更新開發規格書**：為此項重大修正，將《驅動系統計算模組 - 開發規格書》進版至 **v1.1**。
      - **[核心功能擴充] 新增進階工程校核**
          - **新增需求動態負荷計算 ($C_{ac}$)**：在後端計算引擎中，新增了根據使用者壽命需求，反向計算螺桿所需動態負荷的能力。
          - **新增速度校核**：在儀表板的「選定零件規格」區塊，新增了對「最高線速度」的智能符合性校核，即時反饋選定零件組合是否滿足速度要求。
      - **[UI/UX 精修] 儀表板與主視窗佈局優化 (多輪)**
          - **解決佈局過寬問題**：歷經多輪分析與嘗試，最終透過重構 `geometry_widget.py` 中「平台計算與分析」區塊的內部佈局（從 `QFormLayout` 改為 `QVBoxLayout` + `QGridLayout` 的組合），並為結果顯示欄位設定 `setColumnStretch`，從根本上解決了左側面板因內容過長而被不必要撐開的问题。
          - **智慧視窗管理**：
              - **啟動行為可配置**：在 `config.py` 中新增 `STARTUP_SHOW_ANALYSIS` 旗標，允許使用者在啟動時選擇顯示「分析」或「設計」面板。
              - **動態調整**：為「按鈕區」和「結果顯示區」新增 `QGridLayout`，並透過新增可拉伸的「空白欄」吸收多餘空間。
          - **問題回顧 (佈局)**：發現 `geometry_widget.py` 中使用 `QFormLayout` 導致寬度問題，多次測試 `QHBoxLayout` 無效。
          - **問題回顧 (計算)**：發現 `drive_system_engine.py` 中存在簡化假設，導致「靜止負載」與「平均負載」的計算在物理上不完全準確。
          - **計算模型修正方案**：將 `install_angle` 提升為通用參數，並修正了 $F_s$ 與 $F_{eq}$ 的計算公式，確保了模型的普遍適用性與準確性。為此，將《驅動系統計算模組 - 開發規格書》進版至 `v1.1`。
      - **[註記：已解決]** 所有已知的佈局與計算模型問題，均已在 v1.21 穩定版中修正。

  - **[從 v1.19 穩定版 -\> v1.20 穩定版]**

      - **[核心功能升級] 驅動系統計算模組重構**
          - **導入雙模式計算邏輯**：為後端計算引擎 (`drive_system_engine.py`) 導入了「自動模式」與「手動模式」。
              - **手動模式**：實作了基於《開發規格書 v1.0》，從基礎物理參數（移動件重量、加速度等）推導所有結果的精確計算模型。
              - **自動模式**：保留了基於主專案模擬分析結果 (`最大軸向負載`) 進行快速校核的簡化模型。
          - **新增獨立存檔功能**：
              - 建立了全新的 `.dsd` (Drive System Design) 檔案格式，專門用於儲存「手動模式」下的獨立設計參數。
              - 在主視窗 (`main_window.py`) 的檔案選單中新增了「儲存/讀取驅動系統設計」選項。
              - 實現了模式感知 (Mode-aware) 的選單邏輯，可根據使用者當前所處的模式（自動/手動），動態啟用對應的存檔功能。
          - **新增速度校核功能**：在儀表板的零件規格顯示區，新增了「最高線速度」的智能符合性校核，即時反饋選定零件組合是否滿足速度要求。
      - **[核心架構重構] 儀表板介面導入**
          - **導入 `QStackedWidget` 主佈局**：重構了主視窗 (`main_window.py`) 的頂層佈局，使其能夠在「標準分析模式」與全新的「驅動系統設計儀表板」之間進行切換。
          - **建立儀表板專用介面**：將 `drive_system_widget.py` 徹底重構為一組獨立的 UI 模組，並透過 `QStackedWidget` 與主視窗整合。
      - **[註記：已解決]**

  - **[從 v1.18 開發分支 -\> v1.19 穩定版]**

      - **[核心架構重構] UI 導航模式重構**：為從根本上解決一系列頑固的佈局與滾動問題，對主視窗 (`main_window.py`) 進行了關鍵的架構重構。
          - **廢除頁籤佈局**：完全移除了原有的 `QTabWidget` 頁籤佈局模式。
          - **導入功能表列導航**：將「幾何參數設計」、「動力學分析」、「驅動系統計算」三個主要功能模式，直接整合至主功能表列 (`QMenuBar`)，由 `QActionGroup` 管理，確保同一時間只有一個模式處於啟用狀態。
          - **實作獨立滾動區**：為每一個功能面板（`MasterGeometryWidget`, `DynamicsWidget`, `DriveSystemWidget`）配置了其專屬的 `QScrollArea` 容器。此項修改徹底解決了先前版本中，因共享滾動區而導致的垂直滾動不完整與水平滾動功能失效的問題。
      - **[UI/UX 優化] 介面精修與一致性提升**：
          - **介面簡潔化**：移除了主視窗頂部的視角控制工具列 (`QToolBar`)，並將其所有功能整合至「視窗(W)」選單下的「視角控制」子選單中，大幅簡化了主介面。
          - **佈局對齊與間距**：透過為 `QScrollArea` 的子元件外層包裹帶有彈性空間 (`addStretch`) 的容器，成功解決了各功能面板內部元件（如輸入框、按鈕）被不必要水平拉伸的問題，確保了介面佈局的緊湊與和諧。
          - **全域字體統一**：在 `config.py` 中新增了全域 UI 字體配置項，並在 `main_window.py` 中進行應用，確保了整個應用程式字體的一致性。
      - **[錯誤修正] 穩定性提升**：
          - **修正迴歸錯誤 (`AttributeError`)**：修正了在架構重構過程中，因錯誤修改 `get_current_geo_widget()` 輔助函式而導致的程式啟動崩潰問題，恢復了「新建專案」與「讀取專案」功能的穩定性。
          - **修正變數拼寫錯誤 (`NameError`)**：修正了在 `config.py` 中因變數名稱大小寫拼寫錯誤 (`GEOMETRY_SOLVER_Tolerance`) 而導致的「讀取專案」功能失效問題。
      - **[新增] 開發歷程記錄**：在第六章中，**新增**了 v1.19 開發週期中，從診斷頑固的 UI 佈局問題，到最終透過 UI 架構重構與多輪精修達成穩定狀態的全過程記錄。

  - **[從 v1.12 -\> v1.18 開發分支]**

      - **[核心功能新增] 驅動系統計算模組**：為實現驅動系統的選型與校核，新增了完整的後端計算引擎、資料庫管理與前端使用者介面。
          - **資料庫層 (`database_manager.py`)**：建立了使用 SQLite 的零件資料庫，並提供馬達、螺桿、皮帶輪等零件的增刪改查介面。
          - **計算引擎層 (`drive_system_engine.py`)**：實作了基於平台最大軸向力反推馬達所需扭矩、校核安全係數及估算螺桿理論壽命的核心演算法。
          - **使用者介面層 (`drive_system_widget.py`, `database_management_widget.py`)**：建立了完整的驅動系統計算頁籤，提供智慧篩選、零件選型、傳動方式設定及壽命需求輸入等功能；同時提供了一個獨立的資料庫管理對話視窗。

  - **[從 v1.11 -\> v1.12 穩定版]**

      - **[核心功能新增] 平台節點擺角範圍分析**：基於 `AnalysisEngine` 中既有的 LHS 採樣框架，新增了一項全新的分析功能。使用者現在可以一鍵遍歷整個可用工作空間，計算並找出每個獨立關節（A1-A6, B1-B6）在運動過程中所需的最大擺動角度，為硬體選型提供關鍵數據支持。
      - **[核心功能新增] 生成 PDF 設計報告**：建立了一個全新的、獨立的 `report_generator.py` 模組，負責將所有專案數據彙整並生成專業的 PDF 文件。此模組具備進階的字體管理功能，能夠從 `config.py` 讀取並分別設定中、英文字體，確保報告內容的清晰可讀性。
      - **[架構優化] 資料流與狀態管理強化**：為 `state_manager.py` 新增了儲存「節點擺角範圍」分析結果的功能，嚴格遵守了 `StateManager` 作為「唯一數據中心」(Single Source of Truth) 的設計原則。
      - **[UI/UX 微調] 介面佈局與體驗優化**：根據使用者回饋，重新設計了「平台計算與分析」區塊的按鈕佈局，使其更有邏輯性。同時，將主視窗與 3D 預覽視窗的寬度比例，以及主視窗內部左右面板的寬度比例，全部移至 `config.py` 進行集中管理，提升了可配置性。
      - **[錯誤修正] 穩定性提升**：修正了多個因方法遺漏 (`AttributeError`) 或變數名稱拼寫錯誤 (`NameError`) 而導致的程式啟動或分析時的崩潰問題。同時，修正了 PDF 報告中因字體不支援而導致中文字無法顯示的問題。
      - **[新增] 開發歷程記錄**：在第六章中，**新增**了 v1.12 開發週期中，關於擺角分析功能實作與 PDF 中文亂碼問題的解決方案記錄。

  - **[從 v1.10 -\> v1.11 穩定版]**

      - **[核心功能新增] 可選式節點座標顯示**：在「平台姿態控制」面板中，新增了一個「顯示節點座標」的核取方塊。使用者現在可以自由選擇是否在 3D 視圖中，即時顯示每個平台節點的詳細世界座標，便於精確對位與除錯。
      - **[架構重構] 核心參數全面中央化**：為進一步提升專案的可維護性，將大量分散在各個檔案中的硬性編碼參數（如演算法參數、UI 選項文字、物理模型近似值、視窗佈局比例等）**全部遷移至 `config.py`** 進行集中管理，並為每一項參數都添加了詳細的註解。
      - **[架構重構] UI 狀態管理與依賴注入**：為根除一個頑固的 UI 佈局自適應 bug，對 `GeometryWidget`, `SixDofWidget` 和 `ThreeDofWidget` 進行了關鍵的「**依賴注入**」重構。將 `GeometryWidget` 改造為一個**無狀態的 (Stateless)** UI 工廠，徹底解決了因共用實例造成的「狀態污染」問題，並大幅減少了 `three_dof_widget.py` 中的重複程式碼。
      - **[核心修正] 導入路徑全面統一**：為解決因「相對導入」與「絕對導入」路徑混用而導致的、極其頑固的 `ImportError`，對整個 `src/gui/` 目錄下的所有模組進行了一次**全面的導入路徑審查與統一**，全部改為從 `src` 目錄開始的絕對路徑，徹底根除了啟動崩潰問題。
      - **[UI/UX 微調] 介面一致性與可用性提升**：統一了六自由度與三自由度面板中「計算與分析」區塊的按鈕佈局；重新安排並更換了視角控制工具列的圖示；修正了「全域最大出力分析」完成後，分析結果未能同步更新到 UI 的邏輯錯誤。
      - **[新增] 開發歷程記錄**：在第六章中，**新增**了 v1.11 開發週期中，從診斷 UI 佈局問題，到歷經多次重構與除錯，最終達成功能完整並提升架構品質的全過程記錄。

  - **[從 v1.9 -\> v1.10 穩定版]**

      - **[核心模型修正] 引入關節安裝方式選項**：基於關鍵的工程實務洞見，修正了活動平台的物理模型。現在，使用者可以在 UI 介面選擇活動平台關節的安裝方式為「**上置式 (法線朝上)**」或「**下置式 (法線朝下)**」，從根本上解決了先前版本中因模型與物理現實不符而導致的角度計算錯誤與求解器收斂失敗問題。
      - **[UI術語統一] 全域術語更新**：對整個應用程式 UI 介面中的平台命名進行了統一，將「下平台/上平台」全面更新為「**固定平台/活動平台**」。
      - **[新增] 開發歷程記錄**：在第六章中，**新增**了 v1.10 開發與除錯的完整記錄，詳細描述了從發現 `145°` 異常擺角，到最終根據工程實務修正物理模型的整個關鍵決策過程。

  - **[從 v1.8 -\> v1.9 穩定版]**

      - **[核心功能新增] 可選式關節角度限制分析**：為模擬工具新增了關鍵功能，使用者現在可以在工作空間計算中，考慮平台的物理擺動角度限制。
      - **[核心修正] 工作空間分析穩定性**：在 `kinematics.py` 中引入了 `_find_feasible_initial_pose` 的**預處理步驟**，解決了在啟用嚴苛角度限制後，因求解器起始點無效而導致分析必定失敗的數值穩定性問題。
      - **[新增] 開發歷程記錄**：在第六章中，**新增**了 v1.9 開發與除錯的完整記錄，詳細描述了上述數值穩定性問題的診斷與解決過程。

  - **[從 v1.7 -\> v1.8 穩定版]**

      - **[核心功能升級] 全域分析模組**：將「全域最大靜載分析」功能，升級為可由使用者自行選擇分析模式的「**全域最大出力分析**」模組（靜態/動態）。
      - **[核心修正] 執行緒生命週期**：修正了 `QThread` 在完成任務後，因生命週期管理不當而無法再次啟動的問題。
      - **[新增] 開發歷程記錄**：在第六章中，**新增**了 v1.8 開發與除錯的完整記錄。

  - **[從 v1.6 -\> v1.7 穩定版]**

      - **[核心重構] 四層架構導入**：引入全新的四層應用程式架構 (`StateManager`, `CoreEngine`, `AnalysisEngine`, `MainWindow`)，對專案進行了根本性的重構，以實現職責分離與高內聚低耦合。
      - **[核心新增] 全域最大靜載分析功能**：實作了基於 **LHS+SQP** 混合策略的確定性分析演算法。
      - **[核心修正] 多執行緒穩定性**：徹底修復了 `QThread` 崩潰問題。
      - **[新增] 開發歷程記錄**：在第六章中，詳細記錄了 v1.7 開發分支從遇到多執行緒崩潰，到歷經數次除錯與重構，最終達成穩定狀態的全過程。

  - **[從 v1.5 -\> v1.6 正式版]**

      - **[核心修正] 狀態管理與初始化**：解決了在「新建專案」後因狀態污染而導致計算失敗的根本性問題。
      - **[核心修正] 方位與自由度標準化**：新增平台標準方位示意圖，並以此為基準，明確定義了所有自由度的運動方向。
      - **[核心修正] 物理模型校正**：修正了工作空間分析的後端計算邏輯與三自由度平台的旋轉運動學模型。
      - **[新增] 開發歷程記錄**：在第六章中，新增了 v1.6 開發與除錯的完整記錄。

-----

### **第二章：專案目錄架構**

` [開發者註記：v2.2 架構更新] 根據 v2.2-final 交接清單 的檔案列表，此處已更新為 19 個 .py 檔案的精簡架構，移除了 v1.12 中已不存在的  `utils.py`和`report\_widget.py` ，並將  `kinematics\_engine.py`更正為`kinematics.py`。`

```
D:\PYTHON_PROGRAMS\STEWART_PLATFORM\
│
├── .venv/
├── data/
│   └── projects/
├── src/
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py
│   │   ├── analysis_engine.py
│   │   ├── database_manager.py
│   │   ├── dynamics_engine.py
│   │   ├── drive_system_engine.py
│   │   ├── kinematics.py
│   │   ├── report_generator.py
│   │   └── state_manager.py
│   └── gui/
│       ├── __init__.py
│       ├── controls/
│       │   ├── __init__.py
│       │   ├── analysis_widget.py
│       │   ├── database_management_widget.py
│       │   ├── dynamics_widget.py
│       │   ├── drive_system_widget.py
│       │   ├── master_geometry_widget.py
│       │   ├── six_dof_widget.py
│       │   └── three_dof_widget.py
│       ├── geometry_widget.py
│       ├── main_window.py
│       └── pyvista_widget.py
│   └── utils/
│       ├── __init__.py
│       └── custom_widgets.py
├── tests/
├── .gitignore
├── main.py
└── requirements.txt
```

-----

### **第三章：專案初始化與環境設定**

  - **環境需求**：

      - **Python 版本**：建議使用 Python 3.9 或更高版本，確保相容性。
      - **依賴安裝**：使用 `requirements.txt` 安裝以下函式庫：
          - `PyQt6`
          - `PyVista` 與 `PyVistaQT`
          - `NumPy` 與 `SciPy`
          - `Pandas`
          - `openpyxl` (用於 Pandas 讀取 .xlsx)
          - `SQLite` (內建)
          - `reportlab`
          - `pybullet` (保留，暫未使用)
      - **安裝指令**：`pip install -r requirements.txt`。

  - **初始化步驟**：

      - **專案啟動**：執行 `main.py` 啟動應用程式。
      - **配置文件設置**：`config.py` 存儲所有可配置參數。
      - **資料庫初始化**：首次運行時，`database_manager.py` 自動創建 `stewart_platform.db` 資料庫。

  - **測試與部署**：

      - **測試框架**：(建議) 使用 Pytest 運行單元測試。
      - **部署建議**：使用 PyInstaller 打包為執行檔。

-----

### **第四章：核心理念與技術棧**

  - **核心理念**：

      - **模組化與職責分離**：**(v1.7)** 應用程式採用嚴格的四層架構（狀態、核心計算、高層分析、UI），確保高內聚、低耦合。
      - **[修改] 中央化設定**：**(v1.11)** 將所有可調整的預設值、樣式、演算法參數、乃至 UI 選項文字與佈局比例，**全面集中**到單一的設定檔 (`config.py`) 中，實現了設定與程式碼的徹底分離。
      - **驅動系統獨立性**：**(v1.20)** 將驅動系統設計從輔助工具升級為獨立模組，提升專業性。
      - **物理準確性**：**(v1.21)** 通過通用角度模型與校核功能，確保計算結果與現實一致。
      - **資料處理能力**：**(v1.22)** 優化資料庫與篩選，提升大規模零件設計效率。

  - **技術棧**：

      - **計算**：NumPy, SciPy
      - **UI 框架**：PyQt6
      - **3D 可視化**：PyVista 與 PyVistaQT
      - **資料處理**：Pandas
      - **資料庫**：SQLite
      - **報表生成**：ReportLab
      - **測試**：Pytest (建議)

-----

### **第五章：應用程式架構與模組設計**

  - **整體架構**：

      - **導航模式**：**(v1.19)** 採用 `QMenuBar` + `QStackedWidget`，取代 `QTabWidget`，確保單一模式啟用。
      - **儀表板整合**：**(v1.20)** 導入 `QStackedWidget` 切換「標準分析」與「驅動系統設計」模式。
      - **狀態管理**：**(v1.7)** 使用 `StateManager` 作為數據中心。
      - **資料庫模組**：**(v1.22)** 新增 `database_manager.py` 支援 Excel 匯入。

  - **模組細節**：
    `[開發者註記：v2.2 架構更新] 根據 v2.2-final 交接清單 更新檔案列表。`

      - `src/core/kinematics.py`：實現正向/逆向運動學計算。
      - `src/core/config.py`：管理全局配置。
      - `src/core/dynamics_engine.py`：計算推力與負荷。
      - `src/core/state_manager.py`：管理應用狀態與執行緒。
      - `src/core/analysis_engine.py`：執行工作空間與角度分析。
      - `src/core/report_generator.py`：生成 PDF 報表。
      - `src/core/database_manager.py`：處理資料庫與 Excel 匯入。
      - `src/core/drive_system_engine.py`：實現雙模式扭力與壽命計算。
      - `src/gui/main_window.py`：主視窗與導航邏輯。
      - `src/gui/pyvista_widget.py`：3D 可視化渲染。
      - `src/gui/geometry_widget.py`：(v1.11 重構) UI 工廠，建立幾何參數介面。
      - `src/gui/controls/master_geometry_widget.py`：主幾何設計面板。
      - `src/gui/controls/six_dof_widget.py`：六自由度設計。
      - `src/gui/controls/three_dof_widget.py`：三自由度設計。
      - `src/gui/controls/analysis_widget.py`：分析結果顯示。
      - `src/gui/controls/dynamics_widget.py`：動力學模擬。
      - `src/gui/controls/drive_system_widget.py`：驅動系統儀表板。
      - `src/gui/controls/database_management_widget.py`：資料庫管理。
      - `src/utils/custom_widgets.py`：提供自訂的 UI 元件。

  - **模組交互**：

      - 使用 `StateManager` 作為數據中樞，`kinematics.py` 與 `dynamics_engine.py` 提供計算數據，`pyvista_widget.py` 渲染結果，`drive_system_engine.py` 與 `database_manager.py` 獨立運作。

---

### **第六章：變數與參數定義**

[變更註記 - v2.1 (單位系統)]
- **原因**：v2.1 全面從 `m` 遷移至 `mm`。
- **舊版內容 (v1.12)**：GUI 層 (mm) 與 核心引擎 (m) 之間存在轉換。
- **新版內容 (v2.1)**：本章節所有長度單位均已統一為**毫米 (mm)**，以匹配 `kinematics.py` v2.1 之後的核心計算標準。所有 `m` 單位的定義均已廢棄。
[註記結束]

本章定義了應用程式中使用的所有關鍵變數，作為唯一的參考標準。

- **平台幾何參數**：
  - `D_f`：用於 6-DOF 平台，定義為固定平台相鄰節點間的較大弦長 (毫米)。
  - `d_f`：用於 6-DOF 平台，定義為固定平台相鄰節點間的較小弦長 (毫米)。
  - `D_m`：用於 6-DOF 平台，定義為活動平台相鄰節點間的較大弦長 (毫米)。
  - `d_m`：用於 6-DOF 平台，定義為活動平台相鄰節點間的較小弦長 (毫米)。
  
  [變更註記 - v2.2 (3-DOF 幾何模型)]
  - **原因**：根據 `3-DOF 等腰平台幾何參數定義 (v4.6 質心修正版)` 文件，3-DOF 的座標系原點已從「外接圓圓心」變更為「質心」。
  - **舊版內容 (v1.12)**：
    - `D` (m): 固定平台相鄰節點間的弦長。
    - `d` (m): 活動平台相鄰節點間的弦長。
  - **新版內容 (v2.2)**：
    - `D_1`：用於 3-DOF 平台，定義為固定平台等腰三角形的底邊長度 (A1 到 A5) (毫米)。
    - `D_2`：用於 3-DOF 平台，定義為固定平台等腰三角形的高度 (從 A1-A5 中點到 A3) (毫米)。
    - `d_1`：用於 3-DOF 平台，定義為活動平台等腰三角形的底邊長度 (B1 到 B5) (毫米)。
    - `d_2`：用於 3-DOF 平台，定義為活動平台等腰三角形的高度 (B3 到 B1-B5 中點) (毫米)。
  [註記結束]

  - `R_a`：固定平台半徑 (毫米)，自動計算。
  - `R_b`：活動平台半徑 (毫米)，自動計算。
  - `L`：腿長 (毫米)，電動缸最小機械長度。
  - `s`：行程 (毫米)，電動缸可用延伸長度。
  - `s_buffer`：安全裕量 (毫米)，電動缸安全緩衝區。
  - `s_mech`：機械行程 (毫米)，自動計算自 `s + s_buffer`。

  [變更註記 - v2.2 (h/H 高度定義)]
  - **原因**：根據 `v4.3` 和 `v4.6` 幾何文件，`H`（自然平衡零位高度）的定義是基於 `L_mid`（行程中點），且 `h`（初始高度）是基於 `L_min`。
  - **舊版內容 (v1.12)**：(h 和 H 的定義基於 `L` 和 `L + s/2 + s_buffer/2`，且單位為 `m`)
  - **新版內容 (v2.2)**：
    - `h`：平台初始高度 (Initial Height) (毫米)。定義為當平台處於零位姿態 $(Roll=0, Pitch=0, Yaw=\theta)$ 時，可達到的**最低** $Z$ 軸高度。此高度由具有**最小**水平投影距離 ($D_{xy, \min}$) 的致動器決定，且該致動器長度等於 $L_{min}$ (即 $L$)。
    - `H`：自然平衡零位高度 (Natural Balance Zero Position Height) (毫米)。定義為當平台處於零位姿態 $(Roll=0, Pitch=0, Yaw=\theta)$ 時，能使其 6 根 (或 3 根) 致動器 $L_i$ 的**平均長度**等於 $L_{mid}$ ($L + s_{buffer}/2 + s/2$) 時的 $Z$ 軸高度。
  [註記結束]
  
  - `Δθ`：相位角 (Phase Angle)，也可稱為交錯角 (Stagger Angle)。[開發者註記 v2.2：`kinematics.py` 中的 `get_phase_angle_deg` 函數 實作了此計算]。這是一個由上下平台的幾何形狀（`D_f`, `d_f`, `D_m`, `d_m`）決定的內建幾何特性。

- **關節與角度參數**：
  - `base_joint_limit`：基座關節限制角度 (rad)，[UI顯示為 deg]。
  - `platform_joint_limit`：平台關節限制角度 (rad)，[UI顯示為 deg]。
  - `θ`：安裝角度 (rad)，[UI顯示為 deg]，通用參數，影響負載計算。
  - `zero_pose_offset`：零位偏移 (x, y 毫米)，自動計算。

- **動力學參數**：
  [變更註記 - v2.1 (單位系統)]
  - **原因**：v2.1 將加速度單位從 `m/s²` 統一為 `mm/s²`。
  - **舊版內容 (v1.12)**：`a` (m/s²), `g` (9.81 m/s²)。
  - **新版內容 (v2.1)**：
    - `a_lin`：線性加速度 (mm/s²)，預設 [0.0, 0.0, 4903.325] (即 0.5g)。
  [註記結束]
  - `m_p`：平台質量 (公斤)，預設 200.0。
  - `m_l`：負載質量 (公斤)，預設 500.0。
  - `com_l_x/y/z`：負載質心偏移 (毫米)，預設 0.0/0.0/700.0。
  - `a_ang`：角加速度 (rad/s²)，預設 [0.0, 0.0, 0.0]。
  - `I`：平台的轉動慣量張量 (Inertia Tensor) (kg·m²)，[開發者註記 v1.12：目前在 `dynamics_engine.py` 中採用固定近似值]。

- **驅動系統參數**：
  - `m`：移動件重量 (質量) (公斤)，用於手動模式。
  - `a_max`：最高加速度 (m/s²)，用於手動模式。
  - `μ`：摩擦係數，預設 0.01。
  - `f_w`：負荷係數，自動計算或手動輸入。
  - `η_screw`：螺桿效率 (%)，預設 90.0。
  - `η_pulley`：傳動效率 (%)，預設 95.0。
  - `R_h`：每日運轉小時數 (小時)，用於壽命計算。
  - `R_d`：每年運轉日數 (天)，用於壽命計算。
  - `R_y`：運轉年數 (年)，用於壽命計算。
  - `Aval`：稼動率 (%)，用於 L_T 計算。
  - `i`：減速比，支援輸入或皮帶輪選擇。
  - `use_pulley_selection`：使用皮帶輪選擇 (布爾值)，決定減速比來源。

- **姿態參數**：
  - $T = (T_x, T_y, T_z)$ (毫米)：平台的平移位移向量，相對於自然平衡零位。
  - $R = (\phi, \theta, \psi)$ (rad)：平台的旋轉歐拉角 (Roll, Pitch, Yaw)。

- **計算結果**：
  - $L_i$ (毫米)：各電動缸的即時長度。
  - $F_i$ (N)：各電動缸的即時出力。
  - $F_{max}$ (N)：全域最大出力。

- **[開發者註記：v2.0 變數統一]** 本章節所有與電動缸計算相關的變數，均已更新至《計算指南 2.0》的最終統一版本。

---

### **第七章：數學模型與演算法**

[開發者註記：v2.2 彙整] 本章節整合了 v1.12 的「4.1 平台標準方位」章節，並將 v2.0 的驅動系統模型，以及 v2.2 的最新幾何模型 (`v4.3` / `v4.6`) 合併於此。

#### **7.1 平台標準方位與自由度定義 (來自 v1.8 & v1.12)**

[開發者註記：v2.2] 此 `v1.12` 的標準方位定義仍然有效，是所有運動學計算的基礎。

  * **基準座標系與標準方位**

      * **原點**: `(0, 0, 0)` 固定於**下平台**的幾何中心。
      * **座標軸**: 採用右手座標系。`+X` 軸水平向右，`+Y` 軸水平向前，`+Z` 軸垂直向上。
      * **標準初始方位**: 所有平台的初始方位定義，皆以此座標系為基準。下平台的標準方位如下圖所示，此為所有運動學與視覺化呈現的唯一標準。

  * **平台方位示意圖**

      * **六自由度平台 (俯視圖)**:

          * 平台沿 **Y 軸**鏡像對稱。
          * `A1-A6` 弦平行於 X 軸，位於 Y 軸負方向。
          * 節點從右下角 `A1` 開始，**逆時針**編號。

      * **三自由度平台 (俯視圖)**:

          * 平台沿 **Y 軸**鏡像對稱。
          * `A3` 節點位於 Y 軸正向頂點。
          * `A1-A5` 底邊平行於 X 軸，位於 Y 軸負方向。

  * **自由度定義** 本節定義**上平台**相對於**下平台**的運動。所有旋轉均符合右手定則，但前端控制邏輯會進行調整以符合操作直覺。

      * **平移動作 (Translation)**:
          * **Surge (X)**: 沿 X 軸的左右運動。`+X` 為向**右**。
          * **Sway (Y)**: 沿 Y 軸的前後運動。`+Y` 為向**前**。
          * **Heave (Z)**: 沿 Z 軸的垂直運動。`+Z` 為**上升**。
      * **轉動動作 (Rotation)**:
          * **Pitch (俯仰)**: 繞 **X 軸** (橫軸) 旋轉。**操作定義**：增加 Pitch 值，平台**前端 (Y+) 抬起**。
          * **Roll (滾轉)**: 繞 **Y 軸** (縱軸) 旋轉。**操作定義**：增加 Roll 值，平台**右側 (X+) 下壓**。
          * **Yaw (偏航)**: 繞 **Z 軸** (垂直軸) 旋轉。**操作定義**：增加 Yaw 值，從上方俯視，平台進行**逆時針**旋轉。
      * **三自由度平台的自由度**: 包含 **Heave**, **Pitch**, **Roll**。

#### **7.2 運動學模型**

  - **逆向運動學 (Inverse Kinematics)**: 給定平台的姿態 $P = (T, R)$，計算各電動缸的長度 $L_i$。
    公式： $L_i = || p_i - q_i ||$，其中 $p_i$ 為活動平台節點，$q\_i$ 為固定平台節點。
  - **正向運動學 (Forward Kinematics)**: (即「計算零位」) 給定腿長，使用 `least_squares` 優化求解平台姿態。

  [變更註記 - v2.2 (幾何模型)]
  - **原因**：根據 `v4.3` 和 `v4.6` 幾何文件，更新半徑計算的數學模型，以匹配 `kinematics.py` 的最新實作。
  - **舊版內容 (v1.12)**：`半徑計算：從弦長計算半徑 $ R = \frac{D^2}{8h} + \frac{h}{2} $` (註：此為 3-DOF 舊公式，且 h 未定義)。
  - **新版內容 (v2.2)**：
    - **6-DOF 半徑計算**：半徑 $R$ 是以下非線性方程式的解，需使用 `scipy.optimize.minimize` 求解（`fsolve` 經測試證明不穩定）：
      $$\left| \arcsin\left(\frac{D}{2R}\right) + \arcsin\left(\frac{d}{2R}\right) - \frac{\pi}{3} \right| = 0$$
      其中 `D` 為長弦長，`d` 為短弦長。
    - **3-DOF 半徑計算**：半徑 $R$（外接圓半徑）：
      $$R = \frac{a^2 \cdot b}{4 \cdot Area}$$
      其中 $a = \sqrt{(base/2)^2 + height^2}$ (斜邊長), $b = base$ (底邊長), $ Area = 0.5 \cdot base \cdot height $。
  [註記結束]

#### **7.3 動力學與驅動系統模型 (來自 v2.0)**

  1.  **運動負載模型 (7-stage Model)**
      - 系統推力應根據完整的七段式運動負載模型計算，包含上升/下降的加速、等速、減速以及靜止階段。詳細公式如下：
        - $F_{ua} = m \cdot g \cdot (\sin\theta + \mu \cdot \cos\theta) + m \cdot a$
        - $F_{uc} = m \cdot g \cdot (\sin\theta + \mu \cdot \cos\theta)$
        - $F_{ud} = m \cdot g \cdot (\sin\theta + \mu \cdot \cos\theta) - m \cdot a$
        - $F_{da} = \mu \cdot m \cdot g \cdot \cos\theta - m \cdot g \cdot \sin\theta - m \cdot a$
        - $F_{dc} = \mu \cdot m \cdot g \cdot \cos\theta - m \cdot g \cdot \sin\theta$
        - $F_{dd} = m \cdot a + \mu \cdot m \cdot g \cdot \cos\theta - m \cdot g \cdot \sin\theta$
        - $F_s = \max(0, m \cdot g \cdot \sin\theta - \mu \cdot m \cdot g \cdot \cos\theta)$
  2.  **平均(等效)軸向負載 ($F_{eq}$)**
      - 該公式用於壽命計算，綜合考慮了各階段負載與運行時間。
        $$F_{eq} = \left( \frac{\sum |F_i|^3 \cdot t_i}{\sum t_i} \right)^{1/3}$$
  3.  **壽命計算 ($L_t$) 與需求動負荷 ($C_{ac}$)**
      - 根據 $F_{eq}$ 和平均轉速 $n_{eq}$，計算預估壽命 $L_t$ 與需求動負荷 $C_{ac}$。
        $$L_t = \left(\frac{C_a}{F_{eq} \cdot f_w}\right)^3 \cdot \frac{10^6}{60 \cdot n_{eq}}$$ $$C_{ac} = F_{eq} \cdot f_w \cdot \left( \frac{60 \cdot n_{eq} \cdot L_T}{10^6} \right)^{\frac{1}{3}}$$
  4.  **扭矩計算模型**
      - 扭矩計算需區分螺桿效率與傳動效率，並注意單位轉換。
        - **軸向力轉螺桿扭矩**: $T_{screw} = \frac{F_{i} \cdot P_h}{2 \pi \cdot \eta_{screw} \cdot 1000}$
        - **需求馬達扭矩**: $T_{motor\_req} = \frac{T_{screw}}{i \cdot \eta_{pulley}}$
        - **組合公式**: $T_{motor\_req} = \frac{F_{i} \cdot P_h}{2 \pi \cdot \eta_{screw} \cdot i \cdot \eta_{pulley} \cdot 1000}$

- **[開發者註記：v2.0 模型已更新]** 本章節的數學模型已與《計算指南 2.0》 完全同步。

#### **7.4 優化演算法**

  - **工作空間分析 (v1.9)**：使用 `SLSQP` 優化器求解邊界。為確保數值穩定性，演算法包含一個 `_find_feasible_initial_pose` 預處理步驟，用以在啟用角度限制時尋找一個有效的起始點。[開發者註記 v2.2：此功能目前因 `_find_limit` 的數值穩定性問題而**失效**]。
  - **全域分析演算法 (v1.7)**：使用 **LHS + SQP** 混合策略。使用 `scipy.stats.qmc.LatinHypercube` 生成初始採樣點，然後使用 `scipy.optimize.minimize(method='SLSQP')` 對最佳候選點進行精煉。

---

### **第八章：功能說明**

[開發者註記：v2.2 彙整] 本章節內容基於 v1.12 的完整描述，並整合了 v1.22 新增的資料庫與篩選功能。

#### **8.1 主視窗 (`main_window.py`)**
- **導航模式 (v1.19)**：使用 `QMenuBar` 在「幾何參數設計」、「動力學分析」、「驅動系統計算」三大功能間切換。
- **狀態管理**：管理 `StateManager`，追蹤 `is_dirty` 狀態並在關閉前提示儲存。
- **檔案 I/O**：處理專案 (`.json`) 和驅動系統設計 (`.dsd`) 的儲存與讀取。
- **執行緒管理 (v1.7)**：安全地建立、啟動 `AnalysisEngine` 背景執行緒，並透過信號槽接收結果，管理 `QProgressDialog`。
- **3D 視窗連動**：控制 `PyVistaWidget` 的顯示、隱藏與視角變更。
- **[v2.1 更新] 單位轉換提示**：在載入舊版 `m` 單位專案檔並轉換成功後，彈出提示視窗。

#### **8.2 幾何設計 (`master_geometry_widget.py` / `six_dof_widget.py` / `three_dof_widget.py`)**
- **平台切換**：允許使用者在 6-DOF 和 3-DOF 之間切換，動態載入對應的參數面板。
- **參數輸入**：提供 `Df, df, Dm, dm` (6-DOF) 或 `D1, D2, d1, d2` (3-DOF) 及通用電動缸參數（`L, s, s_buffer`）的輸入。
- **自動計算**：
  - **確認參數**：自動計算 `Ra`, `Rb`, `s_mech`。
  - **計算零位**：求解 `H`（自然平衡零位高度）、`h`（初始高度）、相位角 `Δθ` 及零位姿態下的關節擺角。
- **分析啟動**：提供按鈕以觸發「可用工作空間」、「機械極限空間」和「節點擺角範圍」分析。
- **關節限制 (v1.9)**：提供「啟用關節角度限制」選項，允許使用者在計算中納入物理擺角約束。
- **安裝方式 (v1.10)**：提供「上置式/下置式」選項，以確保物理模型正確。

#### **8.3 姿態控制與分析 (`analysis_widget.py`)**
- **即時姿態控制**：提供 6 個（或 3 個）自由度的滑塊，允許使用者即時調整平台姿態。
- **工作空間顯示**：顯示 `_analyze_workspace_...` 方法計算出的工作空間範圍。[v2.2 修正]：現在能正確處理 `mm` 單位和 `deg` 單位，並顯示 `Heave` 的相對座標。
- **姿態展演**：提供「開始展演」、「循環展演」等功能，用於動態演示平台運動。
- **視覺化選項 (v1.11)**：提供「顯示關節角度」和「顯示節點座標」的核取方塊。

#### **8.4 動力學分析 (`dynamics_widget.py`)**
- **參數輸入**：允許使用者輸入 `m_p`, `m_l`, `com_l_z` 等動力學參數。
- **當前姿態分析**：計算並顯示 `analysis_widget.py` 滑塊所在位置的即時致動器出力。
- **全域出力分析 (v1.8)**：觸發 `AnalysisEngine` 執行 LHS+SQP 搜尋，找出整個工作空間內出力最大的點。支援「靜態」和「動態」兩種分析模式。

#### **8.5 驅動系統計算 (`drive_system_widget.py`)**
- **雙模式計算 (v1.20)**：
  - **自動模式**：自動從「動力學分析」頁籤獲取 `F_max`，用於快速校核。
  - **手動模式**：允許使用者獨立輸入所有工程參數（`m`, `a_max`, `v_max`, 運動剖面等），執行完整的七段式運動負載模型計算。
- **零件選型 (v1.22)**：從資料庫讀取馬達、螺桿、皮帶輪資料，並根據計算結果（`T_motor_req`, `C_ac` 等）動態篩選可用零件。
- **壽命計算**：根據運動剖面和壽命需求（`R_h`, `R_d`, `R_y`）計算 `F_eq`, `n_eq`，並反推 `C_ac`（需求動負荷）和 `L_t`（預估壽命）。
- **獨立存檔 (v1.20)**：支援將「手動模式」下的設計參數儲存為獨立的 `.dsd` 檔案。

#### **8.6 資料庫管理 (`database_management_widget.py`)**
- **零件管理 (v1.18)**：提供一個獨立對話視窗，用於對馬達、螺桿、皮帶輪資料庫執行新增、刪除、修改、查詢 (CRUD) 操作。
- **Excel 匯入 (v1.22)**：支援從外部 Excel 檔案批量匯入零件資料，並提供重複型號處理（跳過、覆蓋、新增）功能。

#### **8.7 報告生成 (`report_generator.py`)**
- **PDF 匯出 (v1.12)**：允許使用者將當前專案的所有關鍵數據（幾何參數、動力學參數、所有分析結果）匯出為一份專業的、帶有中文字體的 PDF 設計報告。

---

### **第九章：技術規格**

[開發者註記：v2.2 彙整] 本章節內容基於 v1.12 的完整描述，並整合了 v1.22 的資料庫規格。

- **核心引擎 (`kinematics.py`)**
  - 演算法：SciPy SLSQP, Least Squares
  - [v2.2 更新] 6-DOF 半徑求解：`scipy.optimize.minimize(method='SLSQP')`
  - [v2.2 更新] 3-DOF 節點模型：質心座標系
  - [v2.2 狀態：不穩定] `_find_limit` 方法因變數正規化缺陷導致數值不穩定。
- **動力學引擎 (`dynamics_engine.py`)**
  - 單位：輸入 `mm`/`kg`/`rad`，內部轉換為 `m` 進行力學計算，輸出 `N`。
  - 模型：逆動力學，基於雅可比矩陣轉置。
- **分析引擎 (`analysis_engine.py`)**
  - 策略：LHS (拉丁超立方採樣) + SQP (序列二次規劃)。
  - 執行緒：`QThread` 背景執行緒，透過 `pyqtSignal` 通訊。
- **驅動系統引擎 (`drive_system_engine.py`)**
  - 模型：七段式運動負載模型、平均負載 $F_{eq}$、壽命 $L_t$、需求動負荷 $C_{ac}$、扭矩 $T_{motor\_req}$。
- **資料庫 (`database_manager.py`)**
  - 技術：SQLite
  - 表 (Tables)：`motors`, `screws`, `pulleys`。
  - [v1.22 新增] `motors` 欄位：`rated_power_kw`, `rated_speed_rpm`, `rated_torque_nm`, `max_torque_nm`, `servo_driver_model` 等。
  - [v1.22 新增] `screws` 欄位：`dia_mm`, `lead_mm`, `ca_n` (N), `coa_n` (N), `rigidity_kg_um` 等。
  - [v1.22 新增] `pulleys` 欄位：`teeth`, `diameter_mm`, `belt_width_mm`。
- **GUI (`main_window.py`)**
  - 框架：PyQt6
  - 導航 (v1.19)：`QMenuBar` + `QStackedWidget` + `QScrollArea`。
- **3D 渲染 (`pyvista_widget.py`)**
  - 框架：PyVista / PyVistaQT
  - [v2.1 更新] 單位：內部渲染單位為 `m`。`rebuild_scene` 和 `update_platform_pose` 方法在入口處使用 `MM_TO_M = 0.001` 常數將傳入的 `mm` 數據轉換為 `m`。

---

### **第十章：使用指南**

[開發者註記：v2.2 彙整] 本章節內容基於 v1.12 的完整描述，並整合了 v1.22 的資料庫匯入步驟。

1.  **啟動程式**：執行 `main.py`。
2.  **檔案操作**：
    * **新建專案**：點擊「檔案」->「新建專案」。
    * **讀取專案**：點擊「檔案」->「讀取專案...」，選擇 `.json` 檔案。[v2.1 更新] 程式會自動轉換舊的 `m` 單位檔案。
    * **儲存專案**：點擊「檔案」->「儲存專案」。
3.  **模式切換 (v1.19)**：使用主功能表列的「幾何參數設計」、「動力學分析」、「驅動系統計算」在不同功能面板間切換。
4.  **幾何參數設計**：
    * 選擇「六自由度」或「三自由度」平台類型。
    * 輸入 `Df, df, Dm, dm`（或 `D1, D2, d1, d2`）以及 `L, s, s_buffer` 等參數。
    * 點擊「確認所有幾何參數」，程式將自動計算 `Ra`, `Rb`, `s_mech`。
    * 點擊「計算零位」，程式將求解 `H`, `h`, `Δθ` 及零位擺角。
    * [v1.9 新增] 可選：勾選「啟用關節角度限制」並設定角度，再點擊「計算零位」以進行受限求解。
5.  **工作空間分析**：
    * 在「計算零位」成功後，點擊「分析可用工作空間」或「分析機械極限空間」。
    * 分析結果將顯示在右側「平台空間範圍」區塊。
    * [v1.12 新增] 點擊「分析節點擺角範圍」以獲取硬體選型所需的擺角數據。
6.  **姿態控制與 3D 預覽**：
    * 拖動右側滑塊可即時改變 3D 視窗中的平台姿態。
    * [v1.11 新增] 勾選「顯示節點座標」或「顯示關節角度」以載入輔助視覺資訊。
    * 3D 視窗可使用滑鼠右鍵拖曳（平移）、左鍵拖曳（旋轉）、滾輪（縮放）。
    * 可使用「視窗」->「視角控制」選單來切換預設視角。
7.  **動力學分析**：
    * 切換至「動力學分析」面板。
    * 輸入 `m_p`, `m_l`, `com_l_z` 等參數。
    * 點擊「計算當前姿態出力」，查看當前滑塊姿態下的致動器出力。
    * [v1.8 新增] 勾選（或取消勾選）「僅考慮靜態負載」，然後點擊「分析全域最大出力」，程式將在背景執行緒中搜尋整個工作空間的最差出力點。
8.  **驅動系統計算 (v1.20, v1.22)**：
    * 切換至「驅動系統計算」面板。
    * **資料庫管理**：首次使用時，點擊「資料庫維護」->「零件資料庫管理...」，使用「匯入 Excel」功能將您的零件資料 (`.xlsx`) 匯入程式資料庫。
    * **自動模式**：保持「自動模式」選中，此模式會自動從「動力學分析」結果中讀取 `F_max`。
    * **手動模式**：選中「手動模式」，輸入 `m`, `a_max`, `v_max` 及七段運動剖面時間等參數。
    * **零件選型**：在「零件選型」區塊，從下拉選單中選擇您匯入的馬達和螺桿。
    * **減速比**：選擇「直接輸入」或「透過皮帶輪選擇」。
    * **壽命需求**：輸入 `R_h`, `R_d`, `R_y` 等參數。
    * **計算**：點擊「開始計算與校核」，程式將在右側顯示 `T_motor_req`, `C_ac`, `L_t` 等結果，並在零件規格區塊高亮顯示不符合需求的項目。
    * **存檔**：若在「手動模式」下，可點擊「檔案」->「儲存驅動系統設計...」將此次設計儲存為 `.dsd` 檔案。
9.  **生成報告 (v1.12)**：
    * 在完成所有分析後，點擊「檔案」->「生成設計報告...」。
    * 選擇儲存路徑，程式將匯出包含所有參數與分析結果的 PDF 文件。

---

### **第十一章：開發記錄與迭代修正**

[開發者註記：v2.2 彙整] 本章節內容來自 `v1.12` 文件的原始日誌，`v1.19` 至 `v2.2` 的日誌已被移至文件最開頭的「版本變更日誌」部分，以確保最新的變更最先被看到。

* **6.1 迭代修正一：物理模型與方位定義的標準化 (v1.6)**
  - **主題**：確保軟體的計算、顯示與物理現實完全一致。
  - **遇到的問題與決策記錄**：
    1.  **行程定義偏差**：初版對「可用行程」的定義忽略了 `s_buffer`，導致工作空間分析不準確。
    2.  **方位定義顛倒**：初版六自由度平台的初始方位與設計示意圖顛倒 180 度，是導致旋轉方向混亂的根源。
    3.  **旋轉直覺不符**：在方位修正後，純數學的「右手定則」旋轉方向與模擬器操作員的直覺（如 `+Pitch`=抬頭）不符。
    4.  **3-DOF 旋轉模型錯誤**：初版三自由度平台的旋轉是圍繞「外接圓圓心」而非「幾何中心」，導致動作異常。
  - **最終方案**：
    1.  **明確物理模型**：在第三章中，以圖文方式明確電動缸行程的物理分佈，並以此為基準，重寫了 `kinematics.py` 中所有相關的計算邏輯（`H` 高度，工作空間分析）。
    2.  **強制標準方位**：在 `kinematics.py` 中，透過增加 180 度旋轉偏移，強制使六自由度平台的初始方位與第四章新增的「標準方位示意圖」對齊。
    3.  **前端體驗適配**：在 `main_window.py` 的 `update_3d_view` 函式中，對從 UI 獲取的 Roll 值乘以 `-1`，將純粹的數學旋轉轉換為符合操作直覺的視覺回饋，實現了前後端邏輯的解耦。
    4.  **修正 3-DOF 模型**：在 `kinematics.py` 中，重寫了三自由度平台的運動學計算函式，確保其旋轉是圍繞幾何中心（形心）進行的。
  - **[註記：已解決]** 所有上述問題已在 v1.6 穩定版中透過物理模型校正與方位標準化解決。

* **6.2 迭代修正二：狀態管理與初始化流程的根本性修復 (v1.6)**
  - **主題**：解決在點擊「新建專案」後，程式無法再次進行計算的嚴重迴歸錯誤。
  - **遇到的問題與決策記錄**：
    1.  **錯誤的根源 1 (職責不清)**：子元件 (`six_dof_widget.py`) 在參數被修改時，錯誤地承擔了「禁用按鈕」的職責，導致主視窗 (`main_window.py`) 無法在正確的時機重新啟用它們。
    2.  **錯誤的根源 2 (重置不全)**：「新建專案」的流程沒有徹底重置所有相關的 UI 元件（特別是 `AnalysisWidget`），導致舊的狀態殘留。
  - **最終方案：確立單一、權威的狀態管理流程**：
    1.  **`on_new_project` 為唯一重置入口**：`main_window.py` 中的 `on_new_project` 被確立為唯一、權威的應用程式重置函式。
    2.  **`parameter_changed` 職責簡化**：子元件中的 `_on_parameter_changed` 函式被重構。其**唯一**職責是發射 `parameter_changed` 訊號。
    3.  **`_mark_as_dirty` 職責釐清**：`main_window.py` 中的 `_mark_as_dirty` 函式現在是處理 `parameter_changed` 訊號的**唯一**插槽。其**唯一**職責是將 `is_dirty` 旗標設為 `True`，並呼叫子元件的 `reset_all_calculated_values()` 來清除過期的計算結果並禁用後續按鈕。
  - **[註記：已解決]** 所有上述問題已在 v1.6 穩定版中透過狀態管理重構解決。

* **6.3 迭代修正三：v1.7 多執行緒穩定性重構**
  - **主題**：解決在點擊「分析全域最大靜載」後，程式因多執行緒問題而卡死或崩潰的根本性錯誤。
  - **最終診斷 (`QThread Destroyed`)**：根本原因在於 `MainWindow` 的**物件生命週期管理不當**：它在收到 `analysis_finished` 訊號後，過早地將 `self.analysis_thread` 設為 `None`，導致背景執行緒在其標準退出流程完成前就被強制銷毀。
  - **最終方案：確立 v1.7 的四層架構與安全執行緒模型**：
    1.  **引入 `StateManager`**：引入 `StateManager` 作為唯一數據中心。
    2.  **嚴格單位劃分**：確立了「**後端統一用弧度，前端協調器負責轉角度**」的原則。
    3.  **執行緒安全重構**：
          * 將 `AnalysisEngine` 改造為**完全無狀態**的獨立工作者。
          * `MainWindow` 採用「**任務資料包**」模式，在啟動執行緒前打包好所有數據。
          * `MainWindow` 採用**基於 `finished` 訊號的清理機制**，確保 `QThread` 被安全地清理。
  - **[註記：已解決]** 所有上述問題已在 v1.7 穩定版中透過四層架構與執行緒重構解決。

* **6.4 迭代修正四：v1.8 全域分析功能升級與執行緒生命週期完善**
  - **主題**：將「全域最大靜載分析」升級為可選模式的「全域最大出力分析」，並修復在連續執行分析時遇到的 `QThread` 狀態未重置問題。
  - **根本原因**：`quit()` 這個終止指令被錯誤地連接到了 `finished` 訊號上，但 `finished` 訊號只有在事件迴圈終止後才會發出，造成了死結。
  - **最終方案：功能擴充與生命週期閉環**：
    1.  **UI 擴充**：在 `DynamicsWidget` 中新增一個核取方塊，允許使用者明確選擇分析模式（靜態或動態）。
    2.  **生命週期閉環**：在 `MainWindow` 中，將 `quit()` 的觸發時機，改為在 `on_analysis_finished` 槽函式的**結尾處手動呼叫**，確保執行緒能被正常終止。
  - **[註記：已解決]** 所有上述問題已在 v1.8 穩定版中透過功能升級與執行緒生命週期完善解決。

* **6.5 迭代修正五：v1.9 工作空間分析穩定性與物理約束實作**
  - **主題**：實作「可選式關節角度限制」功能，並解決因此引入的 `SciPy` 最佳化求解器數值穩定性問題。
  - **根本原因診斷 (AI 協作)**：
        * **無效的起始點**：失敗的**最根本原因**，是我們的求解器起始點 `neutral_pose`（零位姿態），在嚴苛的角度限制下，其**本身就是一個物理上不可能達成的「無效點」**。
        * **不穩健的數學模型**：在約束條件中直接使用 `arccos` 函數來計算角度，在數值計算上是不穩定的。
  - **最終方案：根本性的演算法重構**：
    1.  **升級約束模型**：將所有角度限制的計算，升級為更穩定、對求解器更友好的 `cos(angle) >= cos(limit)` 內積（點積）比較形式。
    2.  **新增「可行點搜索」預處理**：在正式進行邊界搜索**之前**，新增了一個名為 `_find_feasible_initial_pose` 的預處理步驟，專門尋找一個能**同時滿足所有行程與角度限制**的有效姿態點。
  - **[註記：已解決]** 所有上述問題已在 v1.9 穩定版中透過演算法重構解決。

* **6.6 迭代修正六：v1.10 物理模型修正與 UI/UX 最終完善**
  - **主題**：根據工程實務，從根本上修正活動平台的物理模型，並統一全域 UI 術語。
  - **根本原因診斷 (來自使用者的工程洞見)**：軟體一直採用一個與工程現實不符的「上置式」物理模型，導致計算出的理論擺角 (`~145°`) 遠超物理極限。
  - **最終方案：確立 v1.10 穩定基線**：
    1.  **修正核心物理模型**：在 `kinematics.py` 中，新增了 `platform_joint_style` 參數，允許 `_calculate_joint_angles` 函式根據「上置式」或「下置式」使用正確的法線方向。
    2.  **升級 UI 功能**：在 `geometry_widget.py` 中，新增了「活動平台關節安裝方式」的單選按鈕，並將「**下置式 (法線朝下)**」設為預設選項。
    3.  **統一全域術語**：將「下平台/上平台」統一更新為「**固定平台/活動平台**」。
  - **[註記：已解決]** 所有上述問題已在 v1.10 穩定版中透過物理模型修正與 UI 統一解決。

* **6.7 迭代修正七：v1.11 UI 佈局自適應與狀態管理重構**
  - **主題**：解決一個在特定操作（勾選「啟用關節角度限制」）下，UI 左側面板寬度無法完美自適應的頑固 bug。
  - **二次診斷 (狀態污染)**：發現 `SixDofWidget` 和 `ThreeDofWidget` **共用了同一個** `GeometryWidget` 的實例，導致狀態污染。
  - **最終方案：無狀態工廠與依賴注入**：
    1.  **重構 `GeometryWidget` 為無狀態工廠**：`create_*` 方法不再儲存 UI 元件，而是將其**作為傳回值**。
    2.  **`Six/ThreeDofWidget` 管理自身狀態**：子元件現在負責儲存自己的 UI 元件引用。
  - **[註記：已解決]** 成功解決了狀態污染與佈局自適應問題，並強化了「依賴注入」架構。

* **6.8 迭代修正八：核心參數全面中央化與導入路徑統一 (v1.11)**
  - **主題**：提升專案的可維護性與穩健性。
  - **遇到的問題**：1. 參數被硬性編碼在各個檔案中。 2. 混合使用相對/絕對導入路徑導致 `ImportError`。
  - **最終方案**：
    1.  **全面遷移至 `config.py`**：將所有可調整參數全部遷移至設定檔。
    2.  **統一為絕對導入**：將所有 `src/gui/` 目錄下的導入路徑統一為從 `src` 開始的絕對路徑。
  - **[註記：已解決]**

* **6.9 迭代修正九：v1.12 新增分析維度與報告功能**
  - **主題**：擴充分析維度，為硬體選型提供數據支持；並實現數據持久化，生成專業的設計報告。
  - **最終方案 (擺角分析)**：複用 `AnalysisEngine` 的 LHS 採樣框架，在 `run_angle_range_analysis` 方法中呼叫 `get_current_joint_angles_and_vectors` 來計算並追蹤每個關節的最大擺角。
  - **最終方案 (PDF 報告)**：新建 `report_generator.py` 模組，實作基於 `reportlab` 的動態字體註冊機制，從 `config.py` 讀取字體路徑，解決中文亂碼問題。
  - **[註記：已解決]**

---

### **第十二章：推薦的第三方函式庫與資源**

[開發者註記：v2.2 彙整] 本章節內容基於 v1.12 的完整描述。

1.  **3D 視覺化: `PyVista` 與 `PyVistaQT`**
      * **用途**: 實現 3D 視覺化介面的核心。`PyVista` 提供對 `VTK` 的高階封裝；`PyVistaQT` 提供 `QtInteractor` 元件以嵌入 PyQt。
      * **資源**: `PyVista` 官方文件及 GitHub 儲存庫。

2.  **物理模擬 (未來擴展): `PyBullet`**
      * **用途**: 未來若要擴展動態模擬、碰撞檢測等功能。
      * **資源**: `PyBullet` 官方指南。

3.  **資料處理: `Pandas` 與 `NumPy`**
      * **用途**: `NumPy` 用於核心數學計算；`Pandas` 用於 Excel 匯入與資料處理。

4.  **GUI 框架: `PyQt6`**
      * **用途**: 應用程式的 UI 框架。

5.  **資料庫: `SQLite`**
      * **用途**: 用於內建的零件資料庫。

6.  **測試框架: `Pytest`**
      * **用途**: 用於單元測試。

7.  **文件生成: `ReportLab`**
      * **用途**: 用於生成 PDF 報告 (註：`v1.12` 文件曾誤植為 FPDF，`report_generator.py` 實際使用的是 `reportlab`)。