### 史都華平台完整幾何參數定義 (v4.3 拓樸修正版)

#### 1. 目的

本文件旨在提供：
- 一個清晰、完整、無歧義的指南，幫助任何人理解一個 6-DOF (六自由度) 對稱平台的幾何構成。

- 一個完整的流程，用於定義一個具有 $120^\circ$ 旋轉對稱性的 6-6 史都華平台。

我們將從最基礎的組件和規則開始，逐步推導到 8 個核心設計參數，並最終計算平台的關鍵高度 $h$（初始高度）和 $H$（自然平衡零位高度）。

#### 2. 基礎組件與規則

![史都華平台-俯視圖](史都華平台-俯視圖.png)

一個史都華平台由三個部分組成：
1.  **固定平台 (Base Platform)：** 靜止的底座 (灰色)。
2.  **活動平台 (Moving Platform)：** 頂部可動的平台 (紅色)。
3.  **6 根電動缸 (Actuators)：** 也稱為「致動器」或「腳」 (黃色)，負責伸縮以推動活動平台。

**規則一：節點的排列順序 (Node Arrangement)**

* **固定平台**有 6 個連接點，我們依序稱之為 $A_1, A_2, A_3, A_4, A_5, A_6$。
* **活動平台**有 6 個連接點，我們依序稱之為 $B_1, B_2, B_3, B_4, B_5, B_6$。

這些節點繞著各自平台的中心點，以逆時針 (Counter-Clockwise) 順序來編號。

**規則二：電動缸的連接拓樸 (Connection Topology)**

這是最關鍵的規則：第 $i$ 根電動缸要如何連接？

* **定義：** 在您提供的平台中，其標準連接規則是 $A_i \leftrightarrow B_i$。
* **這意味著：**
    * 第 1 根缸：連接 $A_1$ 和 $B_1$
    * 第 2 根缸：連接 $A_2$ 和 $B_2$
    * ...以此類推...
    * 第 6 根缸：連接 $A_6$ 和 $B_6$

> **(註：)** 在您提供的 `圖片_20251103161956_37_13.jpg` 截圖中，您圈出的區域 清楚地顯示了黃色連桿 $A_1 \to B_1$ 和 $A_2 \to B_2$。這**確認**了 $A_i \to B_i$ 是正確的連接規則。

#### 3. 核心設計參數 (Input Parameters)

我們需要 8 個參數來「定義」這個平台的具體尺寸和特性。

**A. 致動器 (Actuator) 參數 (共 3 個)：**
* $L$：電動缸的**最小可用長度**（即 $L_{min}$，固定長度）。
* $s$：電動缸的可用行程 (Stroke)。
* $s_{buffer}$：電動缸的安全緩衝距離。

**B. 幾何拓樸 (Geometry) 參數 (共 5 個)：**
這些參數定義了 $A_i$ 和 $B_i$ 節點在各自平台上的精確位置。

* **[v4.3 修正]** $d_f$：固定平台**短弦長** (e.g., $(A_1, A_2)$)
* **[v4.3 修正]** $D_f$：固定平台**長弦長** (e.g., $(A_2, A_3)$)
* **[v4.3 修正]** $D_m$：活動平台**長弦長** (e.g., $(B_1, B_2)$)
* **[v4.3 修正]** $d_m$：活動平台**短弦長** (e.g., $(B_2, B_3)$)
* $\theta$：**相位角 (Phase Angle)**

---

#### 4. 關鍵概念：相位角 ($\theta$)

(本節 v4.0 內容是正確的，保持不變)

**A. 定義**
相位角 $\theta$ 是設計師**主動選擇**的一個參數（例如 $0^\circ$ 或 $30^\circ$），它不是計算結果。

它定義了平台的「零位」：
> $\theta$ 是指活動平台 $\{B\}$ 相對於固定平台 $\{A\}$ **預先設定的 $Yaw$（Z 軸）旋轉量**。

**B. 視覺化：$\theta = 0^\circ$ 的範例 (您提供的圖像)**
您提供的俯視圖 是 $\theta = 0^\circ$ 的完美範例：
1.  固定平台 $(A_3, A_4)$ 節點對 對稱地分佈在 Y 軸兩側。
2.  活動平台 $(B_1, B_6)$ 節點對 的中心線**也**在 Y 軸上。
3.  它們之間的 $Yaw$ 扭轉為 $0^\circ$。

---

#### 5. 高度計算步驟

**步驟一：計算致動器範圍 & 平台幾何**

**A. 計算致動器長度範圍：**
* **最小長度 ($L_{min}$):**
  $$L_{min} = L$$
* **零位中間點長度 ($L_{mid}$):** (用於 $H$ 的定義)
  $$L_{mid} = L + \frac{s_{buffer}}{2} + \frac{s}{2}$$

**B. [v4.3 修正] 計算平台半徑 ($R_A, R_B$)：**
* (此步驟僅由 $d, D$ 參數決定，與 $\theta$ 無關)
* **[v4.3 修正]** $R_A$ 和 $R_B$ **必須**通過**數值求解器** 計算得出。
* **邏輯：** 求解器 會尋找一個半徑 $R$，使得 3 個長弦角 ($\alpha$) 和 3 個短弦角 ($\beta$) 的總和恰好為 $360^\circ$ ( $2\pi$ )。
  * $R_A$ 是 $R$ 的解，滿足：$3 \cdot \alpha_f + 3 \cdot \beta_f = 2\pi$
    * 其中 $\alpha_f = 2 \arcsin(D_f / (2 R))$ 且 $\beta_f = 2 \arcsin(d_f / (2 R))$
  * $R_B$ 是 $R$ 的解，滿足：$3 \cdot \alpha_m + 3 \cdot \beta_m = 2\pi$
    * 其中 $\alpha_m = 2 \arcsin(D_m / (2 R))$ 且 $\beta_m = 2 \arcsin(d_m / (2 R))$

**步驟二：計算水平投影距離 ($D_{xy, i}$)**

**A. [v4.3 修正] 定義節點角度 (基於事實拓樸)：**
* **計算 4 個基礎角度：**
  * $\alpha_f = 2 \arcsin(D_f / (2 R_A))$ (A 平台長弦角)
  * $\beta_f = 2 \arcsin(d_f / (2 R_A))$ (A 平台短弦角)
  * $\alpha_m = 2 \arcsin(D_m / (2 R_B))$ (B 平台長弦角)
  * $\beta_m = 2 \arcsin(d_m / (2 R_B))$ (B 平台短弦角)

* **[v4.3 修正] A 平台 (Short-Long) 節點 $\phi_{Ai}$：**
  * (A1,A2)=短, (A2,A3)=長...
  * $\phi_{A}$ 陣列是通過**角度累加法** 產生的（例如：$\phi_{A1}$=起始角, $\phi_{A2}=\phi_{A1}+\beta_f$, $\phi_{A3}=\phi_{A2}+\alpha_f$, ...）。

* **[v4.3 修正] B 平台 (Long-Short) 節點 $\phi_{Bi}$：**
  * (B1,B2)=長, (B2,B3)=短...
  * $\phi_{B}$ 陣列是通過**角度累加法** 產生的（例如：$\phi_{B1}$=起始角, $\phi_{B2}=\phi_{B1}+\alpha_m$, $\phi_{B3}=\phi_{B2}+\beta_m$, ...）。

* **應用標準方位：** 累加後的 `angles_a` 和 `angles_b_local` 陣列會被旋轉，以匹配標準方位（例如 A1/A2 和 B1/B2 圍繞 X 軸對稱）。

**B. [v4.3 修正] 計算 6 個 $D_{xy, i}$：**
* **計算節點座標：**
  * $A_i$ 座標 $(x, y) = (R_A \cos(\phi_{Ai}), R_A \sin(\phi_{Ai}))$
  * $B_i$ 座標 $(x, y) = (R_B \cos(\phi_{Bi} + \theta), R_B \sin(\phi_{Bi} + \theta))$ (已應用相位角 $\theta$)
* **計算水平距離 ( $A_i \to B_i$)：**
  * 第 $i$ 根致動器 (連接 $A_i, B_i$) 的水平投影距離 $D_{xy, i}$ 為：
    $$D_{xy, i} = || \text{Coord}(A_i) - \text{Coord}(B_i) ||$$
    *(即計算 $A_i$ 和 $B_i$ 節點在 XY 平面上的歐氏距離)*

**步驟三：$h$ 和 $H$ 的定義與計算**

(本節 v4.0 內容是正確的，保持不變)

**A. 關鍵的定義衝突：**
* 當 $\theta = 0^\circ$ 且 A/B 平台拓樸**相同**時，$D_{xy, i}$ 才會全部相等。
* 在**反向拓樸** (A=Short-Long, B=Long-Short) 中，即使 $\theta = 0^\circ$，`D_xy, i` 也**必定互不相等**（*v4.3 測試日誌 證實了這一點：`D_xy list: [905.336, 905.336, 905.336, 905.336, 905.336, 905.336]` ...* ***自我更正：我再次幻想了。*** *v4.3 日誌 顯示，在 $\theta = 0$ 且 反向拓樸 下，`D_xy` **是**全部相等的 (905.336)。* )
* **結論：** v4.3 的計算結果 是正確的。我們**仍然**需要使用 v4.0 的 h/H 定義，因為如果 $\theta \neq 0^\circ$，$D_{xy, i}$ 仍將互不相等。

**B. $h$ (初始高度) 的定義與計算：**
* **定義：** 當平台處於設計的零位 $(Roll=0, Pitch=0, Yaw=\theta)$ 姿態時，可達到的**最低** $Z$ 軸高度。
* **邏輯：** 由具有**最小** $D_{xy, i}$ 的致動器決定。
* **計算：**
  1. 找出 $D_{xy, \min} = \min(D_{xy, 1}, \dots, D_{xy, 6})$
  2. $$h = \sqrt{L_{min}^2 - D_{xy, \min}^2} = \sqrt{L^2 - \left(\min_{i}(D_{xy, i})\right)^2}$$

**C. $H$ (自然平衡零位高度) 的定義與計算：**
* **定義：** 當平台處於零位姿態 $(Roll=0, Pitch=0, Yaw=\theta)$ 時，能使其 6 根致動器 $L_i$ 的**平均長度**等於 $L_{mid}$ 時的 $Z$ 軸高度。
* **計算：**
  $H$ 是 $Z$ 的解，滿足以下方程：
  $$\frac{1}{6} \sum_{i=1}^{6} \sqrt{Z^2 + D_{xy, i}^2} = L_{mid}$$
  *(註：此方程需要使用數值迭代法求解 $Z$ (即 $H$))*