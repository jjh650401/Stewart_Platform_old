您提出的這個顧慮非常重要，這正是軟體重構（Refactoring）中最關鍵的風險點——如何確保修改的**完整性**與**一致性**。直接修改一個檔案的變數，極有可能導致依賴它的其他檔案在您未察覺的情況下出錯。

為了絕對避免這種錯誤，我建議採用一個系統性的、分為三步驟的**「安全重構方案」**。

---
### **安全重構方案**

#### **第一步：全面分析與定義「變數對應表」**

在修改任何一行程式碼之前，我們必須先完成分析工作，並建立一份明確的「變數對應表」。這份表將是我們後續所有修改的唯一依據。

**受影響的程式分析：**
經過分析，這次的變數名稱重構，將主要影響以下兩個直接相關的檔案：
1.  `src/gui/controls/drive_system_widget.py`：負責**產生**參數字典，並將其傳遞給後端。
2.  `src/core/drive_system_engine.py`：負責**讀取**和使用這些參數進行計算。

**變數對應總表：**
| 舊有變數名稱 (在 `params` 字典中) | v2.0 指南標準符號 | 影響範圍 |
| :--- | :--- | :--- |
| `moving_part_mass` | `m` | `drive_system_widget.py`, `drive_system_engine.py` |
| `max_acceleration` | `a_max` | `drive_system_widget.py`, `drive_system_engine.py` |
| `friction_coeff` | `μ` (在程式中可用 `mu`) | `drive_system_widget.py`, `drive_system_engine.py` |
| `install_angle` | `θ` (在程式中可用 `theta`) | `drive_system_widget.py`, `drive_system_engine.py` |
| `max_velocity` | `v_max` | `drive_system_widget.py`, `drive_system_engine.py` |
| `required_lead_mm` | `P_h_req` | `drive_system_widget.py`, `drive_system_engine.py` |
| `hours_per_day` | `R_h` | `drive_system_widget.py`, `drive_system_engine.py` |
| `days_per_year` | `R_d` | `drive_system_widget.py`, `drive_system_engine.py` |
| `expected_years` | `R_y` | `drive_system_widget.py`, `drive_system_engine.py` |
| `duty_cycle` | `Aval` | `drive_system_widget.py`, `drive_system_engine.py` |
| `ratio` | `i` | `drive_system_widget.py`, `drive_system_engine.py` |
| `screw_efficiency` | `η_screw` (在程式中可用 `eta_screw`) | `drive_system_widget.py`, `drive_system_engine.py` |
| `transmission_efficiency` | `η_pulley` (在程式中可用 `eta_pulley`) | `drive_system_widget.py`, `drive_system_engine.py` |

#### **第二步：採用「由內而外」的修改順序**

為了確保程式在修改過程中，處於「可預期失敗」的狀態，我們應採用由內而外（從後端到前端）的順序進行修改。

1.  **修改 `drive_system_engine.py` (後端)**：
    * 我會先修改計算引擎。我將把所有從 `params` 字典中讀取舊變數名稱的地方，全部替換為讀取上表中的「v2.0 指南標準符號」。
    * **此時，如果您運行程式，它將會因為前端傳來的參數名稱不符而崩潰，這是正常的、預期內的行為。**

2.  **修改 `drive_system_widget.py` (前端)**：
    * 在後端修改完畢後，我會接著修改前端UI。我將把所有向 `params` 字典中寫入舊變數名稱的地方，全部替換為寫入新的標準符號。
    * **完成這一步後，前後端的接口將重新對齊，程式功能應恢復正常。**

#### **第三步：遵守我們的程式碼交付流程**

我們將嚴格遵守「先邏輯，後實作」的流程來執行上述每一步的修改：
1.  我會先針對 `drive_system_engine.py`，提出一份詳細的「描述性邏輯」修改方案。
2.  在您同意後，您提供該檔案的原始碼，我回傳修改後的完整版本。
3.  接著，我們對 `drive_system_widget.py` 重複此流程。

---

這個三步驟方案透過**事前全面分析**、**預定修改順序**和**嚴格的交付流程**，可以最大限度地確保這次重構工作的準確性，避免任何遺漏或缺失。

請問您是否同意這份變數對應表，以及我們採用的這個三步驟安全重構方案？


您說的完全正確，我完全同意。

我先前將「變數對齊」視為一個可以稍後處理的獨立任務，這個判斷是錯誤的。您指出的「測試困難」問題，正是在不一致的程式碼庫上進行開發會遇到的最大障礙。一個分裂的、部分更新的系統是不可靠的，也會為我們未來的每一次修改埋下隱患。

非常感謝您強調了這項工作的重要性。我們必須現在就完成它。

---
### **修正後的計畫：啟動「全域變數對齊」任務**

好的，我明白了。我們現在將**「變數名稱 v2.0 全面對齊」**作為我們的最高優先級任務，並將其與「單位正常化」工作**同步進行**。

為了安全、無遺漏地完成這項工作，我建議採用以下擴展後的「安全重構方案」：

#### **第一步：建立「擴充版變數對應總表」**

除了驅動系統的變數，我們現在將核心動力學的變數也納入對齊範圍。

| 舊有變數名稱 (在 `params` 字典中) | v2.0 指南標準符號 | 主要影響檔案 |
| :--- | :--- | :--- |
| `platform_mass` | `m_p` | `kinematics.py`, `dynamics_engine.py`, `dynamics_widget.py` |
| `load_mass` | `m_l` | `kinematics.py`, `dynamics_engine.py`, `dynamics_widget.py` |
| `load_com_x` | `com_l_x` | `kinematics.py`, `dynamics_engine.py`, `dynamics_widget.py` |
| `load_com_y` | `com_l_y` | `kinematics.py`, `dynamics_engine.py`, `dynamics_widget.py` |
| `load_com_z` | `com_l_z` | `kinematics.py`, `dynamics_engine.py`, `dynamics_widget.py` |
| `lin_accel` | `a_lin` | `kinematics.py`, `dynamics_engine.py`, `dynamics_widget.py` |
| `ang_accel` | `a_ang` | `kinematics.py`, `dynamics_engine.py`, `dynamics_widget.py` |
| *(...以及所有先前定義的驅動系統變數...)* | | |

#### **第二步：確認完整影響範圍**

這次的重構將影響所有處理這些參數的檔案，包括：
* **核心層**：`kinematics.py`, `dynamics_engine.py`, `drive_system_engine.py`
* **UI 層**：`dynamics_widget.py`, `drive_system_widget.py`

#### **第三步：採用「由內而外」的修改順序**

我們將按照依賴關係，從最底層的資料定義開始修改：
1.  **`kinematics.py`**：修改 `_initialize_parameters` 中 `dyn_keys` 的鍵值，使其符合 v2.0 標準。
2.  **`dynamics_engine.py`**：修改 `calculate_actuator_forces` 方法，使其從傳入的 `dynamics_params` 字典中讀取新的 v2.0 變數名稱。
3.  **`dynamics_widget.py`**：修改此 UI 檔案，讓它在打包傳送給後端的參數時，使用新的 v2.0 變數名稱。

---
