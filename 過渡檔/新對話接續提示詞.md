以下是 Stewart Platform 專案的完整上下文，請根據此信息無縫接續工作：

- **專案概述**：我正在開發一個模組化的 Stewart Platform 設計與模擬工具，基於 v1.21 穩定基線進行開發，目前進入 v1.22 穩定基線的程式修改與測試階段。
- **已提供資料與完成內容**：
  1. **應用程式開發指南**：模組化史都華平台設計與模擬工具-1.21_彙整版.md
     - 包含專案架構、模組化設計原則及 v1.21 版本的詳細指南。
  2. **v1.21專案工作總結與新對話交接清單.md**
     - 總結 v1.21 版本的完成工作，並提供新對話交接的清單。
  3. **基礎背景與規則.md**
     - 定義開發規則，例如使用繁體中文、一次處理一個檔案、保留原有註釋等。
  4. **v1.22 穩定基線完整修改計畫.md**
     - 由您（Grok）規劃的 v1.22 版本修改計畫，涵蓋所有檔案的更新與整合。
- **已確認檔案與最新版本**：
  - `database_management_widget.py` (artifact_id: faaf1733-020c-46ac-a2a5-8103e10003db)
  - `drive_system_engine.py` (artifact_id: 31a352c0-5f97-48cc-84d4-4cadd7810640)
  - `drive_system_widget.py` (artifact_id: de12c5b2-474e-4bb9-bd95-6bf5a4082908)
  - `config.py` (artifact_id: e7b4a3f1-9e6c-4b7a-9f8c-5f7b2e3a4d9c)
  - `main.py` (artifact_id: a8f3b2c1-4e5d-4c7b-9a1e-3c9f8e7a4b2c)
- **當前任務**：程式修改後進入測試階段，發生錯誤需修正。具體問題為 Excel 匯入功能失敗，錯誤訊息為 `ValueError: Sheet 'Sheet1' does not exist`，因為 `database_manager.py` 在執行 `pd.read_excel(file_path, sheet_name='Sheet1')` 時，Excel 檔案的工作表名稱非 `Sheet1`。
- **已採取行動**：
  - 更新 `requirements.txt`，新增 `pandas` 和 `openpyxl`，內容為：
numpy
scipy
PyQt6
pybullet  # 我們暫時保留它，以備後用
reportlab
pyvista   # 新增
pyvistaqt # 新增
pandas
openpyxl

- 已安裝 `openpyxl` 和 `pandas`，但匯入錯誤仍存在。
- **已選擇方案**：方案 2（允許使用者選擇工作表），計劃修改 `database_manager.py` 和 `database_management_widget.py`，以動態獲取並讓使用者選擇 Excel 工作表。
- **最新進度**：
- 上次回覆中，您錯誤提供了兩個檔案的修改（`database_manager.py` 和 `database_management_widget.py`），並出現記憶混淆，與最新版本不符。
- 目前記憶已混亂，需在新對話中重置並接續。
- **下一個步驟**：請從修改 `database_manager.py` 開始，實現方案 2。修改後的程式碼應：
- 接受 `sheet_name` 參數，預設為 `'Sheet1'`。
- 使用 `pd.ExcelFile(file_path).sheet_names` 獲取所有工作表名稱。
- 若 `sheet_name` 無效，拋出錯誤並列出可用工作表。
- 確保與 `database_management_widget.py` 配合，後續再處理 GUI 部分。
- **檔案路徑**：`D:\Python_Programs\Stewart_Platform\src\core\database_manager.py`
- **注意事項**：
- 一次只處理一個檔案，等待我的確認後再處理下一個。
- 保留原有註釋與結構，僅添加必要修改。
- 提供修改後的程式碼並分配新 `artifact_version_id`。
- 基於 v1.21 穩定基線進行修改，符合「應用程式開發指南」和「基礎背景與規則」。
- **確認需求**：修改完成後，請等待我確認是否正確，然後繼續 `database_management_widget.py` 的修改。
- **環境信息**：
- 目前日期與時間：2025 年 9 月 16 日下午 3:43 JST。
- Python 版本：3.12.3。
- 作業系統：假設 Windows（基於檔案路徑 `D:\`）。
- **其他信息**：
- Excel 檔案包括 `螺桿資料.xlsx`、`伺服馬達資料.xlsx` 和 `皮帶輪資料.xlsx`，但工作表名稱未知。
- 請根據最新版本的 `database_manager.py` 進行修改，若需要最新內容，請要求我提供。

請根據以上上下文，從 `database_manager.py` 的修改開始，並確保與最新版本一致。謝謝！

