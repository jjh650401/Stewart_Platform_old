剛才三自由度平台的問題已解決。我又進行另一項測試，我發現：
1. 無論是三自由度平台或是六自由度平台，姿態展演與視覺化區塊中的三個勾選項，它們的預設值已在 config.py 中預設為 off，但是我在程式運行中把 "顯示關節角度" 和 "顯示節點座標" 二個項目勾選起來，但是在 3D 預覽中卻看不到這些。這些之前改到 config.py 中集中管理前是正常的，但現在卻不能顯示。
2. 我讀取了兩個六自由度平台的檔案，一個可以順利讀進來，另一個資料卻有異常，它們是：(1) 雖然可以讀進來卻給了我一個 "設計約束衝突" 的視窗，告訴我無法找到一個有效的初始姿態，此時看到它的 "啟用關節角度限制" 是被勾選的，(2) 我把此勾選項取消後儲存再去讀取時，這個勾選項仍然存在。
請分析為何會這樣，並提出計畫給我確認。