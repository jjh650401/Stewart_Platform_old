# **核心演算法邏輯說明書 (v2.4)**

**適用版本：** v2.4
**核心模組：** `src/core/kinematics.py`
**依賴庫：** `numpy`, `scipy.optimize`, `scipy.spatial.transform.Rotation`

---

### **1. 基礎系統定義 (System Fundamentals)**

在重建演算法前，必須確立以下全域標準：

* **單位系統 (Unit System)**：
    * **長度**：統一使用 **毫米 (mm)**。
    * **角度 (內部計算)**：統一使用 **弧度 (rad)**。
    * **角度 (UI 輸入/輸出)**：統一使用 **度 (deg)**。
* **座標系 (Coordinate System)**：
    * 採用 **右手座標系 (Right-Handed)**。
    * **原點 (Origin)**：位於固定平台 (Base) 的幾何中心/質心。
    * **軸向定義**：`+Z` 垂直向上，`+Y` 指向平台「後方」或特定頂點（視 3/6-DOF 定義），`+X` 指向右側。
* **姿態向量 (Pose Vector)**：
    * 定義為 6 維向量：$P = [x, y, z, \phi, \theta, \psi]$。
    * 旋轉順序：**Z-X-Y** (Yaw-Pitch-Roll) 歐拉角順序。

---

### **2. 6-DOF 幾何組構演算法 (Geometry Construction)**

此演算法負責根據輸入參數，計算出固定平台 ($A_i$) 與活動平台 ($B_i$) 的 6 個節點座標。

#### **A. 輸入參數**
* $D_f, d_f$：固定平台長弦、短弦長度。
* $D_m, d_m$：活動平台長弦、短弦長度。
* $\theta_{input}$ (`phase_angle_deg`)：使用者輸入的相位角 (deg)。

#### **B. 半徑求解 (Radius Calculation)**
由於平台由長短弦交錯構成，半徑 $R$ 不是直接輸入，而是方程式的解。
* **目標函式**：尋找 $R$，使得 3 個長弦角 ($\alpha$) 與 3 個短弦角 ($\beta$) 之和等於 $2\pi$。
    $$3 \cdot (2 \arcsin(\frac{D}{2R})) + 3 \cdot (2 \arcsin(\frac{d}{2R})) - 2\pi = 0$$
* **實作方法**：使用 `scipy.optimize.minimize` (SLSQP) 求解上述方程式，分別計算出 $R_a$ (固定平台) 與 $R_b$ (活動平台)。

#### **C. 節點生成 (Node Generation)**
* **拓樸結構**：
    * 固定平台 (Base)：採用 **(短-長)** 交錯。角度累加序列：$[0, \beta_f, \beta_f+\alpha_f, \dots]$。
    * 活動平台 (Mobile)：採用 **(長-短)** 交錯。角度累加序列：$[0, \alpha_m, \alpha_m+\beta_m, \dots]$。
* **相位角應用 (Crucial)**：
    * 活動平台節點的最終角度 $\psi_{B_i} = \text{Angle}_{raw} + \text{RotOffset} + \text{deg2rad}(\theta_{input})$。
    * **注意**：嚴禁計算所謂的「內建交錯角 (stagger)」，必須直接使用使用者的輸入 $\theta_{input}$ 作為 Yaw 的零位偏移。
* **座標轉換**：
    * $A_i = [R_a \cos(\psi_{A_i}), R_a \sin(\psi_{A_i}), 0]$
    * $B_i = [R_b \cos(\psi_{B_i}), R_b \sin(\psi_{B_i}), 0]$ (此為局部座標)

---

### **3. 3-DOF 幾何組構演算法**

此演算法基於「質心模型」生成等腰三角形佈局的 3 個節點。

#### **A. 輸入參數**
* $D_1, D_2$：固定平台底邊長、高。
* $d_1, d_2$：活動平台底邊長、高。
* **注意**：此模式**不使用**也不計算半徑 $R$。

#### **B. 節點生成 (Centroid Model)**
* **質心偏移計算**：三角形質心位於高的 $1/3$ 處。
* **固定平台節點 ($A_i$)**：
    * $A_1 (右下) = [-D_1/2, -D_2/3, 0]$
    * $A_3 (頂點) = [0, 2 D_2/3, 0]$
    * $A_5 (左下) = [D_1/2, -D_2/3, 0]$
* **活動平台節點 ($B_i$)**：
    * 同理，使用 $d_1, d_2$ 計算 $B_1, B_3, B_5$ 的局部座標。

---

### **4. 零位高度 (H) 求解演算法 (Forward Kinematics)**

此演算法用於尋找平台的「自然平衡高度」。

* **定義**：尋找一個高度 $Z$ (即 $H$)，使得在此姿態下，所有電動缸長度的**平均值**等於 $L_{mid}$。
    $$L_{mid} = L_{min} + s_{buffer}/2 + s_{stroke}/2$$
* **初始猜測**：
    * 位置：$[0, 0, L_{mid}]$ (3-DOF) 或 $[0, 0, L_{mid}, 0, 0, \theta_{input\_rad}]$ (6-DOF)。
* **目標函式**：
    $$Error = \sum (\|T \cdot B_i - A_i\|^2 - L_{mid}^2)^2$$
    其中 $T$ 是由 $Z$ (和 $\theta_{input}$) 構成的變換矩陣。
* **求解器**：使用 `scipy.optimize.least_squares` (LM 算法) 或 `minimize`。
* **關鍵規則**：在此步驟中，**不應用**任何物理約束（如腿長最大值、關節角度）。這是為了確保在任何幾何參數下都能算出一個數學上的參考零點。

---

### **5. 平台空間分析演算法 (Workspace Analysis)**

這是本專案最複雜的部分，用於計算各個自由度的極限範圍。

#### **A. 演算法核心**
* **方法**：針對每一個自由度（如 Pitch），分別執行兩次優化：一次找最小值 (`min`), 一次找最大值 (`max`)。
* **優化器**：`scipy.optimize.minimize`，使用 `SLSQP` 方法（因其支援邊界與不等式約束）。

#### **B. 變數正規化 (Variable Normalization)**
為了讓求解器在混合單位（mm 與 rad）下穩定收斂，必須使用**縮放向量 (Scale Vector)**：
* **位置變數 ($x, y, z$)**：縮放因子 = $R_a$ (若無則設 100.0)。理由是將 mm 級距 (約 500-1000) 縮小至 1.0 附近。
* **角度變數 ($\phi, \theta, \psi$)**：縮放因子 = **1.0**。理由是弧度本身 (約 0.5-1.0) 已在理想範圍，**不可**再被 $R_a$ 縮放。
* **運作流程**：
    1.  `scaled_x0 = real_x0 / scale_vector`
    2.  求解器在 `scaled` 空間運作。
    3.  `real_result = solver_output * scale_vector`

#### **C. 邊界設定 (Bounds)**
這是防止求解器發散的最後一道防線。
* **位置**：相對寬鬆，例如 $x_0 \pm (R_a + L + s)$。
* **角度 (關鍵)**：強制設定為 **$\pm 85^\circ$ (約 1.48 rad)**。嚴格禁止使用預設的 $\pm \pi$，以防止出現非物理的數學解 (如 $\pm 1000^\circ$)。

#### **D. 約束條件 (Constraints)**
* **腿長約束**：
    $$L_{min} \le \| T(P) \cdot B_i - A_i \| \le L_{max}$$
    * `Operational` 模式：$L_{max} = L + s_{buffer}/2 + s$
    * `Mechanical` 模式：$L_{max} = L + s + s_{buffer}$
* **關節角度約束 (可選)**：
    $$\text{LegVec} \cdot \text{NormalVec} \ge \cos(\theta_{limit})$$
    * 計算腿向量與平台法向量的夾角，確保不超過物理限制。

#### **E. 輸出處理**
* **6-DOF**：直接回傳絕對座標/角度。
* **3-DOF**：直接回傳絕對座標 (Z) 與角度。
* **UI 層責任**：由 UI 層 (`analysis_widget.py`) 負責將絕對高度 $Z$ 減去 $H$，轉換為使用者可讀的相對位移 (`Heave`)。

