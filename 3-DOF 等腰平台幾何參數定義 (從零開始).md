### 3-DOF 等腰平台幾何參數定義 (v4.6 質心修正版)

#### 1. 目的

本文件旨在提供一個清晰、完整的指南，用於定義一個 3-DOF（三自由度，通常用於 Z, Roll, Pitch）的並聯式運動平台 (Motion Base)。

本文件的基礎是**等腰三角形**，並使用您指定的節點標籤和連接規則。

**[v4.6 規格變更]**：本文件已更新。座標系原點 $(0,0,0)$ 已從「外接圓圓心」 修正為**「質心 (Centroid)」**，以確保中心點 始終位於三角形**內部**。

#### 2. 基礎組件與規則

**A. 組件**
1.  **固定平台 (Base Platform)：** 靜止的底座。
2.  **活動平台 (Moving Platform)：** 頂部可動的平台。
3.  **3 根電動缸 (Actuators)：** 連接上下平台。

**B. 節點標籤 (Node Labels) [v4.5 修正]**
* **固定平台**有 3 個連接點，我們依規格書 座標定義稱之為 $A_1, A_5, A_3$。
* **活動平台**有 3 個連接點，我們依規格書 座標定義稱之為 $B_1, B_5, B_3$。

**C. 電動缸連接拓樸 (Connection Topology) [v4.5 修正]**
* 第 1 根缸：連接 $A_1 \leftrightarrow B_1$
* 第 5 根缸：連接 $A_5 \leftrightarrow B_5$
* 第 3 根缸：連接 $A_3 \leftrightarrow B_3$

#### 3. 核心設計參數 (Input Parameters)

**A. 致動器 (Actuator) 參數 (共 3 個)：**
* $L$：電動缸的**最小可用長度**（即 $L_{min}$）。
* $s$：電動缸的可用行程 (Stroke)。
* $s_{buffer}$：電動缸的安全緩衝距離。

**B. 幾何拓樸 (Geometry) 參數 (共 4 個)：**
這些參數定義了 $A_i$ 和 $B_i$ 節點在各自平台上的精確位置。
* $D_1$：固定平台**底邊長度**（$A_1$ 到 $A_5$ 的距離）。
* $D_2$：固定平台**三角形高度**（從 $A_1-A_5$ 底邊中點到 $A_3$ 的垂直距離）。
* $d_1$：活動平台**底邊長度**（$B_1$ 到 $B_5$ 的距離）。
* $d_2$：活動平台**三角形高度**（從 $B_1-B_5$ 底邊中點到 $B_3$ 的垂直距離）。

#### 4. 座標系與節點位置定義 (v4.6 質心修正)

**A. [v4.6 修正] 固定平台 $\{A\}$ 座標系：**
* **原點 $P_A (0,0,0)$：** 位於 $A_1, A_5, A_3$ 三角形的**質心 (Centroid)**。
* **平面：** $Z=0$ 平面。
* **Y 軸：** 沿著三角形的高度，指向 $A_3$。
* **X 軸：** 沿著底邊，指向 $A_5$。

**B. [v4.6 修正] 固定平台節點 $A_i$ 的座標 (在 $\{A\}$ 中)：**
* **計算衍生參數：**
    * 質心 $Y$ 偏移（相對於底邊中點） $C_{yA} = D_2 / 3$
* **節點座標：**
    * $A_1 = [-D_1/2, -D_2/3, 0]$
    * $A_5 = [D_1/2, -D_2/3, 0]$
    * $A_3 = [0, 2D_2/3, 0]$

**C. [v4.6 修正] 活動平台 $\{B\}$ 座標系：**
* **原點 $P_B (0,0,0)$：** 位於 $B_1, B_5, B_3$ 三角形的**質心 (Centroid)**。
* **平面：** $Z=0$ 平面（在其自身座標系中）。
* **Y 軸：** 沿著三角形的高度，指向 $B_3$。
* **X 軸：** 沿著底邊，指向 $B_5$。

**D. [v4.6 修正] 活動平台節點 $B_i$ 的座標 (在 $\{B\}$ 中)：**
* **計算衍生參數：**
    * 質心 $Y$ 偏移（相對於底邊中點） $C_{yB} = d_2 / 3$
* **節點座標：**
    * $B_1 = [-d_1/2, -d_2/3, 0]$
    * $B_5 = [d_1/2, -d_2/3, 0]$
    * $B_3 = [0, 2d_2/3, 0]$

#### 5. 高度 $h$ 和 $H$ 的計算 (v4.6 質心修正)

**A. 零位姿態 (Neutral Pose) 定義：**
* 3-DOF 平台的「零位」姿態被定義為 $(Roll=0, Pitch=0, Yaw=0)$。

**B. 步驟一：計算致動器長度**
* **最小長度 ($L_{min}$):**
  $$L_{min} = L$$
* **零位中間點長度 ($L_{mid}$):**
  $$L_{mid} = L + \frac{s_{buffer}}{2} + \frac{s}{2}$$

**C. [v4.6 修正] 步驟二：計算水平投影距離 ($D_{xy, i}$)**
在零位姿態下， $A_i$ 和 $B_i$ 節點之間的水平 $XY$ 平面投影距離 $D_{xy, i}$ ( $i=1, 3, 5$ )。

* **$D_{xy, 1}$** ( $A_1$ 與 $B_1$ 之間)：
  $$D_{xy, 1}^2 = (B_{1x}-A_{1x})^2 + (B_{1y}-A_{1y})^2$$
  $$D_{xy, 1}^2 = (-d_1/2 - (-D_1/2))^2 + (-d_2/3 - (-D_2/3))^2$$
  $$D_{xy, 1} = \sqrt{\left(\frac{D_1 - d_1}{2}\right)^2 + \left(\frac{D_2 - d_2}{3}\right)^2}$$

* **$D_{xy, 5}$** ( $A_5$ 與 $B_5$ 之間)：
  $$D_{xy, 5}^2 = (B_{5x}-A_{5x})^2 + (B_{5y}-A_{5y})^2$$
  $$D_{xy, 5}^2 = (d_1/2 - D_1/2)^2 + (-d_2/3 - (-D_2/3))^2$$
  $$D_{xy, 5} = \sqrt{\left(\frac{D_1 - d_1}{2}\right)^2 + \left(\frac{D_2 - d_2}{3}\right)^2}$$

* **$D_{xy, 3}$** ( $A_3$ 與 $B_3$ 之間)：
  $$D_{xy, 3}^2 = (B_{3x}-A_{3x})^2 + (B_{3y}-A_{3y})^2$$
  $$D_{xy, 3}^2 = (0 - 0)^2 + (2d_2/3 - 2D_2/3)^2$$
  $$D_{xy, 3} = \left|\frac{2(d_2 - D_2)}{3}\right|$$

**D. 步驟三：計算 $h$ 和 $H$**
* (v4.0 邏輯 - 未修改)

* **$h$ (初始高度)：**
  由最先達到 $L_{min}$ 的致動器決定，即 $D_{xy, i}$ 最小的那根。
  $$D_{xy, \min} = \min(D_{xy, 1}, D_{xy, 3})$$ (因為 $D_{xy, 1} = D_{xy, 5}$)
  $$h = \sqrt{L_{min}^2 - D_{xy, \min}^2} = \sqrt{L^2 - \left(\min(D_{xy, 1}, D_{xy, 3})\right)^2}$$

* **$H$ (自然平衡零位高度)：**
  定義為 3 根致動器 $L_i$ 的**平均長度**等於 $L_{mid}$ 時的 $Z$ 軸高度。
  $$H = Z$$，其中 $Z$ 滿足：
  $$\frac{1}{3} \left( \sqrt{Z^2 + D_{xy, 1}^2} + \sqrt{Z^2 + D_{xy, 3}^2} + \sqrt{Z^2 + D_{xy, 5}^2} \right) = L_{mid}$$
  *(代入 $D_{xy, 1} = D_{xy, 5}$)*
  $$\frac{1}{3} \left( 2\sqrt{Z^2 + D_{xy, 1}^2} + \sqrt{Z^2 + D_{xy, 3}^2} \right) = L_{mid}$$
  *(註：此方程通常需要使用數值迭代法求解 $Z$ (即 $H$)。)*