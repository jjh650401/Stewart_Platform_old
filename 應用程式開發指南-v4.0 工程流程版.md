**《應用程式開發指南 v4.0 (工程流程版)》**

**版本：4.0 (工程流程版) - v4.6 規格對齊**
**日期：2025年11月5日**

---

### **第一部分：工程設計推演 (The Workflow)**

**\[重要提示] 本部分是 v4.0 指南的核心，定義了從專案需求到零件選型的完整工程設計流程。軟體開發的目標是實現此流程中定義的五個階段。**

---

#### **階段一：專案需求定義 (Project Requirements)**

**目標：** 確立設計的頂層約束與目標。這是所有後續計算的「輸入源」。

* **1.1 負載參數 (Payload)**：
    * 定義活動平台自身的質量 (`platform_mass` [kg])。
    * 定義附加於活動平台的負載質量 (`load_mass` [kg])。
    * 定義負載質心(COM)相對於活動平台幾何中心的偏移量 (`load_com_x/y/z` [mm])。

* **1.2 運動需求 (Motion Profile)**：
    * **範圍 (Workspace)**：定義平台在六個自由度 (Tx, Ty, Tz, Roll, Pitch, Yaw) 上所需的**最小運動範圍**。
    * **速度 (Velocity)**：定義平台在各自由度上所需的**最大速度**。
    * **加速度 (Acceleration)**：定義平台在各自由度上所需的**最大加速度** (`lin_accel` [mm/s²], `ang_accel` [rad/s²])。這是動力學分析的關鍵輸入。

* **1.3 壽命需求 (Lifetime)**：
    * 定義預期的使用年限 (`R_y`)、每年天數 (`R_d`)、每天小時數 (`R_h`)。
    * 定義預期的稼動率 (`Aval` [%])。
    * *(這些參數將共同導出「需求壽命 $L_T$」)*。

---

#### **階段二：平台選型與幾何設計 (Geometry & Kinematics)**

**目標：** 根據「階段一」的需求，設計平台的「幾何骨架」，並進行第一次運動學驗證。

* **2.1 平台選型**：
    * 根據「階段 1.2」的運動範圍需求（例如：是否需要 Yaw 運動），決定採用 **3-DOF** 或 **6-DOF** 平台。

* **2.2 幾何參數定義 (核心輸入)**：
    * **致動器 (Actuator)**：定義 `L` (最小機械長度)、`s` (可用行程)、`s_buffer` (安全裕量總和)。
    * **6-DOF 平台 (依據 `6-DOF...md` v4.3 文件)**：
        * 固定平台：`d_f` (短弦長), `D_f` (長弦長)。
        * 活動平台：`D_m` (長弦長), `d_m` (短弦長)。
        * **相位角**：`$\theta$` (Phase Angle)。
    * **3-DOF 平台 (依據 `3-DOF...md` v4.6 文件)**：
        * 固定平台：`D_1` (底邊長度), `D_2` (垂直高度)。
        * 活動平台：`d_1` (底邊長度), `d_2` (垂直高度)。

* **2.3 運動學分析與驗證 (第一關卡)**：
    * **衍生計算**：系統必須自動計算所有衍生幾何參數。
        * **[v4.3 修正]** 6-DOF：$R_A, \alpha_f, \beta_f, R_B, \alpha_m, \beta_m$ (使用**數值求解器**)。
        * **[v4.6 修正]** 3-DOF：$A_i, B_i$ 座標 (使用**質心**模型)。
        * 通用：$D_{xy, i}$ (水平投影距離)、 $h$ (初始高度)、 $H$ (自然平衡零位高度)。
    * **工作空間分析**：系統執行「工作空間邊界分析」。
    * **【驗證點 1】**：**比較**「計算出的可用工作空間」是否**大於或等於**「階段 1.2」中定義的「範圍 (Workspace)」需求？
    * **【驗證點 2】**：系統執行「節點擺角範圍分析」。**檢查**所有關節的最大擺角是否在合理的物理限制內（例如 < 45°）？
    * **迭代**：如果任一驗證點失敗，使用者**必須**退回「階段 2.2」，調整幾何參數（弦長、相位角）或致動器參數（L, s），然後重新執行本階段分析，直到通過驗證。

---

#### **階段三：動力學分析 (Dynamics Analysis)**

**目標：** 平台幾何確認可行後，計算其在運動時所產生的「最大致動器出力 ($F_{max}$)」。

* **3.1 動力學參數定義**：
    * **轉動慣量 ($I$)**：使用者**必須**輸入平台與負載的「轉動慣量張量 ($I$)」([kg·mm²])。
    * **動態放大係數 ($K_d$)**：
        * **定義**：使用者輸入一個「動態放大係數 ($K_d$)」（預設值 1.0，建議值 1.2-1.5）。
        * **目的**：此係數作為一個工程安全係數，用於**補償**在逆動力學模型中被簡化（忽略）的 $\boldsymbol{\omega} \times (\mathbf{I}\boldsymbol{\omega})$ 項（科氏力/向心力矩）。

* **3.2 全域最大出力分析 (第二關卡)**：
    * **輸入**：
        1.  「階段 1.1」的負載參數 (`mass`, `COM`)。
        2.  「階段 1.2」的運動需求 (最大 `lin_accel`, `ang_accel`)。
        3.  「階段 3.1」的動力學參數 (`I`, `$K_d$`)。
    * **執行**：系統**必須**執行「動態模式」的全域最大出力分析。
    * **計算模型**：逆動力學模型在計算總外力/力矩 $\mathbf{W}_{ext}$ 時，**必須**使用以下修正後公式：
        * $\mathbf{W}_{ext} = \mathbf{W}_{gravity} + \mathbf{K_d} \cdot \mathbf{W}_{inertia}$
        * *(註：$\mathbf{W}_{inertia} = [m\mathbf{a} \quad \mathbf{I}\boldsymbol{\alpha}]$)*。
    * **【輸出】**：得到一個已被 $K_d$ 放大、相對可靠的**「$F_{max}$ (峰值致動器出力)」**。此 $F_{max}$ 是下一階段零件選型的**唯一依據**。
    * **【安全警告】**：如果使用者嘗試跳過此步驟（例如 $F_{max}=0$ 或 $K_d=0$），系統在「階段四」中**必須**彈出警告，強制使用者返回本階段完成動態分析。

---

#### **階段四：驅動系統設計 (Drive System Design)**

**目標：** 根據 $F_{max}$（力）和 $L_T$（壽命），從資料庫中篩選出合格的致動器關鍵零件。

* **4.1 載入負載與定義係數**：
    * **自動模式**：系統**必須**自動載入「階段 3.2」產生的 $F_{max}$。
    * **手動模式**：明確此模式僅用於獨立的、非平台的直線致動器估算。
    * **負荷係數 ($f_w$)**：
        * **定義**：使用者輸入「負荷係數 ($f_w$)」（例如 1.2-1.5）。
        * **目的**：此係數**必須**用於補償所有在「階段三」動力學模型中未計算的**摩擦力**（如關節、導軌、螺桿自身摩擦等），以及衝擊、震動等環境因素。

* **4.2 零件篩選與驗證 (第三關卡)**：
    * **計算需求 (Input)**：
        1.  計算 $F_{eq}$ (平均等效負載)，此計算**必須**基於 $F_{max}$。
        2.  載入「階段 1.3」的壽命需求，計算 $L_T$ (需求壽命)。
        3.  載入「階段 4.1」的負荷係數 $f_w$。
    * **計算門檻 (Thresholds)**：
        1.  **需求動態負荷 ($C_{ac}$)**：系統計算 $C_{ac} = F_{eq} \cdot f_w \cdot ( \dots )^{1/3}$。
        2.  **需求馬達扭矩 ($T_{motor\_req}$)**：系統計算 $T_{motor\_req} = \frac{F_{max} \cdot P_h}{\dots}$。
    * **【驗證點 1：篩選】**：系統查詢資料庫，篩選出**所有**滿足以下條件的零件組合：
        * 螺桿 `$C_a$` (基本額定動負荷) $\ge$ `$C_{ac}$` (需求動態負荷)。
        * 馬達扭矩 $\ge$ `$T_{motor\_req}$` (需求馬達扭矩)。
    * **【驗證點 2：最終校核】**：系統對「驗證點 1」篩出的每個組合，執行最終壽命校核：
        * 計算 $L_t$ (預估壽命) = $\left(\frac{C_a}{F_{eq} \cdot f_w}\right)^3 \cdot \frac{10^6}{\dots}$。
        * **僅保留** $L_t \ge L_T$ 的組合。
* **【輸出】**：一份包含螺桿、馬達、皮帶輪的**最終合格零件清單**。

---

#### **階段五：總結報告 (Final Report)**

**目標：** 彙總設計流程，固化設計結果。

* **5.1 生成報告**：
    * 系統提供「一鍵生成 PDF 設計報告」功能。
    * 報告內容**必須**彙總「階段一」到「階段四」的所有關鍵輸入參數、驗證結果與最終零件清單。

* **5.2 專案存檔**：
    * 系統提供「儲存專案」功能，將此次完整的設計流程（包含所有階段的輸入與輸出）保存為 `.json` 文件，以便未來載入和修改。

遵照您的指示。

我們將執行 v4.6 計畫的**任務 B**：同步更新 `應用程式開發指南-v4.0 工程流程版.md` 檔案，使其\*\*「第二部分：軟體實現規格」\*\*的 3-DOF 邏輯，與我們剛剛在「任務 A」中建立的 v4.6「質心」規格 完全一致。

**[最高優先級規則 0]：不再幻想。**

我已將 v4.0 指南 的「第二部分」 進行了以下**根本性修正**：

1.  **【v4.3 修正 - 第六、七章 6-DOF】**：
      * 我**刪除**了**錯誤的 `arccot` 代數公式**。
      * 我將其**替換**為 v4.3 中**已驗證為真**的\*\*「數值求解器」\*\* (`3 \alpha + 3 \beta = 2 \pi`) 的邏輯描述。
      * 我**修正**了弦長的描述，以**正確**反映 A 平台 (Short-Long) 和 B 平台 (Long-Short) 的事實。
      * 我**刪除**了**錯誤的 `\phi_A = [\alpha_A, -\alpha_A, ...]` 陣列定義**。
      * 我將其**替換**為 v4.3 中**已驗證為真**的\*\*「角度累加法」\*\* 邏輯（A=Short-Long, B=Long-Short）。
      * 我**刪除**了**錯誤的 `cos` 公式**，並**替換**為 v4.3 中**已驗證為真**的\*\*「直接距離計算」\*\* ($D_{xy, i} = || A_i - B_i ||$) 的邏輯，並註明連桿連接為 $A_i \to B_i$。
2.  **【v4.6 修正 - 第六、七章 3-DOF】**：
      * 我**刪除**了**所有**基於「底邊中點」 的 3-DOF 規格。
      * 我將其**替換**為我們剛剛在 v4.6 `3-DOF...md` 檔案中**全新定義**的\*\*「質心 (Centroid)」\*\* 數學模型（包括新的座標定義和新的 $D_{xy}$ 公式）。

---

### **第二部分：軟體實現規格 (The Specification)**

**\[重要提示] 本部分是 v4.0 指南的技術附錄，繼承自 v3.0 規格書。它提供了實現「第一部分：工程設計推演」流程所需的詳細軟體架構、數學模型、變數定義與技術棧。**

**\[v4.6 規格更新] 本附錄中的第六章和第七章已被更新，以匹配 `kinematics_engine.py` (v4.3) 中已驗證的 6-DOF「數值求解器」和「反向拓樸角度累加」 邏輯，以及 v4.6 中**全新定義**的 3-DOF「質心」 模型。**

---

### **第一章：目標**

**\[重要提示] 本章定義了本應用程式的開發目標與服務對象。**

**目標：** 為遊樂設備行業的技術人員，打造一款基於 Python 的視窗版桌面應用程式，用於史都華平台 (Stewart Platform)（六自由度與三自由度）的參數化設計、性能分析、動態模擬與驗證，並整合獨立的電動缸驅動系統設計功能。

---

### **第二章：專案目錄架構**

**\[重要提示] 本章描述專案的最終文件結構 (基於 v3.0 規格)，後續維護者在更新文件時，請參考最新程式碼結構同步更新，確保文件準確性。**

```
D:\PYTHON_PROGRAMS\STEWART_PLATFORM\
│
├── .venv/                      # Python 虛擬環境
├── data/                       # 數據文件
│   ├── projects/               # .json 專案文件儲存位置
│   └── components.db           # SQLite 零件資料庫
├── src/                        # 原始碼目錄
│   ├── core/                   # 核心後端模組
│   │   ├── __init__.py
│   │   ├── analysis_engine.py      # 高層分析引擎 (LHS+SQP)
│   │   ├── config.py               # 全局配置文件
│   │   ├── database_manager.py     # 資料庫管理器
│   │   ├── drive_system_engine.py  # 驅動系統計算引擎
│   │   ├── dynamics_engine.py      # 動力學計算引擎 (逆動力學)
│   │   ├── kinematics_engine.py    # 運動學計算引擎 (IK, FK, WS)
│   │   ├── report_generator.py     # PDF 報告生成器
│   │   ├── state_manager.py        # 全局狀態管理器
│   │   └── utils.py                # 通用工具函數
│   └── gui/                    # 圖形使用者介面模組
│       ├── __init__.py
│       ├── controls/               # 複雜 UI 控制項元件
│       │   ├── __init__.py
│       │   ├── analysis_widget.py          # 姿態控制與結果顯示面板
│       │   ├── database_management_widget.py # 資料庫管理對話框
│       │   ├── drive_system_widget.py      # 驅動系統儀表板
│       │   ├── dynamics_widget.py          # 動力學參數輸入與分析面板
│       │   ├── master_geometry_widget.py   # 幾何設計主容器
│       │   ├── report_widget.py            # 報告生成觸發介面
│       │   ├── six_dof_widget.py           # 六自由度設計面板
│       │   └── three_dof_widget.py         # 三自由度設計面板
│       ├── geometry_widget.py      # 幾何參數 UI 工廠
│       ├── main_window.py          # 主視窗 (UI 總管, 事件協調)
│       └── pyvista_widget.py       # 3D 可視化元件
├── tests/                      # 單元測試目錄
├── .gitignore                  # Git 忽略配置
├── main.py                     # 應用程式主入口點
└── requirements.txt            # Python 依賴列表
```

---

### **第三章：專案初始化與環境設定**

**\[重要提示] 本章包含專案運行的基礎環境要求，後續維護者若引入新依賴或變更 Python 版本，請務必同步更新本章內容。**

  * **環境需求**：

      * **Python 版本**：建議使用 Python 3.9 或更高版本。
      * **依賴安裝**：需使用 `requirements.txt` 文件安裝所有必要的 Python 函式庫。關鍵函式庫包括：
          * `PyQt6`：用於圖形使用者介面 (GUI) 開發。
          * `PyVista` 與 `PyVistaQT`：用於 3D 模型渲染與互動。
          * `NumPy` 與 `SciPy`：用於核心的數學計算與最佳化求解。
          * `Pandas`：用於處理 Excel 檔案的匯入與數據操作。
          * `SQLite`：Python 內建，用於本地零件資料庫。
          * `FPDF`：用於生成 PDF 設計報告。
          * `Pytest`：用於執行單元測試。
      * **安裝指令**：在設定好 Python 虛擬環境後，於專案根目錄執行 `pip install -r requirements.txt`。

  * **初始化步驟**：

      * **虛擬環境**：強烈建議使用 Python 虛擬環境 (如 `venv`) 來隔離專案依賴。
          * 建立指令: `python -m venv .venv`。
          * 啟動指令: `.\.venv\Scripts\activate`。
      * **專案啟動**：執行位於專案根目錄的 `main.py` 文件來啟動應用程式。
      * **首次運行**：
          * **資料庫**：首次運行時，`database_manager.py` 會自動在 `data/` 目錄下創建 `components.db` SQLite 資料庫檔案。使用者需透過介面匯入 Excel 零件數據。
          * **配置**：需檢查 `config.py` 中的字體路徑 (`REPORT_FONT_PATH_REGULAR`, `REPORT_FONT_PATH_BOLD`) 是否指向有效的 `.ttf` 檔案，以確保 PDF 報告能正確顯示中文。
          * **啟動面板**：可透過修改 `config.py` 中的 `STARTUP_SHOW_ANALYSIS` 旗標來決定啟動時預設顯示「設計」或「分析」面板。

  * **測試與部署**：

      * **測試**：專案包含 `tests/` 目錄，建議使用 `pytest` 框架運行單元測試。
      * **部署**：可使用 `PyInstaller` 或類似工具將 Python 程式碼打包成獨立的可執行檔，方便在未安裝 Python 環境的電腦上運行。打包時需注意包含所有依賴項及必要的數據文件 (如字體檔)。

---

### **第四章：核心理念與技術棧**

**\[重要提示] 本章闡述專案的設計哲學與技術選型，是理解架構決策的基礎，後續維護者應遵循這些理念。**

  * **核心理念**：

      * **模組化設計**：功能劃分清晰，強調職責分離 (UI / 狀態 / 分析 / 核心計算)，實現高內聚、低耦合，便於維護、測試與擴展。
      * **中央化狀態管理**：使用 `StateManager` 作為**唯一、權威的數據中心 (Single Source of Truth)**，集中管理全局狀態，避免狀態散亂。
      * **視覺化回饋**：提供即時的 3D 視覺化反饋 (PyVista)，讓設計調整與結果分析更直觀。
      * **中央化設定**：將所有可配置的參數（預設值、UI 字串、樣式、演算法參數等）**全面集中**到 `config.py`，實現設定與程式碼的分離，便於維護。
      * **物理準確性**：持續提升計算結果與工程現實的一致性。
      * **穩定性與健壯性**：優先解決崩潰、數值穩定性、UI 佈局、單位混亂等問題，並增強異常處理。
      * **引導式計算**：程式應作為設計夥伴，理解參數依賴，引導使用者補全數據，自動推算衍生參數。
      * **設計驗證**：內建驗證機制，檢查幾何矛盾與物理極限 (行程、角度)，確保設計合理性。
      * **使用者狀態追蹤**：追蹤使用者修改狀態 (`is_dirty`)，關閉前提示儲存。
      * **驅動系統獨立性**：將驅動系統設計提升為獨立模組，增加手動模式與獨立存檔。
      * **資料處理能力**：透過 Excel 匯入與零件篩選優化，提升處理大量零件數據的效率。

  * **技術棧**：

      * **程式語言**: Python (建議 3.9+)。
      * **數值/科學計算**: NumPy, SciPy。
      * **GUI 框架**: PyQt6。
      * **3D 視覺化**: PyVista, PyVistaQT。
      * **資料處理**: Pandas。
      * **資料庫**: SQLite (Python 內建)。
      * **報告生成**: FPDF。
      * **測試**: Pytest。
      * **(未來擴展)** 物理模擬: PyBullet。

---

### **第五章：應用程式架構與模組設計**

**\[重要提示] 本章詳述 v1.7 引入並持續優化的四層架構，是理解程式碼結構與數據流的關鍵，後續開發應遵循此架構模式。**

  * **整體架構 (四層)**：

    1.  **狀態管理層 (`StateManager`)**: 唯一數據中心，儲存全局狀態。
    2.  **核心計算層 (`kinematics_engine.py`, `dynamics_engine.py`, `drive_system_engine.py`)**: 提供無狀態的、底層的數學計算功能。
    3.  **高層分析層 (`analysis_engine.py`)**: 執行耗時的、基於核心計算的複雜分析任務，通常在獨立執行緒中運行。
    4.  **使用者介面與協調層 (GUI)**:
          * **`main_window.py`**: UI 總管 與事件協調器。
          * **`controls/`**: 各功能面板 (儀表板)。
          * **`pyvista_widget.py`**: 3D 渲染畫布。
          * **`geometry_widget.py`**: UI 元件工廠。

  * **關鍵架構特性**：

      * **單向數據流**: 數據主要從後端流向前台，UI 操作透過信號觸發後端計算。
      * **執行緒安全**: 耗時分析 (`AnalysisEngine`) 在獨立 `QThread` 執行，透過「任務資料包」傳遞數據，`pyqtSignal` 安全回傳結果。
      * **依賴注入**: `GeometryWidget` 作為無狀態 UI 工廠注入子元件，實現複用並避免狀態污染。
      * **模式切換**: 使用 `QStackedWidget` 切換主分析模式和驅動系統儀表板。
      * **導航模式**: 使用 `QMenuBar` + `QActionGroup` + `QScrollArea` 進行導航和佈局管理。

  * **模組職責詳述**：

      * **`StateManager`**: 儲存 `project_path`, `workspace_limits`, `platform_type`, `is_dirty`, `max_force_result`, `joint_angle_ranges` 等全局狀態。提供 `get/set` 接口。
      * **`kinematics_engine.py` (CoreEngine)**: 負責 IK, FK, 半徑計算, 工作空間邊界分析, 姿態驗證 (含角度限制), 專案參數 JSON 序列化/反序列化。**本身無狀態**。
      * **`dynamics_engine.py`**: 負責逆動力學計算，輸入姿態、運動參數，輸出致動器力。
      * **`drive_system_engine.py`**: 負責驅動系統計算 (雙模式扭矩、壽命等)。
      * **`analysis_engine.py`**: 獨立執行緒工作者，執行 LHS+SQP 全域分析、LHS 節點擺角分析。**無狀態**，透過任務資料包接收數據。
      * **`main_window.py` (核心協調器)**: 組合 UI；接收 UI 信號，調用後端；管理數據流 (單位轉換)；創建/管理 `AnalysisEngine` 執行緒；協調視覺化選項。
      * **`controls/*.py` (UI 面板)**: 顯示數據；監聽使用者操作；發射信號通知 `MainWindow`。**不含複雜 logique**。
      * **`pyvista_widget.py`**: 渲染 3D 場景；提供視角控制接口。
      * **`database_manager.py`**: 封裝 SQLite 操作；處理 Excel 匯入 (Pandas)。
      * **`report_generator.py`**: 使用 FPDF 生成 PDF；處理字體註冊。

---

### **第六章：變數與參數定義**

**\[重要提示] 本章是理解程式中所有計算的基礎，定義了各參數的物理意義、單位和計算來源。後續維護者在修改計算 logique 或新增參數時，必須同步更新本章定義。**

**\[v4.6 規格更新] 本章 6-DOF 和 3-DOF 幾何參數已被修正，以匹配 v4.6 驗證通過的「數值求解器」、「反向拓樸」 及「質心」 邏輯。**

**\[核心計算單位系統標準 (v3.0 規格)]**

> **為確保內部計算的一致性與準確性，專案核心計算層（`core/` 目錄下的所有模組）嚴格遵守以下標準單位系統。單位轉換僅在與使用者介面 (GUI) 或外部文件 (報告、JSON) 交互的邊界進行 (通常由 `MainWindow` 或 `utils.py` 處理)。**
>
>   * **長度**: **毫米 (mm)**
>   * **角度**: **弧度 (rad)**
>   * **質量**: **公斤 (kg)**
>   * **時間**: **秒 (s)**
>   * **力**: **牛頓 (N)**
>   * **扭矩**: **牛頓毫米 (N·mm)**
>   * **轉動慣量**: **公斤毫米平方 (kg·mm²)**
>   * **速度**: **mm/s**
>   * **加速度**: **mm/s²**
>   * **角速度**: **rad/s**
>   * **角加速度**: **rad/s²**

  * **單位約定 (GUI vs 核心)**：

      * **GUI 層 (使用者輸入/顯示)**: 長度 **毫米 (mm)** | 角度 **度 (°)** | 加速度 **重力加速度 (g)**。
      * **核心引擎層 (內部計算)**: **遵循上述核心計算單位系統標準**。
      * **數據流**: `MainWindow` 或 `utils.py` 負責在 GUI 與核心層之間進行單位轉換。

  * **平台幾何參數 (Geometry) - 6-DOF (v4.3 修正)**：

      * **(核心輸入)** `d_f`: **固定平台**相鄰節點間的**短弦長 (mm)**。 (e.g., $A_1, A_2$)
      * **(核心輸入)** `D_f`: **固定平台**相鄰節點間的**長弦長 (mm)**。 (e.g., $A_2, A_3$)
      * **(核心輸入)** `D_m`: **活動平台**相鄰節點間的**長弦長 (mm)**。 (e.g., $B_1, B_2$)
      * **(核心輸入)** `d_m`: **活動平台**相鄰節點間的**短弦長 (mm)**。 (e.g., $B_2, B_3$)
      * **(計算得出)** $R_A$: **固定平台**的**外接圓半徑 (mm)**。
          * **[v4.3 修正] 描述性邏輯**: $R_A$ 是通過**數值求解器** 得到的 $R$ 值，該值滿足 `3*alpha_f + 3*beta_f = 2*pi`，其中 `alpha_f = 2*asin(D_f/2R)` 且 `beta_f = 2*asin(d_f/2R)`。
      * **(計算得出)** $R_B$: **活動平台**的**外接圓半徑 (mm)**。
          * **[v4.3 修正] 描述性邏輯**: $R_B$ 是通過**數值求解器** 得到的 $R$ 值，該值滿足 `3*alpha_m + 3*beta_m = 2*pi`，其中 `alpha_m = 2*asin(D_m/2R)` 且 `beta_m = 2*asin(d_m/2R)`。

  * **平台幾何參數 (Geometry) - 3-DOF (v4.6 修正)**：

      * **(核心輸入)** `D_1`: **固定平台**等腰三角形的**底邊長度 (mm)**。 ( $A_1$ 到 $A_5$ )
      * **(核心輸入)** `D_2`: **固定平台**等腰三角形的**垂直高度 (mm)**。 (從 $A_1-A_5$ 底邊中點到 $A_3$)
      * **(核心輸入)** `d_1`: **活動平台**等腰三角形的**底邊長度 (mm)**。 ( $B_1$ 到 $B_5$ )
      * **(核心輸入)** `d_2`: **活動平台**等腰三角形的**垂直高度 (mm)**。 (從 $B_1-B_5$ 底邊中點到 $B_3$)
      * **(計算得出)** $C_{yA}$: **固定平台**質心 Y 偏移（相對於底邊中點）。 $C_{yA} = D_2 / 3$。
      * **(計算得出)** $C_{yB}$: **活動平台**質心 Y 偏移（相對於底邊中點）。 $C_{yB} = d_2 / 3$。
      * **(計算得出)** $R_a, R_b$: 3-DOF 模式下不使用外接圓半徑 $R$，回傳 0.0。

  * **平台通用參數 (Geometry - Common)**：

      * **(核心輸入)** `$\theta$` (**Phase Angle**): **相位角 (rad** 或 deg)。
          * **物理意義**: 僅用於 6-DOF 平台。由設計師**主動選擇**的參數，定義了活動平台 $\{B\}$ 相對於固定平台 $\{A\}$ **預先設定的 $Yaw$（Z 軸）旋轉量**。(3-DOF 平台的相位角固定為 $0^\circ$)。
      * **(計算得出)** `h`: **平台初始高度 (Initial Height) (mm)**。
          * **物理意義**: 當平台處於設計的零位姿態 $(Roll=0, Pitch=0, Yaw=\theta)$ 時，可達到的**最低** $Z$ 軸高度。
          * **計算邏輯**: 此高度 $h$ 是由 $N$ 根致動器中，那一根**水平投影距離最小** ($D_{xy, \min}$) 的致動器最先達到其**最小物理長度 $L$** 所決定的。 $h = \sqrt{L^2 - D_{xy, \min}^2}$。
      * **(計算得出)** `H`: **自然平衡零位高度 (Natural Balance Zero Position Height) (mm)**。
          * **物理意義**: 當平台處於零位姿態 $(Roll=0, Pitch=0, Yaw=\theta)$ 時，能使其 $N$ 根（N=6 或 3）致動器 $L_i$ 的**平均長度**等於**零位中間點長度 $L_{mid}$** (`L + s_buffer/2 + s/2`) 時的 $Z$ 軸高度。這是軟體中所有相對運動的**參考原點 (零點)**。
          * **計算邏輯**: $H$ 是 $Z$ 的解，滿足 $\frac{1}{N} \sum_{i=1}^{N} \sqrt{Z^2 + D_{xy, i}^2} = L_{mid}$。此方程需要使用**數值迭代法**求解 $Z$。
      * **(計算得出)** `$D_{xy, i}$`：第 $i$ 根致動器在零位姿態下的**水平投影距離 (mm)**。
          * **描述性邏輯**: 這是計算 $h$ 和 $H$ 的關鍵中間值。它代表在零位姿態下，固定節點 $A_i$ 和活動節點 $B_i$ 之間的 $XY$ 平面距離。

  * **電動缸參數 (Actuator)**：

      * **(核心輸入)** `L`: 電動缸的**最小機械長度 (mm)**。即完全收縮時的物理長度 $L_{min}$。
      * **(核心輸入)** `s`: 電動缸的**可用行程 (Usable Stroke) (mm)**。使用者定義的、用於運動規劃的最大伸長量。
      * **(核心輸入)** `s_buffer`: 電動缸兩端預留的**安全裕量總和 (mm)**。用於防止硬碰撞的緩衝區。
      * **(計算得出)** `s_mech`: 電動缸的**總機械行程 (mm)**。
          * **計算邏輯**: `s_mech = s + s_buffer`。代表從完全收縮 (長度 L) 到完全伸展 (長度 L + s\_mech) 的總物理行程。
      * **(計算得出)** `$L_{mid}$`：**零位中間點長度 (mm)**。
          * **計算邏輯**: $L_{mid} = L + \frac{s_{buffer}}{2} + \frac{s}{2}$。
      * `l_i`: 第 $i$ 根電動缸的**當前長度 (mm)**。
          * **可用運動範圍 (Usable Range)**: $[L + s_{buffer}/2, \quad L + s_{buffer}/2 + s]$。這是工作空間分析「可用行程」模式和姿態控制滑塊的預設操作範圍。零位 H 對應此範圍的中點。
          * **機械極限範圍 (Mechanical Range)**: $[L, \quad L + s_{mech}]$。這是工作空間分析「機械極限」模式所使用的範圍，代表物理上的最大可能行程。

  * **關節與角度參數 (Joint & Angle)**：

      * **(核心輸入)** `enable_joint_limits`: 是否啟用關節角度限制分析 (布林值)。
      * **(核心輸入)** `base_joint_limit`: **固定平台**萬向節最大允許擺角 (**rad** 或 deg)。相對於 Z 軸法線。
      * **(核心輸入)** `platform_joint_limit`: **活動平台**萬向節最大允許擺角 (**rad** 或 deg)。相對於平台自身法線。
      * **(核心輸入)** `platform_joint_style`: 活動平台關節安裝方式 ('top' 上置式 或 'bottom' 下置式)。影響角度計算的物理模型。預設為 'bottom'。
      * **(核心輸入)** `θ` (`install_angle`): 電動缸**安裝角度 (rad** 或 deg)。指電動缸軸線與水平面的夾角。主要影響驅動系統**手動模式**下的負載計算。

  * **動力學參數 (Dynamics)**：

      * **(核心輸入)** `platform_mass`: **活動平台**自身質量 (**kg**)。
      * **(核心輸入)** `load_mass`: 附加於活動平台的**負載質量 (kg)**。
      * **(核心輸入)** `load_com_x/y/z`: 負載**質心 (COM)** 相對於活動平台幾何中心的**偏移量 (mm)**。
      * **(核心輸入)** `I`: 平台與負載的**轉動慣量張量 (kg·mm²)**。
          * **(規格註記)**：此參數**必須**由使用者根據平台與負載的實際幾何分佈計算並輸入，單位為 **kg·mm²**。
      * **(核心輸入)** `lin_accel`: 平台期望的**線性加速度向量 (mm/s²)**。
      * **(核心輸入)** `ang_accel`: 平台期望的**角加速度向量 (rad/s²)**。
      * **(核心輸入)** `$K_d$` (`dynamic_amplification_factor`): **動態放大係數**。
          * **(規格註記)**：一個無單位的工程安全係數 (預設 1.0)。用於放大慣性項，以補償模型中被簡化的動力學效應 (如 $\boldsymbol{\omega} \times (\mathbf{I}\boldsymbol{\omega})$ 項)。
      * `g`: 重力加速度 (常數, 9810 **mm/s²**)。

  * **驅動系統參數 (Drive System)**：

      * **(核心輸入/來自資料庫)** `$P_h$` (`lead_mm`): 螺桿**導程 (mm/rev)**。
          * **物理意義**: 馬達（或螺桿本身）旋轉一圈，螺桿螺母直線移動的距離。
      * **(核心輸入/來自資料庫)** `$i$` (`ratio`): **減速比**。
          * **物理意義**: 馬達轉速與最終輸出（螺桿）轉速的比值。無單位。
      * **(核心輸入/來自資料D庫)** `η_screw`: **螺桿效率 (%)**。
      * **(核心輸入/來自資料庫)** `η_pulley`: **傳動效率 (%)**。
      * **(核心輸入)** `m` (`moving_part_mass`): **移動件總質量 (kg)**。用於**手動模式**。
      * **(核心輸入)** `a_max`: **最高加速度 (mm/s²)**。用於**手動模式**。
      * **(核心輸入)** `μ` (`friction_coefficient`): **摩擦係數**。用於**手動模式**。
      * **(核心輸入)** `$f_w$` (`load_factor`): **負荷係數**。
          * **(規格註記)**：一個綜合的工程係數 (e.g., 1.2-1.5)，**必須**用於補償衝擊、震動、溫度，**以及所有在主動力學模型中未計算的摩擦力（如關節、導軌等）**。
      * **(核心輸入)** `R_h`, `R_d`, `R_y`: **每日/年運行小時/天數/年數**。用於計算總需求壽命 `L_T`。
      * **(核心輸入)** `Aval` (`availability_factor`): **稼動率 (%)**。
      * **(資料庫屬性)** `$C_a$` (`ca_n`): 螺桿的**基本額定動負荷 (N)**。
      * **(計算得出)** `$F_{eq}$` (`average_axial_load`): **平均(等效)軸向負載 (N)**。
      * **(計算得出)** `$L_T$` (`required_life_hours`): **需求壽命 (小時)**。
      * **(計算得出)** `$C_{ac}$` (`required_dynamic_load`): **需求動態負荷 (N)**。
      * **(計算得出)** `$L_t$` (`estimated_life_hours`): **預估壽命 (小時)**。
      * **(計算得出)** `$T_{motor\_req}$` (`required_motor_torque`): **需求馬達扭矩 (N·mm)**。

  * **姿態參數 (Pose)**：

      * $\mathbf{T} = (T_x, T_y, T_z)$: 平台的**平移向量 (mm)** (Surge, Sway, Heave)，相對於自然平衡零位 H。
      * $\mathbf{R} = (\phi, \theta, \psi)$: 平台的**旋轉歐拉角 (rad** 或 deg) (Roll, Pitch, Yaw)，相對於自然平衡零位。*(核心計算使用 rad)*。

  * **分析結果 (Results)**：

      * `workspace_limits`: 工作空間各自由度的**最大/最小值** (mm 或 deg)。
      * `max_force_static / max_force_dynamic`: 全域分析得出的**最大致動器出力 (N)** 及其對應的**姿態** (mm, rad)。
      * `joint_angle_ranges`: 各關節 (A1-A6, B1-B6) 在工作空間内所需的**最大擺角 (deg)**。

---

### **第七章：數學模型與核心演算法**

**\[重要提示] 本章定義了軟體核心計算所依賴的數學模型與演算法。這些是軟體功能的基石，後續維護者若修改底層計算 logique，必須同步更新本章內容，並確保與第六章的變數定義一致。**

**\[v4.6 規格更新] 本章 6-DOF 和 3-DOF 幾何模型已被修正，以匹配 v4.6 驗證通過的「數值求解器」、「反向拓樸角度累加」 及「質心」 邏輯。**

#### **7.1 平台幾何模型與標準方位**

**\[規格定義] 本節定義了 3-DOF 與 6-DOF 平台的基礎座標系、節點位置與連接方式。**

  * **基準座標系 (Coordinate System)**:

      * **原點**: `(0, 0, 0)` 固定於**固定平台 (Base Platform)** 的幾何中心。
          * **(6-DOF)**：幾何中心指外接圓圓心。
          * **(3-DOF) [v4.6 修正]**：3-DOF 平台的**座標原點**被**明確定義**在**固定平台等腰三角形的質心 (Centroid)**。
      * **座標軸**: 採用標準**右手座標系 (Right-Handed Coordinate System)**。
          * `+Z` 軸: 垂直**向上**。
          * `(6-DOF)` `+Y` 軸: 水平**向前** (朝向 $A_3, A_4$ 節點對的中心線)，`+X` 軸: 水平**向右** (朝向 $A_1, A_2$ 節點對的中心線)。
          * `(3-DOF) [v4.6 修正]` `+Y` 軸: 沿著固定平台三角形的**高度**（從質心 指向 $A_3$），`+X` 軸: 沿著固定平台**底邊**（從質心 指向 $A_5$）。

  * **節點座標計算邏輯 (`_get_canonical_nodes`)**:

      * **6-DOF (v4.3 修正)**:

        1.  **[v4.3 修正] 計算半徑 R**: 根據弦長 $d_f, D_f$ 和 $d_m, D_m$，使用**數值求解器** (如 `scipy.optimize.root_scalar`) 求解 `3*alpha + 3*beta = 2*pi`，以分別獲得 $R_A$ 和 $R_B$。
        2.  **[v4.3 修正] 定義節點角度**:
              * 計算 4 個基礎角度：$\alpha_f$ (A 長弦), $\beta_f$ (A 短弦), $\alpha_m$ (B 長弦), $\beta_m$ (B 短弦)。
              * **固定節點 $\phi_{Ai}$**：使用**角度累加法** 實現 (A1,A2)=短( $\beta_f$ ), (A2,A3)=長( $\alpha_f$ ) 的\*\*「Short-Long」\*\*拓樸。
              * **活動節點 $\phi_{Bi}$**：使用**角度累加法** 實現 (B1,B2)=長( $\alpha_m$ ), (B2,B3)=短( $\beta_m$ ) 的\*\*「Long-Short」\*\*拓樸。
              * **標準方位**：應用旋轉使節點對（例如 A1/A2 和 B1/B2）圍繞 X 軸對稱。
        3.  **計算座標**:
              * 固定節點 $A_i$ (在 $z=0$): $A_i = [R_A \cos(\phi_{Ai}), R_A \sin(\phi_{Ai}), 0]$
              * 活動節點 $B_i^0$ (自身座標系 $z=0$): $B_i^0 = [R_B \cos(\phi_{Bi}), R_B \sin(\phi_{Bi}), 0]$

      * **3-DOF (v4.6 修正)**:

        1.  **[v4.6 修正] 描述性邏輯**: 根據 $D_1, D_2$ 和 $d_1, d_2$ 及 3-DOF 質心 座標系定義，直接計算節點座標。
        2.  **[v4.6 修正] 固定節點 $A_i$ (在 $z=0$)**:
              * $A_1 = [-D_1/2, -D_2/3, 0]$
              * $A_5 = [D_1/2, -D_2/3, 0]$
              * $A_3 = [0, 2D_2/3, 0]$
        3.  **[v4.6 修正] 活動節點 $B_i^0$ (自身座標系 $z=0$)**:
              * $B_1 = [-d_1/2, -d_2/3, 0]$
              * $B_5 = [d_1/2, -d_2/3, 0]$
              * $B_3 = [0, 2d_2/3, 0]$

  * **連桿連接方式 (Link Connection)**:

      * **[v4.3 修正] 6-DOF**: 對位直連 $A_i \to B_i$ (i=1..6)。
      * **[v4.5 修正] 3-DOF**: 對位直連 $A_1 \to B_1$, $A_3 \to B_3$, $A_5 \to B_5$。

  * **水平投影距離計算 ($D_{xy, i}$)**:

      * **描述性邏輯**: 這是計算 $h$ 和 $H$ 的關鍵步驟，計算在零位姿態下 (即 $Roll=0, Pitch=0, Yaw=\theta$)，第 $i$ 根連桿兩端點 $A_i$ 和 $B_i$ 之間的 $XY$ 平面距離。
      * **[v4.3 修正] 6-DOF**:
          * $A_i$ 座標 $(x, y) = (R_A \cos(\phi_{Ai}), R_A \sin(\phi_{Ai}))$
          * $B_i$ 座標 $(x, y) = (R_B \cos(\phi_{Bi} + \theta), R_B \sin(\phi_{Bi} + \theta))$ (已應用相位角 $\theta$)
          * $$D_{xy, i} = || \text{Coord}(A_i) - \text{Coord}(B_i) ||$$
            *(即 $A_i$ 和 $B_i$ 的歐氏距離)*
      * **[v4.6 修正] 3-DOF**: (零位 $Yaw=0$)
          * $D_{xy, 1} = \sqrt{\left(\frac{D_1 - d_1}{2}\right)^2 + \left(\frac{D_2 - d_2}{3}\right)^2}$
          * $D_{xy, 5} = \sqrt{\left(\frac{D_1 - d_1}{2}\right)^2 + \left(\frac{D_2 - d_2}{3}\right)^2}$
          * $D_{xy, 3} = \left|\frac{2(d_2 - D_2)}{3}\right|$

#### **7.2 運動學模型 (Kinematics)**

  * **逆向運動學 (Inverse Kinematics - IK)**:

      * **目標**: 已知平台期望位姿 $\mathbf{P} = (\mathbf{T}, \mathbf{R})$, 計算每根電動缸所需的長度 $l_i$。
      * **描述性邏輯**: 對於第 $i$ 根連桿，首先將活動平台節點 $B_i^0$ (在自身座標系) 透過旋轉矩陣 $\mathbf{R}$ 和平移向量 $\mathbf{T}$ 轉換到世界座標系得到 $B_i = \mathbf{T} + \mathbf{R} \cdot \mathbf{B}_i^0$。然後計算 $B_i$ 與固定平台對應節點 $A_i$ 之間的歐氏距離，即為所需的腿長 $l_i$。
      * **公式**: $l_i = || (\mathbf{T} + \mathbf{R} \cdot \mathbf{B}_i^0) - \mathbf{A}_i ||$。

  * **正向運動學 (Forward Kinematics - FK)**:

      * **目標**: 已知所有電動缸的實際長度 $l_{i, \text{given}}$, 求解平台實際達到的位姿 $\mathbf{P} = (\mathbf{T}, \mathbf{R})$。
      * **描述性邏輯**: 這是一個複雜的非線性問題。程式使用**數值優化**方法 (如 `scipy.optimize.least_squares` 在 `_solve_fk` 中)，從一個初始猜測位姿開始，不斷調整 $\mathbf{T}$ 和 $\mathbf{R}$，直到計算出的腿長與給定腿長的平方誤差總和最小化。

  * **姿態驗證 (Pose Validation - `is_pose_valid`)**:

      * **目標**: 檢查給定的期望姿態 $\mathbf{P}$ 是否在物理上可達。
      * **描述性邏輯**:
        1.  使用 IK 計算姿態 $\mathbf{P}$ 所需的腿長 $l_i$。
        2.  檢查所有 $l_i$ 是否落在允許的**行程範圍**內 (可用範圍或機械極限範圍，取決於模式)。
        3.  如果啟用了角度限制，則進一步計算姿態 $\mathbf{P}$ 下每個固定關節和活動關節的實際**擺動角度**。
        4.  角度計算時，根據使用者選擇的**關節安裝方式** ('top'/'bottom') 使用正確的法線向量。
        5.  檢查計算出的擺角是否**小於等於**設定的角度限制 (`base_joint_limit`, `platform_joint_limit`)。
        6.  只有當**所有**檢查 (行程和角度) 都通過時，該姿態才被認為是有效的。

  * **高度 h 和 H 的計算 (v3.0 規格)**:

      * **$h$ (初始高度)**：
          * **邏輯**: 由最先達到 $L_{min}$ (即 $L$) 的致動器決定，即 $D_{xy, i}$ 最小的那根。
          * **公式**: $h = \sqrt{L^2 - D_{xy, \min}^2}$
              * (6-DOF) $D_{xy, \min} = \min(D_{xy, 1}, \dots, D_{xy, 6})$
              * (3-DOF) $D_{xy, \min} = \min(D_{xy, 1}, D_{xy, 3})$ (因為 $D_{xy, 1} = D_{xy, 5}$)
      * **$H$ (自然平衡零位高度)**：
          * **邏輯**: 定義為在零位姿態 $(Yaw=\theta)$ 下，使 N 根致動器的**平均長度**等於 $L_{mid}$ (`L + s_buffer/2 + s/2`) 時的 $Z$ 軸高度。
          * **公式**: $H$ 是 $Z$ 的解，滿足 $\frac{1}{N} \sum_{i=1}^{N} \sqrt{Z^2 + D_{xy, i}^2} = L_{mid}$ (N=6 或 3)。
          * **(規格註記)**：此方程**必須**使用數值迭代法 (如 `scipy.optimize.root_scalar`) 求解 $Z$ (即 $H$)。

#### **7.3 動力學模型 (Dynamics)**

  * **逆動力學 (Inverse Dynamics - ID)**:
      * **目標**: 已知平台期望運動及負載，計算致動器所需力 $F_i$ (**N**)。
      * **描述性邏輯**: 根據牛頓-歐拉方程，已知期望的運動 (加速度) 和其他力 (重力)，求解電動缸需要提供多少力。這通常透過**雅可比矩陣 (Jacobian Matrix)** $\mathbf{J}$ 來實現。通過計算總的外部 Wrench $\mathbf{W}_{ext}$ (包含重力和慣性)，然後求解 $\mathbf{F} = (\mathbf{J}^T)^{-1} \cdot \mathbf{W}_{ext}$ 即可得到 $F_i$。
      * **核心方程**: $\mathbf{J}^T \cdot \mathbf{F} = \mathbf{W}_{ext}$
      * **雅可比矩陣 $\mathbf{J}$**: 其計算需要平台的瞬時幾何狀態。
      * **總外力/力矩 $\mathbf{W}_{ext}$ (N, N·mm)**:
          * $\mathbf{W}_{gravity}$: 由總質量 (**kg**) 和質心位置 (**mm**) 計算重力 (**N**) 和力矩 (**N·mm**)。
          * $\mathbf{W}_{inertia}$: 由線加速度 (**mm/s²**)、角加速度 (**rad/s²**) 和轉動慣量 `I` (**kg·mm²**) 計算慣性力 (**N**) 和力矩 (**N·mm**)。(註：忽略 $\boldsymbol{\omega} \times (\mathbf{I}\boldsymbol{\omega})$ 項)。
          * **(規格)**：$\mathbf{W}_{ext} = \mathbf{W}_{gravity} + \mathbf{K_d} \cdot \mathbf{W}_{inertia}$ (其中 $K_d$ 是動態放大係數)。
      * **(開發規格：單位轉換)**：
          * **(規格)**：慣性力 $F_{inertia} = m\mathbf{a}$ (kg·mm/s²) 轉換為 N 時，**必須除以 1000**。
          * **(規格)**：慣性力矩 $T_{inertia} = \mathbf{I}\boldsymbol{\alpha}$ (kg·mm²/s²) 轉換為 N·mm 時，**必須除以 1000**。

#### **7.4 驅動系統模型 (Drive System)**

**\[規格定義] 本節定義用於單個電動缸及其驅動系統選型的計算模型。**

  * **運動負載模型 (7-stage Model)**:
      * **目的**: 計算單缸在直線往復運動中各階段的軸向力 $F_i$ (**N**)。用於手動模式。
      * **公式**: $F_{ua} = m \cdot g \cdot (\sin\theta + \mu \cdot \cos\theta) + m \cdot a$ ...等。
        *(註: 需確保 $m$ (kg), $g$ (mm/s²), $a$ (mm/s²) 單位一致並最終轉換為 N)*。
  * **平均(等效)軸向負載 ($F_{eq}$)**:
      * **目的**: 將變動負載 $F_i$ (**N**) 折算成對壽命影響等效的恆定負載 (**N**)。
      * **公式**: $F_{eq} = \left( \frac{\sum |F_i|^3 \cdot t_i}{\sum t_i} \right)^{1/3}$
  * **壽命計算 ($L_t$) 與需求動負荷 ($C_{ac}$)**:
      * **目的**: 預估壽命 (**小時**) 或反算螺桿規格需求 (**N**)。
      * **公式**: $L_t = \left(\frac{C_a}{F_{eq} \cdot f_w}\right)^3 \cdot \frac{10^6}{60 \cdot n_{eq}}$ ; $C_{ac} = F_{eq} \cdot f_w \cdot \left( \frac{60 \cdot n_{eq} \cdot L_T}{10^6} \right)^{1/3}$
  * **扭矩計算模型 ($T_{motor\_req}$)**:
      * **目的**: 計算驅動單缸所需的馬達扭矩 (**N·mm**)。
      * **公式 (N·mm)**: $T_{motor\_req} = \frac{F_{max} \cdot P_h}{2 \pi \cdot \eta_{screw} \cdot i \cdot \eta_{pulley}}$

#### **7.5 核心分析演算法**

  * **工作空間邊界分析**:
      * **演算法**: **SLSQP** (`scipy.optimize.minimize`)。
      * **目標**: 尋找自由度 (mm 或 rad) 的邊界。
      * **約束**: 腿長 $l_i$ (mm) 在範圍內；(可選) 關節角 (rad) $\le$ 限制 (rad) (內積形式)。
      * **關鍵技術**: `_find_feasible_initial_pose` (用於尋找穩定求解起點); 變數正規化。
  * **全域最大出力分析**:
      * **演算法**: **LHS + SQP** 混合策略。
      * **目標**: 找到最大致動器出力 $|F_i|$ (**N**) 的姿態。
      * **邏輯**: LHS 採樣 (mm, rad) -\> `is_pose_valid` -\> SQP 優化 (目標函數調用 ID 計算 $F_i$ (**N**)) -\> 全局比較。
      * **模式**: 可選靜態/動態。
  * **節點擺角範圍分析**:
      * **演算法**: **LHS** 採樣。
      * **目標**: 找出每個關節所需的最大擺角 (**rad**, 輸出轉 deg)。
      * **邏輯**: LHS 採樣 (mm, rad) -\> `is_pose_valid` -\> 計算即時角度 (rad) -\> T追蹤最大值。

---

### **第八章：功能說明**

**\[重要提示] 本章概述軟體提供的核心功能 (基於 v3.0 規格)，後續若新增主要功能模組，應在此處添加摘要說明。**

本應用程式旨在提供一個整合的史都華平台設計與分析環境，主要功能包括：

1.  **參數化幾何設計**:
      * 支援六自由度 (6-DOF) 和三自由度 (3-DOF) 平台類型。
      * 提供直觀的介面輸入平台的關鍵幾何參數 (6-DOF: 弦長 D/d, **相位角 $\theta$**; 3-DOF: D1/D2, d1/d2; 通用: L, s, s\_buffer 等)，單位為 **毫米 (mm)**。
      * **[v4.6 修正]** 自動計算衍生參數 (6-DOF: $R_A, R_B$; 3-DOF: 質心座標; 通用: $D_{xy, i}$, $h$, $H$ 等)。
      * 提供即時的 3D 視覺化預覽。
2.  **運動學分析**:
      * **工作空間分析**: 計算平台在「可用行程」或「機械極限」下的六自由度運動範圍 (Tx, Ty, Tz (mm), Roll, Pitch, Yaw (°))。
      * **可選角度限制**: 可在工作空間計算中考慮萬向節的物理擺角限制。
      * **節點擺角範圍分析**: 計算在可用工作空間內，每個獨立關節 (A1-A6, B1-B6) 所需的最大擺動角度 (°)。
      * **姿態控制與驗證**: 提供滑塊手動控制平台姿態 (mm, °)，即時 3D 更新並驗證姿態是否可達 (考慮行程與角度限制)。
      * **自動姿態展演**: 可自動在計算出的工作空間範圍內演示平台的運動。
3.  **動力學分析**:
      * **逆動力學計算**: 計算在給定姿態、期望加速度 (g)、**轉動慣量 (kg·mm²)** 和 **動態放大係數 ($K_d$)** 下，各電動缸所需的瞬時出力 $F_i$ (N)。
      * **全域最大出力分析**: 自動搜索整個工作空間，找到使某根電動缸承受最大出力 $|F_i|$ (N) 的最差工況姿態點。
      * **靜態/動態模式可選**: 全域分析可選擇僅考慮重力 (靜態)，或同時考慮重力與慣性力 (動態)。
4.  **驅動系統設計輔助**:
      * **零件資料庫管理**: 提供 SQLite 資料庫管理介面 (增刪改查螺桿、馬達、皮帶輪)。
      * **Excel 匯入**: 支援從外部 Excel 檔案批量匯入零件數據。
      * **雙模式計算**: **自動模式** (基於 $F_{max}$) 和 **手動模式** (用於獨立直線致動器)。
      * **零件智慧篩選**: 根據需求扭矩 `$T_{motor\_req}$` (N·mm)、轉速 (rpm)、需求動負荷 `$C_{ac}$` (N) 和**負荷係數 ($f_w$)**，自動篩選零件組合，並進行壽命驗證 (`$L_t \ge L_T$`)。
      * **進階工程校核**: 提供最高線速度校核 (mm/s) 等。
      * **獨立存檔**: 手動模式設計可獨立儲存為 `.dsd` 文件。
5.  **專案管理與輸出**:
      * **專案存trx**: 保存/載入包含所有參數的 `.json` 文件。支援舊版 (`m` 單位) 文件自動轉換。
      * **PDF 設計報告**: 一鍵生成包含參數、結果、截圖的 PDF 報告。
6.  **使用者體驗**:
      * **即時 3D 視覺化**: 互動式 3D 視圖。
      * **可選節點座標顯示**: 顯示節點實時世界座標 (mm)。
      * **視角控制**: 多種視角操作及儲存/恢復。
      * **狀態追蹤**: 關閉前提示儲存。
      * **多執行緒分析**: 背景計算，進度反饋。
      * **菜單導航與獨立滾動區**: 改善佈局和操作流暢性。

---

### **第九章：技術規格與實現細節**

**\[重要提示] 本章記錄部分關鍵功能的技術實現細節，有助於理解程式碼內部 logique。**

  * **核心計算引擎 (`kinematics_engine.py`)**:
      * **[v4.3 修正]** **數學庫**: `NumPy`, `SciPy` (`optimize.least_squares`, `optimize.root_scalar`, `optimize.fsolve`, `optimize.minimize(method='SLSQP')`)。
      * **角度計算**: `np.arccos`, 約束用內積。
      * **單位**: **mm**, **rad**。
      * **穩定性**: `_find_feasible_initial_pose` (用於尋找穩定求解起點), 變數正規化。
      * **[v4.3 修正]** **半徑計算**: 6-DOF $R_A, R_B$ 透過**數值求解器** (`scipy.optimize.root_scalar`) 求解。
      * **[v4.6 修正]** **3-DOF 座標**: 3-DOF $A_i, B_i$ 座標 透過**質心**模型 代數計算。
      * **高度計算**: $h$ (初始高度) 透過 $D_{xy, \min}$ 代數計算。$H$ (零位高度) 透過數值求解器 (`scipy.optimize.root_scalar`) 求解平均長度方程。
  * **3D 視覺化 (`pyvista_widget.py`)**:
      * **渲染庫**: `pyvista`, `pyvistaqt.QtInteractor`。
      * **模型**: `pyvista.PolyData`, `pyvista.MultipleLines` / `pyvista.Tube`。
      * **渲染策略**: 「原地更新」(`actor.mapper.dataset.points = ...`)。
      * **視角**: `plotter.camera_position`。
      * **標籤**: `plotter.add_point_labels`。
      * **單位**: 繪圖前核心 `mm` 座標需乘以 `MM_TO_M` (0.001) 轉為 `m`。
  * **狀態管理 (`state_manager.py`)**: 簡單 Python 類別。
  * **高層分析引擎 (`analysis_engine.py`)**: `QObject`, `QThread`, `scipy.stats.qmc.LatinHypercube`, `pyqtSignal`。
  * **資料庫 (`database_manager.py`)**: `sqlite3`, `pandas.read_excel`。
  * **PDF 報告 (`report_generator.py`)**: `fpdf`, `fpdf.add_font`。
  * **GUI (`gui/` 目錄)**: `PyQt6`, `QMenuBar`, `QStackedWidget`, `QScrollArea`, `pyqtSignal`, `pyqtSlot`。

---

### **第十章：使用指南**

**\[重要提示] 本章提供基本的使用流程 (基於 v3.S UI)，後續若 UI 或操作流程有重大變更，應同步更新。**

**\[註] 本章描述的 UI 操作流程，旨在實現「第一部分：工程設計推演」中定義的階段。**

1.  **啟動**: 執行 `main.py`。
2.  **新建/讀取專案**: 使用「檔案」菜單。
3.  **模式選擇**: 使用菜單欄切換「幾何參數設計」、「動力學分析」、「驅動系統設計」。
4.  **幾何參數設計 (對應 階段一 & 階段二)**:
      * **輸入**: 在左側面板依序輸入平台幾何參數 (6-DOF: 弦長 D/d, **相位角 $\theta$** (°); 3-DOF: D1/D2, d1/d2; 通用: L, s, s\_buffer)。
      * **角度限制 (可選)**: 勾選「啟用...」，設定最大擺角 (°)。
      * **確認**: 點擊「**確認幾何參數**」。(系統執行 階段 2.3 的衍生計算)。
      * **工作空間**: 點擊「**計算可用工作空間**」。(系統執行 階段 2.3 的驗證點 1)。
      * **擺角分析 (可選)**: 點擊「**分析節點擺角範圍**」。(系統執行 階段 2.3 的驗證點 2)。
      * **姿态控制**: 拖動滑塊控制平台姿態，3D 視圖即時更新。
5.  **動力學分析 (對應 階段三)**:
      * **輸入**: 輸入「階段一」的負載參數 (kg, mm) 和運動需求 (g)，並輸入「階段三」的動力學參數 (**kg·mm²** [轉動慣量], **$K_d$** [動態放大係數])。
      * **執行**: 選擇「動態」模式，點擊「分析全域最大出力」。(系統執行 階段 3.2)。
      * **查看結果**: 獲取 $F_{max}$ (N)。
6.  **驅動系統設計 (對應 階段四)**:
      * **載入**: (首次) 匯入 Excel 數據。(系統自動載入 $F_{max}$)。
      * **輸入**: 輸入「階段一」的壽命需求 (R\_h, R\_d, R\_y) 和「階段四」的**負荷係數 ($f_w$)**。
      * **執行**: 點擊「計算與篩選零件」。(系統執行 階段 4.2 的篩選與驗證)。
      * **查看結果**: 查看合格的零件列表。
7.  **生成報告 (對應 階段五)**:
      * 「檔案」-\>「生成 PDF 設計報告」。
8.  **專案存檔 (對應 階段五)**:
      * 「檔案」-\>「儲存專案 / S另存專案」。

---

### **第十一章：推薦的第三方函式庫與資源**

**\[重要提示] 本章列出專案依賴的關鍵外部函式庫，有助於理解技術選型。**

1.  **GUI 框架**: `PyQt6`
2.  **3D 視覺化**: `PyVista` 與 `PyVistaQT`
3.  **數值計算**: `NumPy` 與 `SciPy`
4.  **資料處理**: `Pandas`
5.  **資料庫**: `SQLite` (Python 內建)
6.  **PDF 生成**: `FPDF`
7.  **測試框架**: `Pytest`
8.  **物理模擬 (未來擴展)**: `PyBullet`

---

**任務完成提示：**

